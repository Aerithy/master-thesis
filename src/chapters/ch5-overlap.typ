#import "../../template/src/algorithm.typ": pseudocode-list
#import "../../template/src/constant.typ": font-type

= 通信受限场景下的混合并行通信掩盖策略

== 引言

单数据中心互联（如 InfiniBand/RDMA）相比，广域网（WAN）通常只有 10–30 Gb/s 量级带宽、30–100 ms 往返时延（RTT），使得同步训练中的通信开销直接进入关键路径，造成 GPU 长时间空闲与吞吐下降 @jain2013b4。

从云边端协同训练的视角，当训练以跨域数据并行（DP）为并行策略骨架时，梯度的 All-Reduce 同步集合通信过程必须跨越域间的 WAN 链路，从而在每一次训练迭代的反向传播后形成显著的同步尾部。域内的设备持续将数据/梯度贡献汇入域间的网络出口，造成了严重的域间通信瓶颈，进一步放大 WAN 抖动对全局吞吐的影响。

#figure(
  image("../../image/ch5_pp_across_dcs.png", width: 75%),
  caption: [跨域流水线并行：激活数据在域间传输，产生域间通信瓶颈]
) <fig:cross-dc-pp>

从系统角度看，跨域训练的难点不只在于带宽更低，同时存在着通信时延更高且更难被隐藏的问题。当 RTT 达到毫秒量级时，许多传统在单数据中心可忽略的等待（例如一次集合通信的启动、同步点的排队）都会叠加成显著的迭代尾部。与此同时，跨域的网络抖动还会放大同步屏障对全局吞吐的影响：任何一个域内计算进程的短暂变慢，都可能让所有的计算资源在屏障处空等。

=== 并行策略骨架的选择

在跨域训练的环境中，以流水线并行为骨架会迫使训练样本/激活在域间频繁穿梭，入口与跨段链路容易成为瓶颈，如@fig:cross-dc-pp 所示 @crosspipe2025。相对地，以数据并行为主要策略将前/后向计算限定在域内，仅在迭代末进行梯度同步，天然契合数据就地处理，并对网络波动更鲁棒，如@fig:cross-dc-dp 所示。

#figure(
  image("../../image/ch5_dp_across_dcs.png", width: 75%),
  caption: [跨域数据并行 + 域内流水线并行：各域处理本地数据，仅跨 WAN 同步梯度]
) <fig:cross-dc-dp>

为进一步说明跨域数据并行优于跨域流水线的结构性原因，可以用通信量随 batch 规模的增长趋势来刻画。设共有 $D$ 个域，每个域需要处理本域内的数据 batch $b$，则全局 batch 为 $B = D dot b$。令 $alpha$ 表示每个参数参与同步的字节数（与精度/压缩有关），$P$ 表示模型参数规模（以参数个数或字节计），$A$ 表示当流水线切分跨域训练时每个样本需要跨 WAN 传输的激活（及其反向激活梯度）大小。

当跨域训练采用数据并行时，跨 WAN 的通信主要来自每步一次的梯度同步，其通信量近似与模型规模相关：

$ V_"DP" approx alpha dot P $

在模型固定时，$V_"DP"$ 随 $B$ 增加近似不变，从而更容易通过增大 batch 或提升域内计算并行来摊薄 WAN 固定开销。

当跨域训练采用流水线并行时，一旦流水线切分跨域，则每个 micro-batch 都需要跨 WAN 传输激活，迭代内 WAN 流量与样本数近似线性增长：

$ V_"PP" approx Theta(B dot A) $

并且流水线并行的跨域传输位于受限通信链路的路径之上，并且每个流水线 stage 间的计算强依赖于流水线间的激活值传输，一旦 WAN 出现波动，容易级联放大为全流水线停顿。因此，本文选择跨域进行数据并行和域内采用流水线的层次化骨架，以在数据就地训练的前提下，尽可能将 WAN 通信从强依赖链路中剥离。

综上，本章聚焦本文设定的层次化混合并行：跨域采用数据并行，域内采用流水线并行或其他的模型并行方法。该并行策略的设置避免了跨域传输激活/样本的开销，但数据并行策略骨架引入了新的关键瓶颈：跨域梯度同步尾部。

=== 同步尾部成为主要瓶颈

在 WAN 延迟主导时，即使框架在反向传播期间做了梯度分桶来使梯度同步 All-Reduce 与计算重叠，也容易在每次训练迭代末尾出现无法被剩余计算掩盖的通信同步阻塞尾部。原因在于，WAN 的 $T_"ar"$ 可能大于单个 bucket 之后剩余的可用计算窗口，导致最后几个 bucket 的同步受限于串行堆叠，形成长尾。@fig:dppp-timebreakdown 展示了 DP+PP 配置下跨域与域内开销分解：跨域同步在每步中占据一定比例，导致 GPU 等待。

// #figure(
//   image("../../supplementary/images/bar_time_by_network.png", width: 75%),
//   caption: [DP+PP 配置下每步计算/通信开销分解：跨域同步成为主要等待来源]
// ) <fig:dppp-timebreakdown>

#figure(
  image("../../image/ch5-dppp-timebreakdown.svg", width: 75%),
  caption: [DP+PP 配置下每步计算/通信开销分解：跨域同步成为主要等待来源]
) <fig:dppp-timebreakdown>

面对同步尾部，一类直观做法是弱化同步（如 Local-SGD），该类做法通过在各个计算域内部维护一个局部模型，并通过约定好的同步频率来维护各个域的模型参数一致，但这类方法的本质是降低同步频率而非消除同步尾部，且会引入更强的优化漂移风险 @stich2019local。本章提出在通信受限场景下的混合并行通信掩盖策略（本文简称其为 POLAR-SGD）：在不改变“每迭代一次全局同步”这一基本语义的前提下，通过 前缀触发的异步 All-Reduce + 预测误差修正，创造了一个更早、更长的可重叠窗口（在 micro-batch 前缀处触发一次 all-reduce 集合通信），让整个跨 WAN 的 All-Reduce 得以被后续反向传播的通信所掩盖，从而将同步从阻塞尾部中去除。

== 问题分析与研究思路

考虑一个 DP+PP 混合训练过程：为减少显存负载压力并缓解各阶段 GPU 气泡（Bubble），系统通常将一个全局迭代（step）的单个批次（mini-batch）进一步切分为 $M$ 个更细粒度的微批次 (micro-batch）。在执行引擎层面，常采用 1F1B（One-Forward-One-Backward）调度执行，即打破单纯的串行执行逻辑，令处于流水线前后的设备交替执行前向与反向传播。但在单次迭代的末尾，这种流水线必须经历一个排空阶段（Flush），不再下发新的数据批次，最终使得所有设备退避到同步状态。

在本章的实验拓扑映射中，跨域使用 DP（DP 组大小为 $N$），同一 域 内使用 PP（用于模型切分与计算流水线）。

#figure(
  table(
    columns: (auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*符号*], [*含义*]),
    table.hline(stroke: 0.5pt),
    [$M$], [每个 mini-batch 的 micro-batch 数],
    [$m$], [micro-batch 索引],
    [$t$], [全局迭代索引],
    [$r$], [DP 组内 worker（rank）索引],
    [$g_(r,t,m)$], [worker $r$ 在迭代 $t$、micro-batch $m$ 的梯度],
    [$g_(r,t)^(tau)$], [前缀累积梯度：从 $m=1$ 到 $m=tau$ 的累积],
    [$g_(r,t)^(M)$], [完整累积梯度：从 $m=1$ 到 $m=M$ 的累积],
    [$g_"pred",(r,t)$], [截断点处构造、送入 All-Reduce 的预测梯度（缩放后）],
    [$g_"sync",t$], [All-Reduce 的同步梯度（DP 组内均值）],
    [$e_(r,t)$], [误差缓冲（error buffer）],
    [$s_(r,t)$], [通信发送缓冲（send buffer）],
    [$h_t$], [异步通信句柄（handle）],
    [$w_t$], [迭代 $t$ 的模型参数],
    [$eta$], [学习率],
    [$tau$], [通信触发的截断 micro-batch 索引],
    table.hline(),
  ),
  caption: [POLAR-SGD 记号表]
) <tab:polar-notation>

在该设定下，域内 PP 负责把模型切分成多层以提升显存可承载的模型规模，并通过 1F1B 将多个 micro-batch 的前向/反向交错执行来提高单域内的流水线利用率；跨域 DP 则要求每步获得一致的更新方向，需要对 DP 组内的梯度做均值 All-Reduce。该 All-Reduce 因 WAN 特性成为尾部瓶颈。

在一个迭代 $t$ 内、DP 组内的 worker（rank）$r$ 上，micro-batch $m$ 的局部梯度记为 $g_(r,t,m)$。我们关心如何选择一个 micro-batch 截断点 $tau$，在 $m=tau$ 时启动一次非阻塞 All-Reduce，并利用误差反馈在后续迭代补齐未同步的梯度信息。

== 详细方案设计与实现

@fig:polar-overview 对比了标准 DP+PP 与 POLAR-SGD 的单次迭代时间线，可以看到POLAR-SGD 通过前缀触发的异步 All-Reduce 缩短同步尾部，每一行代表着在各个 DP 组内的各个 DP 进程中的对等 PP rank 的行为。其中，红色块代表一个微批次的前向传播，白色块代表一个微批次的反向传播，紫色块代表在反向传播结束后的梯度通信同步。标准做法往往在处理完全部 $M$ 个 micro-batch 的反向传播后，才在迭代尾部执行阻塞式 All-Reduce，形成明显同步尾部；POLAR-SGD 则在 micro-batch 前缀完成时（$m=tau$）触发一次非阻塞 All-Reduce，并放入独立通信 stream，使其与后续 $(M-tau)$ 个 micro-batch 的反向传播重叠，从而缩短迭代尾部。

#figure(
  image("../../image/ch5_polar_overview.png", width: 98%),
  caption: [DP+PP 下的执行时间线对比]
) <fig:polar-overview>

POLAR-SGD 通过梯度缩放 + 误差反馈将后缀信息以残差形式注入下一次迭代，以同时保持系统重叠收益与优化语义稳定性。POLAR-SGD 的设计可概括为系统效率、语义约束、优化稳定性三者并重：在系统层面尽可能将跨 域 All-Reduce 前移到更早位置，以获得更长的计算重叠窗口并缩短迭代尾部；在语义层面保持“每迭代一次 DP 同步”的宏观节奏，避免完全异步导致的严重的梯度滞后（staleness）；在优化层面通过可控残差通道补齐后缀梯度信息，使收敛轨迹尽量贴近基线同步训练。

== 前缀触发的异步梯度同步

本节进一步给出 POLAR-SGD 在单次训练迭代内的具体同步流程，说明如何选择触发点、构造前缀梯度并在独立通信流上发起非阻塞 All-Reduce。整体思路是在不改变每步一次全局同步语义的前提下，将原本位于迭代末尾的阻塞通信前移到仍有反向计算可执行的窗口中，从而为后续的梯度缩放与误差反馈机制提供执行基础。

=== 梯度 All-Reduce 的触发时机

POLAR-SGD 在每个 micro-batch 反向结束时累积梯度。当到达截断点 $m=tau$ 时，对当前前缀梯度做快照并构造发送缓冲 $s_(r,t)$，随后触发一次非阻塞 All-Reduce；在 $m=M$（mini-batch 结束）时等待通信完成，得到 $g_"sync",t$ 并执行优化器更新。

#figure(
  kind: "algorithm",
  // placement: top,
  supplement: [算法],

  pseudocode-list(booktabs: true, numbered-title: [POLAR-SGD 的前缀触发异步 All-Reduce（每迭代一次）], full: true)[
    - *全局状态：* 累积梯度 $g_a$；前缀快照 $g^(tau)$；通信句柄 $h_t$
    - *回调：* 在每个 micro-batch 反向结束时调用

    + $g_a <- g_a + g_(r,t,m)$ #h(1em) ▷ PP 的梯度累积
    + *if* $m = tau$ *then*
      + $g^(tau) <- g_a$ #h(1em) ▷ 记录前缀梯度快照
      + $s_(r,t) <- "BuildSendBufferAtCutoff"(t, g^(tau))$
      + $h_t <- "AsyncAllReduceMean"(s_(r,t))$
    + *end*
    + *if* $m = M$ *then*
      + *wait* $(h_t)$ #h(1em) ▷ 得到同步梯度 $g_"sync",t$
      + $"OptimizerStep"(g_"sync",t)$
      + $"UpdateError"(t, g_a)$ #h(1em) ▷ 用完整累积梯度更新误差缓冲
      + $g_a <- 0$; $h_t <- "NULL"$
    + *end*
  ],
) <algo:polar-communication>

=== 截断点 $tau$ 的选择规则

令 $T_"ar"$ 表示本次梯度 All-Reduce 的平均通信时延估计，$T_"comp"(tau)$ 表示在 $m=tau$ 触发通信后，迭代内剩余的计算时间（通常来自后续 micro-batch 的反向传播）。本文采用最早可完全掩盖的截断点：

$ tau^* := max {tau in {1, ..., M}: T_"comp"(tau) >= T_"ar"} $

实践中，可通过滑动窗口在线 profiling 获得 $T_"ar"$ 与 $T_"comp"(tau)$ 的估计：较小 $tau$ 带来更长的可重叠窗口，但也会加大对误差反馈的依赖；较大 $tau$ 则更接近基线的同步语义。

当对所有 $tau$ 都满足 $T_"comp"(tau) >= T_"ar"$ 时，可以退化为 $tau^* = M$，此时算法行为接近在所有微批次迭代末同步的基线。该退化机制使得 POLAR-SGD 能在网络条件较好或模型计算较短等情形下自动回到保守策略。

在工程实现上，$T_"ar"$ 可由最近若干步 All-Reduce 的完成时延统计得到；$T_"comp"(tau)$ 则可由 profiler 记录从 micro-batch $tau$ 反向完成到本迭代结束（$m=M$）的剩余反向时间估计。为了避免 $tau$ 在相邻迭代间剧烈抖动，通常会对估计值做滑动平均，并在更新 $tau$ 时加入简单的迟滞（例如只有当新 $tau$ 带来显著收益时才切换）。

== 预测误差修正

前缀触发意味着本次迭代的同步更新方向来自前缀梯度。为避免后缀 micro-batch 的梯度信息被忽略，POLAR-SGD 通过前缀放缩的机制构造全 batch 尺度的预测梯度，并维护误差缓冲记录预测与真实完整累积之间的偏差。

=== 前缀梯度与完整梯度

在 worker $r$ 的迭代 $t$ 中，定义：

$ g_(r,t)^(tau) := sum_(m=1)^tau g_(r,t,m),  quad g_(r,t)^(M) := sum_(m=1)^M g_(r,t,m) $

当 $m=tau$ 时，累积缓冲 $g_a$ 等于 $g_(r,t)^(tau)$；当 $m=M$ 时，$g_a$ 等于 $g_(r,t)^(M)$。

=== 发送缓冲与误差更新

在截断点处构造发送缓冲（同时作为预测梯度的未归约形式）：

$ s_(r,t) = (M/tau) dot (g_(r,t)^(tau) + e_(r,t)),  quad g_"pred",(r,t) := s_(r,t) $

对 $s_(r,t)$ 做 DP 组内均值 All-Reduce，得到同步梯度：

$ g_"sync",t = (1/N) sum_(r=1)^N g_"pred",(r,t) $

当迭代内全部 $M$ 个 micro-batch 完成后，用完整累积梯度 − 预测梯度更新误差缓冲：

$ e_(r,t+1) = g_(r,t)^(M) - g_"pred",(r,t) $

该残差包含后缀 $(M-tau)$ 个 micro-batch 的梯度信息以及前缀预测误差，并在下一迭代通过 $e_(r,t)$ 注入到发送缓冲中，从而以受控方式延迟使用后缀梯度的信息。

从直观上看，$(M/tau)$ 的缩放因子承担了把前缀的梯度尺度外推到全 batch 的作用：如果梯度的统计分布在 micro-batch 维度上相对平稳，那么 $g_(r,t)^(tau)$ 的期望规模与 $tau$ 成正比，乘以 $(M/tau)$ 后就与 $g_(r,t)^(M)$ 更接近。误差缓冲 $e_(r,t)$ 则用于获取外推梯度与后缀梯度信息的偏差，把这些信息以误差补偿的形式带到下一步同步中。

此外，对 DP 组内取平均，令 $bar g_t^(M) := (1/N) sum_(r=1)^N g_(r,t)^(M)$ 且 $bar e_t := (1/N) sum_(r=1)^N e_(r,t)$，则在误差更新定义下可得到

$ g_"sync",t + bar e_(t+1) = bar g_t^(M) $

该等式说明：同步更新方向与下一步的平均残差分解获取了本步完整累积梯度的信息；也就是说，后缀 micro-batch 的信息并未被忽略，只是被延迟到下一迭代中体现。

#figure(
  kind: "algorithm",
  placement: top,
  supplement: [算法],

  pseudocode-list(booktabs: true, numbered-title: [POLAR-SGD 的误差反馈（前缀缩放 + 误差更新）], full: true)[
    - *全局状态：* 误差缓冲 $e_(r,t)$；预测梯度 $g_"pred",(r,t)$；$tau$ 与 $M$

    + *procedure* $"BuildSendBufferAtCutoff"(t, g_(r,t)^(tau))$
      + $s_(r,t) <- (M/tau) dot (g_(r,t)^(tau) + e_(r,t))$
      + $g_"pred",(r,t) <- s_(r,t)$
      + *return* $s_(r,t)$
    + \
    + *procedure* $"UpdateError"(t, g_(r,t)^(M))$
      + $e_(r,t+1) <- g_(r,t)^(M) - g_"pred",(r,t)$
  ],
) <algo:polar-error-feedback>

== 工程实现与系统集成

虽然本章重点在算法机制，但在真实的 DP+PP 系统中获得稳定收益，还需要在控制流与执行资源（CUDA stream）层面保证异步通信确实与计算并行。这里给出更贴近工程实现的集成要点。

=== 与 1F1B 流水线的集成位置

在 1F1B 调度中，micro-batch 的反向传播会按固定节奏在各 stage 上完成。POLAR-SGD 的关键是：在每个 DP rank 上，在某个 micro-batch 的反向结束时刻统一触发一次 All-Reduce。实现时通常会在梯度累积完成处设置钩子函数（hook）（或在训练循环里显式判断 $m=tau$），以便在该时刻将 $s_(r,t)$ 交给通信后端。由于各 stage 的反向完成存在相位差，实际系统中更常见的做法是：在每个 DP rank 内先对所有参数做一次完整梯度张量的累积（或以 bucket 形式累积），然后在 $m=tau$ 时对该累积缓冲做快照并外推梯度，而后进入 All-Reduce 通信环节。这样能避免在每层/每 bucket 上重复触发通信，符合每个训练迭代执行一次 All-reduce 梯度同步的设计初衷。

=== 通信流与计算流的隔离

要实现重叠，All-Reduce 必须在独立的通信流上启动，且计算流不应等待其完成。

一个常见实现骨架是：计算流在 micro-batch $m=tau$ 反向结束后先记录 event；通信流等待该 event 以确保 $s_(r,t)$ 写入完成后，再启动异步 All-Reduce（如 NCCL async_op）；随后计算流继续处理后续 micro-batch 的反向传播和梯度累积，直到 $m=M$ 的更新点才显式等待通信句柄并获取 $g_"sync",t$。

=== 误差缓冲的存储与开销

$e_(r,t)$ 与模型梯度同形，最直接的实现是在每个 DP rank 上维护一个与梯度张量同大小的 buffer。其更新发生在 $m=M$ 之后：此时已得到完整累积梯度 $g_(r,t)^(M)$，且预测梯度 $g_"pred",(r,t)$ 在 $m=tau$ 时已记录。更新 $e_(r,t+1)$ 是一次向量差，额外开销相对一次完整反向传播通常较小，但会带来一定显存占用；因此工程上常结合张量扁平化/分桶来减少 buffer 管理复杂度。

== 理论分析

在标准随机优化假设下（$L$-光滑、随机梯度无偏、方差有界），本文给出 POLAR-SGD 的收敛性分析：其渐近收敛行为与同步 SGD 一致，且仅额外引入一个由误差缓冲控制的项。

设 $bar e_t := (1/N) sum_(r=1)^N e_(r,t)$，当学习率满足 $eta <= 1/(2L)$ 时，经过 $T$ 次全局迭代，有：

$ (1/T) sum_(t=0)^(T-1) bb(E)[||nabla f(w_t)||^2] <= (2 (f(w_0)-f^*))/(eta T) + (L eta sigma^2)/N + (L eta)/T sum_(t=0)^(T-1) bb(E)[||bar e_t||^2] $

该上界表明：当按 $tau$ 选择规则使得通信尽量被掩盖、并使误差缓冲保持有界时，POLAR-SGD 的优化稳定性可以与基线同步训练保持接近。

从解释角度看，该上界包含三个互补部分：一是随迭代次数增长而下降的 $O(1/T)$ 优化项，二是与标准 DP 均值 All-Reduce 一致的 $sigma^2/N$ 方差缩减项，反映“DP rank 越多，随机梯度方差越小”的统计规律，三是与 $||bar e_t||^2$ 相关的误差项，用于刻画前缀触发近似偏差在 error feedback 机制下的受控程度。

因此，$tau$ 的选择在理论与实践中都扮演折中角色：更早触发（更小 $tau$）带来更强的重叠潜力，但也可能使 $||bar e_t||$ 更大；更晚触发则降低误差项，但可能无法完全掩盖通信。

== 实验与验证

=== 实验设置

本文在跨域训练约束下评估 POLAR-SGD，并与标准 DP+PP 基线（DDP+1F1B，迭代末阻塞 All-Reduce）以及放松同步的 DiLoCo（$k=4$）对比。实验配置如下。

从云边端协同角度看，该实验设置对应跨域 DP（跨云-边）+ 域内 PP（边缘域内流水线）的层次化训练骨架：WAN 的带宽与 RTT 约束刻画边缘↔云（或边缘↔边缘）链路特性，而域内互联与多卡节点刻画边缘域内部的高带宽计算集群。因而，本章实验可用于验证在云边端协同场景中，如何通过更早触发、可重叠的异步 All-Reduce 来降低跨域同步尾部。

// Data source: image/csv/ch5_tab_polar_setup.csv
#figure(
  table(
    columns: (auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*组件*], [*配置*]),
    table.hline(stroke: 0.5pt),
    [Model], [LLaMA-2 7B],
    [Dataset], [WikiText-103],
    [Hardware], [32× NVIDIA A100 (40GB)，4 nodes],
    [Network], [Bandwidth: 30 Gb/s；RTT: 50 ms],
    [Parallelism], [Hybrid DP+PP（PP=8，DP=4）；batch=256；microbatch=32],
    [Software], [PyTorch 2.5；NCCL 2.23；CUDA 12.1],
    [Metrics], [吞吐（tokens/s），训练 loss],
    table.hline(),
  ),
  caption: [实验设置]
) <tab:polar-setup>

=== 端到端吞吐

// Data source: image/csv/ch5_tab_polar_throughput.csv
#figure(
  table(
    columns: (auto, auto, auto),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*方法*], [*吞吐 (tokens/s)*], [*相对加速比*]),
    table.hline(stroke: 0.5pt),
    [DDP+1F1B], [35,350], [1.00×],
    [DiLoCo ($k=4$)], [62,216], [1.76×],
    [POLAR-SGD], [66,104], [1.87×],
    table.hline(),
  ),
  caption: [跨域训练约束下端到端吞吐]
) <tab:polar-throughput>

从吞吐对比可以看出：在跨域训练约束下，基线 DDP+1F1B 的尾部同步显著拉长了单步时间；DiLoCo 通过减少同步强度/频率获得了较大吞吐提升，但其优化语义偏离基线更明显；POLAR-SGD 在保持每步一次同步的节奏下仍获得最高吞吐，约为基线的 1.87 倍。

该结果的关键意义在于区分了两条不同优化路径：DiLoCo 主要通过降低同步强度来换取系统速度，而 POLAR-SGD 主要通过重排同步时机来提升重叠效率。前者本质上改变了同步语义，后者在每步一次同步的训练语义下优化系统训练速度。因此，当目标是兼顾吞吐与语义一致性时，POLAR-SGD 的方法路线更符合跨域 DP+PP 的实际需求。

从关键路径角度看，吞吐提升与通信总量变化有关，也由通信被计算掩盖的程度有关。即使总通信字节不变，只要将 All-Reduce 前移到可重叠窗口，迭代末端的纯等待区间就会明显缩短。表格中的相对加速比正体现了这一点：优化对象是迭代尾部而非全程任意位置的通信片段，因此收益具有明确因果指向。

此外，POLAR-SGD 与 DiLoCo 的对比还支持本文关于系统优化与优化语义协同的结论。DiLoCo 在系统侧收益较高，但其训练语义偏离基线更明显；POLAR-SGD 在保持步级同步节奏的前提下达到更高吞吐，说明通过异步触发与误差修正可以在不通过降低训练同步频率的语义约束下实现系统增益。这一结果为后续系统集成章节提供了直接证据。

=== 收敛曲线与消融

#figure(
  image("../../supplementary/images/json_curves_by_steps.png", width: 75%),
  caption: [训练 loss 随 step 变化：POLAR-SGD 与基线高度一致，优于 DiLoCo]
) <fig:polar-loss-steps>

本文进一步给出了按迭代次数对齐的 loss 曲线（验证训练稳定性）、按墙钟时间对齐的 loss 曲线（验证目标 loss 到达时间），以及对“梯度缩放 / 误差反馈”两项关键机制的消融结果。该组实验的目的并不只是观察最终 loss 是否接近，而是从优化轨迹、系统时间收益和机制必要性三个角度共同验证 POLAR-SGD 的有效性。

若按迭代次数对齐的曲线产生漂移，则说明提前同步改变了优化语义；若墙钟时间曲线没有优势，则说明系统加速未能转化为训练效率；若消融后曲线明显退化，则可进一步定位收益依赖的关键机制。

如@fig:polar-loss-steps 所示，按迭代次数对齐的结果回答了算法是否改变每一步优化行为这一问题。POLAR-SGD 的 loss 曲线与 DDP+1F1B 基线基本重合，在训练前期快速下降阶段和后期趋于平缓阶段均未出现持续偏离，说明前缀触发并没有引入系统性的更新方向偏差。换言之，虽然 POLAR-SGD 在每个 step 内提前使用前缀 micro-batch 构造同步梯度，但经过梯度缩放和误差反馈后，其长期优化轨迹仍能贴近完整梯度同步路径。

该结果也说明 POLAR-SGD 与 DiLoCo 类降低同步频率的方法在优化语义上存在差异。DiLoCo 通过减少全局同步频率获得通信收益，其局部模型会在多个 step 内独立演化，因此 step 对齐曲线更容易表现出与同步基线的间隔；POLAR-SGD 则仍保持每步一次全局同步，只是在 step 内重排同步触发时间，因此更容易维持与基线一致的收敛轨迹。这一点对跨域训练尤其重要，因为系统吞吐提升若伴随明显 loss 漂移，后续还需要额外调参或延长训练来弥补，工程收益会被削弱。

#figure(
  image("../../supplementary/images/json_curves_by_time.png", width: 75%),
  caption: [训练 loss 随墙钟时间变化：POLAR-SGD 更快进入低 loss 区间]
) <fig:polar-loss-time>

如@fig:polar-loss-time 所示，按墙钟时间对齐则更直接反映系统收益。由于 step 对齐曲线已经表明不同方法的优化轨迹接近，墙钟时间曲线中的差异主要来自单步迭代时间的缩短，而不是来自更激进或更不稳定的优化过程。POLAR-SGD 曲线更早进入低 loss 区间，说明前文表格中的吞吐提升能够实际转化为 time-to-target 收益，即在相同训练时间预算内完成更多有效 step，或在达到相同目标 loss 时消耗更少实际时间。

从系统角度看，这一结果补充了端到端吞吐表的不足。吞吐指标只说明单位时间内可处理的 token 数增加，但无法单独证明模型质量会随时间同步改善；墙钟时间 loss 曲线则说明 POLAR-SGD 的收益能够落到训练任务最终关心的目标 loss 到达时间上。因此，step 对齐与时间对齐两类曲线构成了互补证据：前者验证语义稳定，后者验证效率收益。

如@fig:polar-ablation 所示，消融结果强调了 *梯度缩放（Gradient Scaling）与误差反馈（Error Feedback）* 两个机制的必要性。一方面，若不执行 $(M/tau)$ 缩放，同步梯度只反映前缀 micro-batch 的平均贡献，其尺度相对完整 mini-batch 梯度会系统性偏小，导致参数更新幅度不足，表现为 loss 下降速度变慢。该现象说明，前缀触发不是简单地“提前发送部分梯度”即可成立，必须通过尺度校正使同步梯度在期望意义上对齐完整梯度。

#figure(
  image("../../supplementary/images/json_curves_by_steps_ablation.png", width: 75%),
  caption: [消融实验：去除梯度缩放或误差反馈会带来轻微收敛退化或稳定性风险]
) <fig:polar-ablation>

另一方面，若不维护误差反馈，后缀 micro-batch 中未被本轮同步完整表达的信息会在当前 step 后直接丢失，造成长期累积偏差。误差反馈的作用在于把前缀近似与完整梯度之间的差异显式保留下来，并在后续迭代中逐步补偿，使每一步的近似误差不会单向累积。消融曲线中去除误差反馈后的退化表明，该机制主要负责控制长期稳定性；而去除梯度缩放后的退化则更多体现为短期更新尺度不足。二者分别对应“单步梯度尺度正确性”和“跨步误差守恒性”，共同保证 POLAR-SGD 在提前通信的同时仍维持接近同步训练的优化行为。

综合三组曲线可以得到更完整的结论：POLAR-SGD 的性能收益来自通信触发时机前移和计算通信重叠，但其可用性依赖于梯度缩放与误差反馈对优化语义的约束。也就是说，本方法并非通过放松同步语义或牺牲收敛质量换取吞吐，而是在保持每步同步节奏的前提下，将原本暴露在迭代尾部的跨域通信转移到可重叠窗口中，并通过误差修正机制限制由前缀近似带来的训练偏差。

=== 网络敏感性分析

在不同网络条件下评估 POLAR-SGD 的性能，可以分析其对 WAN 特性的适应性。理论上，随着 RTT 增加或带宽降低，POLAR-SGD 的相对优势应该更明显，因为其核心设计目标就是掩盖 WAN 同步的尾部；但随着网络进一步劣化，POLAR-SGD 的优势可能会减弱，因为当通信延迟远大于每次迭代中的反向传播计算时间时，即使将通信前移至 $tau = 1$ 的位置，仍然会出现无法掩盖的通信开销，此时仅凭 POLAR-SGD 难以完全解决通信瓶颈；相反，如果网络条件较好，POLAR-SGD 可能会退化为与基线类似的行为。

#figure(
  image("../../image/polar_network_sensitivity.png", width: 75%),
  caption: [POLAR-SGD 在不同网络条件下的性能表现]
) <fig:polar-network-sensitivity>

如@fig:polar-network-sensitivity 所示，在网络条件较好的情况下，POLAR-SGD 的性能与基线相近；随着网络条件恶化，POLAR-SGD 相对基线的性能提升更明显。但总体来看，在约 20 Gb/s 的环境中，POLAR-SGD 的绝对性能已开始减弱，并在进一步的带宽恶化下，相对基线的性能提升也逐渐降低。这一结果验证了 POLAR-SGD 在 WAN 同步受限场景下的设计初衷，同时也揭示了其适用范围和潜在局限。在极端恶劣的网络条件下，仅靠 POLAR-SGD 可能无法完全解决通信瓶颈问题，此时需要结合前文所述的压缩与调度优化策略进一步提升性能。

=== 截断点敏感性分析

在不同的截断点取值下，POLAR-SGD 的性能表现可能也会产生差异。本文通过实验验证了在不同的截断点取值下，POLAR-SGD 的性能表现是否一致。理论上，较小的截断点（如 $tau=1$）会带来更长的重叠窗口，但也可能使得误差缓冲中的残差更大，从而增加优化漂移的风险；较大的截断点则更接近基线的同步语义，但可能无法完全掩盖通信。本文据此设计实验，测试 POLAR-SGD 方法是否在不同的截断点取值下具有鲁棒性。

如@fig:polar-tau-sensitivity 所示，整体上来说，在流水线并行阶段数为8，微批次数量为32的实验设置下，POLAR-SGD 在不同截断点（$tau in [1, 31]$）取值下的性能表现基本一致，并未有显著差别能够证明不同截断点取值对性能产生影响。

#figure(
  image("../../image/ch5_polar_tau_sensitivity.svg", width: 75%),
  caption: [POLAR-SGD 在不同截断点取值下的性能表现]
) <fig:polar-tau-sensitivity>

这一结果验证了 POLAR-SGD 的设计初衷，即通过前缀触发的异步 All-Reduce 来掩盖 WAN 同步的尾部，同时通过误差反馈机制来补偿前缀触发带来的偏差，从而在保持每步一次同步的训练语义下实现系统性能提升。该结果还表明，在实际应用中，POLAR-SGD 对于截断点的选择具有一定的鲁棒性，不需要过于精细地调整截断点以获得性能提升。此外，我们还观察到当截断点 $tau$ 取值大于10时，吞吐开始下降，并在$tau$取值为31时，吞吐下降到与基线相近的水平。这一现象验证了前面关于截断点选择的理论分析：较大的截断点虽然更接近基线的同步语义，但可能无法完全掩盖通信，从而导致性能下降。因此，在实际应用中，选择一个适当的截断点可以在获得最大重叠收益的同时，保持优化稳定性。

=== 实验结论

收敛曲线分析显示，POLAR-SGD 与基线在 step 对齐下保持高度接近，说明前缀触发和误差修正并未破坏每步优化方向。该现象是对方法有效性的核心验证：如果系统加速是以明显训练漂移为代价，则其工程价值会大幅下降；当前曲线结果表明该风险被有效控制。

墙钟对齐曲线进一步解释了吞吐提升如何转化为训练效率提升。由于收敛轨迹接近，单步时间缩短会直接体现为更快达到同等 loss 区间。

消融实验则从反证角度补强结论。去除缩放后出现尺度偏差，去除误差反馈后后缀信息回流不足，两者都导致收敛性能劣化，说明 POLAR-SGD 的有效性来自梯度缩放和误差控制两部分的协同作用。

综合吞吐、收敛与消融三组证据，方法在系统侧缩短尾部等待，在优化侧保持轨迹稳定，在机制层具备可解释的必要性。因此，本章关于 POLAR-SGD 在跨域 DP+PP 场景中的适用性结论具备充分实验支撑。此外，本章还进行了POLAR-SGD的网络敏感性分析，验证了其在不同网络条件下的性能表现，证明了方法存在局部有效性，需要结合具体模型与网络条件来评估其适用性。并且说明了其在极端恶劣的网络条件下可能无法完全解决通信瓶颈问题，此时需要结合本文中之前所述的优化策略来进一步提升性能。

== 本章小结

本章针对跨域训练条件下 DP+PP 训练的同步尾部问题，提出并验证了 POLAR-SGD 的通信掩盖方案。WAN 高 RTT 使得迭代末 All-Reduce 成为关键路径瓶颈，常规分桶重叠难以覆盖尾部阻塞。于是，本章采用“跨域DP、域内 PP”的层次化并行骨架，将跨域通信限定为梯度同步；在每迭代只触发一次 All-Reduce 的约束下，引入截断点 $tau$ 与独立通信流提前触发异步同步，并通过前缀缩放与误差反馈机制补齐后缀梯度信息。最终，在 30 Gb/s、50 ms RTT 的跨域训练约束下，方法获得约 1.87× 的吞吐提升，并保持与基线接近的收敛轨迹；消融结果进一步证明梯度缩放与误差反馈均为稳定性与效果的关键组成。综上，本章在不改变每步一次梯度同步的训练语义的前提下，通过同步时机重排与误差修正有效削减尾部阻塞。

#pagebreak()
