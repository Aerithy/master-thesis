= 绪论

== 研究背景

=== 大语言模型的规模演进与分布式训练需求

以 Transformer 架构 @vaswaniAttentionAllYou2023 为核心的预训练范式推动了大规模语言模型与基础模型（Foundation Model）的快速演进 @bommasani2021foundation。从 BERT（3.4 亿参数）的双向预训练 @devlin2019bert，到 GPT-3（1750 亿参数）的少样本学习 @brown2020gpt3，再到 PaLM（5400 亿参数）的 Pathways 架构 @chowdhery2022palm、LLaMA 系列（7B 至 70B 参数，开源高效训练）@touvron2023llama，以及近期 DeepSeek-R1（6710 亿参数混合专家架构）的推理增强模型 @deepseekai2025deepseekr1，语言模型的参数规模在短短数年间完成了数量级的跨越。Kaplan 等人通过实证揭示了模型性能与训练计算量、参数量之间的幂律扩展关系（Scaling Law） @kaplan2020scaling，进一步奠定了"大规模训练范式"的理论基础；Hoffmann 等人则在此基础上给出了计算最优的参数量与训练数据量匹配准则（Chinchilla 定律） @hoffmann2022chinchilla，两项工作共同引导了产业界在算力投入不断增大背景下的大模型训练策略。基础模型的概念 @bommasani2021foundation 进一步将这一范式推广至视觉、多模态、科学计算等领域，极大地拓宽了大规模分布式训练的应用场景。

随着参数规模持续攀升，单 GPU 乃至单节点训练已无力承载百亿至千亿参数模型。以 GPT-3（175B）为例，BF16 精度下仅存储模型参数即需约 350 GB，结合 Adam 优化器的梯度与状态，显存需求可超过 2 TB，远超单张 GPU 的 80 GB 容量上限 @brown2020gpt3。大规模分布式并行训练因此成为必要的基础设施。学术界与工业界共同构建了多维并行体系：数据并行（Data Parallelism, DP）通过批次切分实现规模扩展并以 All-Reduce 同步梯度；张量并行（Tensor Parallelism, TP）在算子粒度切分权重矩阵以降低单设备显存压力 @shoeybi2019megatron；流水线并行（Pipeline Parallelism, PP）将模型层划分为多个 stage 并以 micro-batch 串行注入实现层间并行 @gpipe @pipedream；专家并行（Expert Parallelism, EP）通过条件计算动态路由实现超大规模模型扩展 @lepikhin2020gshard；ZeRO 技术通过对优化器状态、梯度与参数进行分层分片，显著降低每节点显存占用 @zero2020。Narayanan 等人在千亿参数模型上展示了 DP+TP+PP 组合并行（3D Parallelism）的规模化工程实践 @narayanan2021efficient，PyTorch 生态进一步提供了对分片并行的工程化支持 @pytorch_distributed2020 @fsdp2023，形成了覆盖"计算、通信、内存"三个维度的完整分布式训练工程体系。

=== 云边端协同训练的兴起

训练数据与算力资源正从"集中式单数据中心"走向"云-边-端协同"的跨域形态，驱动力来自多个层面。其一，数据隐私与合规约束（如 GDPR、HIPAA、金融数据本地化规定）要求敏感数据就地留存于边缘站点或终端设备，无法集中回传至中心云；其二，业务地理上的分布要求在多个数据中心并行维护训练与推理能力；其三，5G 边缘节点、企业私有云与端侧加速器的快速部署在全球范围形成了分散而异构的算力资源池；其四，出于弹性成本与算力利用率的考量，企业往往希望在训练高峰期借助边缘节点动态扩展算力，在低峰期释放以节省成本。

在这一背景下，云-边-端协同训练涉及三类参与主体：*云侧*（Cloud）具有强算力（大规模 GPU 集群）与大容量存储，承担主要的计算任务与全局模型状态维护；*边缘侧*（Edge）通常为微型数据中心或边缘服务器，可提供中等算力并具备就近处理本地数据的能力；*端侧*（Device）如手机、车载计算单元，算力有限但数据产生丰富，参与局部计算或梯度上传。三者构成"端→边→云"的逐级汇聚拓扑，在通信特性上呈现显著的层次异构性。

=== 跨域通信的层次异构性

在云边端协同的物理环境中，由于各类算力中心和终端设备的地理位置分散，系统底层的网络连接在物理形态和性能指标上呈现出极端的“层次异构”特征。具体而言，整个通信架构通常可划分为三个明显的层级：第一层为“节点内互联”，例如同一台多卡服务器内部通过 PCIe 或 NVLink 互联，其单向通信带宽可高达数百 GB/s（如 NVLink 往往具有 600 GB/s 的极高吞吐量），通信延迟在微秒级别 @nvidia_nvlink_v3_2021；第二层为“域内局域网（LAN）互联”，同一物理机房或边缘计算节点群内部通过高性能以太网或 InfiniBand 交换机连接，通常可提供数十至数百 Gb/s（如 200 Gb/s）的高吞吐量和极低的抖动 @mellanox_hdr200；第三层为“跨域广域网（WAN）互联”，即不同地理位置的边缘节点之间，或边缘与中心云之间的公共网络链路，其可用带宽往往下降至 1 Gb/s 甚至几百 Mb/s 级别，且网络往返时延（RTT）常高达数十至数百毫秒，并伴随着由复杂路由和流量拥塞引起的抖动 @jain2013b4。

这种高达数倍的“域内快—域间慢”非对称带宽差异与复杂的长尾延迟特征，与当前主流的大规模分布式训练框架的设计假设产生了严重背离。现有的分布式训练框架（如基于 PyTorch DDP 或 Megatron-LM 的数据与模型并行方案）和底层通信库（如 NCCL），在设计之初全部默认了整个参与计算的节点处于一个同构、带宽高且延迟极低的超算集群中。它们广泛采用的 Ring All-Reduce 或 Tree All-Reduce 等扁平化集合通信原语，将系统中所有参与者跨层级地视为对等的通信者。一旦将此类框架强行部署在云边端跨域场景下，系统整体的通信效率将严格受制于跨域低速链路。这会使得原本高速的域内互联因为必须原地等待长距离网络的慢速传输完成，而导致节点自身的计算资源与高带宽资源被严重闲置，进而成为制约云边端分布式训练算力扩展的核心瓶颈。

跨域链路绝对带宽的极度匮乏，要求系统必须首先从数据源头削减通信的传输体积；各层级间传输速率的非对称性，要求我们打破传统的扁平化同步，将通信过程针对网络层级进行解耦并引入独立流水线，以充分隐藏层级间的通信时差；而广域网不可忽略的长时延特征，则进一步要求在保证分布式全局同步正确性的语境下，尽最大可能发掘计算与通信并行的时间窗口来掩盖网络阻塞。因此，正是这一层次异构性所带来的系列挑战，全面驱动了本文从通信量、集合通信调度以及计算-通信重叠等三个维度对分布式通信系统进行的重构与优化。

== 云边端协同分布式训练的通信挑战

在同步数据并行训练框架中，每次迭代末尾需通过 All-Reduce（或其等价变体）聚合全局梯度均值。当训练跨域展开时，同步通信面临以下四个系统性挑战：

*挑战一：跨域带宽受限与梯度规模的耦合放大。* 大模型梯度同步的通信量与参数规模线性正相关：以 BF16 精度计，175B 参数模型每步梯度约需 350 GB 的单向传输量。在采用层次化 All-Reduce 时，跨域部分至少需传输一次完整的归约梯度（Reduce-Scatter 阶段后）与广播结果（All-Gather 阶段），总量仍达数百 GB 量级。若可用跨域带宽仅有 10–30 Gb/s，即使通过梯度量化将通信量压缩至原始的 1/4 至 1/32，跨域传输时间仍可能占据总迭代时间的相当比例，出现"算力翻倍但吞吐停滞"乃至"加节点反而更慢"的现象。

*挑战二：高 RTT 与链路抖动加剧同步尾部延迟。* WAN 链路的高 RTT（跨大洲可达 50–200 ms）与抖动（10–50 ms 量级）会将集合通信的启动开销与同步等待放大至不可忽略的程度 @jain2013b4。在同步 DP 框架中，每步训练须等待最慢节点完成通信（"木桶效应"），而链路抖动带来的随机慢节点（straggler，指由于各种系统或网络干扰而执行明显慢于其他节点的成员）效应会使吞吐分布出现长尾，系统整体有效吞吐受最慢单次通信主导。在 DP+PP 混合并行场景中，流水线末尾阶段的 All-Reduce 本已处于计算关键路径之外，高 RTT 进一步扩大尾部阻塞窗口，加重设备空闲 @crosspipe2025 @geopipe2025。

*挑战三：分层拓扑与扁平化集合通信不匹配。* 传统扁平化 Ring/Tree All-Reduce 将各节点视为对等参与者，无法感知"域内高速—域间低速"的拓扑差异，常导致域内高速链路的聚合潜力被域间低速瓶颈拖累。层次化 All-Reduce（域内 Reduce-Scatter → 跨域 All-Reduce → 域内 All-Gather）能够将跨域通信量从正比于全局节点数降为仅传输一份归约结果，显著降低跨域流量；但如何进一步对层次化各阶段进行流水化与并发调度，以充分利用域内高速带宽并最大化跨域传输的重叠度，仍是调度层面的核心问题。

*挑战四：混合并行下计算-通信重叠窗口受限。* 在数据并行框架中，常见的优化手段是将梯度按层分组为若干 bucket（数据桶），在反向传播过程中逐批启动 All-Reduce，使通信与后续层的反向计算形成时间重叠。然而在跨域高 RTT 环境下，最后若干 bucket 对应的反向层计算已完成，而其 All-Reduce 尚未结束，形成不可压缩的同步阻塞间隙（synchronization gap）。在 DP+PP 混合并行的流水线排空（flush）阶段，此问题尤为突出：PP 进入排空阶段后已无新 micro-batch 可投入计算，跨域 All-Reduce 仍在进行，所有设备不得不空等，直接拉低整体硬件利用率。

上述四个挑战共同表明：面向云边端协同的跨域分布式训练，单纯依赖单一层面的改进已无法消除系统瓶颈。必须从通信量、集合通信算法以及并行策略优化三个维度构建协同优化的理论与工程闭环：首先，压缩通信产生的绝对数据体积，减少物理传输的载荷（应对挑战一）；其次，重排分层集合通信的调度顺序，以更大限度发掘异构带宽潜力并降低启动延迟（应对挑战二、三）；最后，在系统关键路径维度上改变同步的触发时机，将不可避免的残余通信掩盖在计算窗口之下（应对挑战二、四）。沿着通信量、集合通信调度以及计算-通信重叠这三个层面展开协同优化，在不牺牲训练语义的前提下有效提升端到端训练效率。

== 研究目标与内容

在此研究背景和问题挑战驱动下，本文围绕云边端协同的约束特征构筑解决方案。为便于刻画跨域网络特征并开展可控实验，本文以跨数据中心训练作为典型强约束实例。跨 DC 场景的 WAN 带宽与 RTT 特征与边缘↔云或边缘↔边缘的典型互联相吻合，且可在实验中通过流量整形工具（tc-netem）精确复现 @hemminger2005netem。在此基础上，本文的研究目标与具体内容如下。

=== 研究目标

针对云边端协同的跨域分布式训练场景中存在的跨域带宽受限、高时延（RTT）、网络强抖动导致的梯度同步低效和训练吞吐不足等问题，本文拟研究跨域分布式训练中的梯度通信量压缩机制、集合通信分层调度优化以及计算-通信重叠策略等关键技术。最终面向跨域异构拓扑与资源受限环境，在云边端分布式训练系统中实现高效的大规模梯度同步与模型迭代。

=== 研究内容

论文拟研究面向云边端协同的跨域分布式训练通信优化技术，针对跨域链路上存在的传输开销大、通信链路利用率低、混合并行下同步尾部阻塞长等问题展开研究，优化跨域梯度同步通信通路，提升端到端训练效率。具体的研究内容包括：

（1） 面向跨域带宽瓶颈的低比特量化通信机制

跨域网络带宽受限，大规模模型梯度传输的绝对数据量庞大导致通信延时极长。在跨域带宽极具受限时，庞大体积的数据传输不仅严重拖慢单步训练速度，还极易引发网络拥堵与重传。因此，在跨域分布式训练系统中实现高效的梯度通信量压缩机制以降低网络传输负荷具有重要意义。

为了降低分布式训练系统的通信开销，研究人员主要针对稀疏化和量化两方面开展研究。在稀疏化方面，以 Top-K 为代表的选择策略，通过仅提取并传输绝对值最大的参数或梯度，极大地下降了通信数据量，但往往引入了额外的筛选与排序负担且难以保证精确恢复；在量化方面，以 INT8 或混合精度为代表的低秩通信机制，通过降低数据表达精度极大缓解了带宽压力，但低比特量化容易引入较大数值误差而使得模型最终的收敛轨迹受损。

针对上述问题与现状，本文在保证模型收敛的前提下，引入基于随机舍入机制的低比特策略，设计与之配套的张量位打包与误差补偿策略，最大化压缩跨域链路上的梯度有效传输字节数。最终，构建一套面向跨域带宽瓶颈的低比特量化通信机制。

（2） 基于张量分块与双流并行的层次化流水线 All-Reduce 调度机制

跨域与域内网络带宽性能差异巨大，传统扁平同步原语在非对称拓扑中执行低效导致链路资源大量闲置。域内互联高速而跨域互联慢速，现有的扁平集合通信策略让所有节点对等参与全连接通信，导致域内高速链路处于饥饿状态，被域间低速瓶颈拖累。因此，在跨域分布式系统中实现与物理拓扑相适配的分层集合通信机制以提升整体带宽利用率是十分必要的。

目前，众多研究人员已经针对复杂异构网络拓扑的集合通信过程展开了广泛的研究，探讨了多种调度机制。其中，以层次化 Ring 算法为代表的分层调度方法，通过解耦域内与域间的归约流程将跨域通信数据量降至理论最低，但未能解决各通信阶段之间相互串行等待的同步阻塞；以多流通信重叠为目的的调度方法，在宏观上调度了独立通信流并实现了并发执行，但未充分考虑到张量级别的细粒度流水线以及同步原语带来的异步瓶颈。

针对上述问题与现状，本文依据跨域通信系统内的极度非对称带宽特性，引入张量分块机制将层次化 All-Reduce 的跨域流程进一步微细化拆分，同时结合双通信流协调跨阶段流程的并发与接力执行，实现跨域同步过程中的调度高重叠率。最终，构建一套基于张量分块与双流并行的层次化流水线 All-Reduce 调度机制。

（3） 面向高 RTT 混合并行场景的前缀触发异步与计算-通信重叠策略

广域网络高时延特征与网络抖动严重，混合并行任务内的流水线排空期导致长尾通信延迟极易使计算设备长时间闲置。同步迭代更新是保证模型无损收敛的核心准则，但在跨域高 RTT 场景下，原生训练后端要求等待所有后向传播计算结束后才触发梯度同步的机制，直接暴露出了不可掩盖的超长空窗期。因此，在跨域分布式系统里引入更为积极主动的计算-通信并行重叠机制以对冲广域高时延负面影响并提升吞吐率至关重要。

为隐藏并重叠分布式系统中的通信延迟开销，研究人员提出了诸多时域重叠方法。其中，以张量桶（Bucket）分块调度为典型的重叠方案，利用后向梯度逐层计算产生的顺序差，实现计算过程对通信过程的掩盖。但这类优化大多局限于反向计算范围之内，面对跨域极其极端的广域 RTT 网络仍会剩下大量未能遮蔽的间隙时间；以完全异步参数服务器为代表的非同步更新策略，通过完全打破全局屏障消除了集群等待现象，却常常导致梯度更新的陈旧效应与收敛轨迹明显发散。

针对上述问题，本文结合数据与流水线混合并行的模型训练特征，将集合通信任务的触发时机从反向传播最后期抽离并提前移至早期阶段，使得跨域低速通信可以完全与模型的反向计算时段在时域上并发。本文并配合对陈旧参数更新的滞后进行预测和误差修正，平衡异步行为的影响。最终，构建一套面向高 RTT 混合并行场景的前缀触发异步与计算-通信重叠优化机制。

== 本文主要工作与创新点

本文面向以云边端协同的广域分布式训练系统，以通过通信优化方式提升面向云边端场景下的分布式训练任务效率为目标，通过充分调研、分析和总结国内外相关工作，针对跨域链路传输模型梯度数据的开销高、在跨域链路上执行集合通信的带宽利用率低、训练各轮所需同步尾部长等问题，从"通信量—调度—重叠"三个层次对跨域分布式训练通信优化进行系统性研究，分别提出，面向跨域带宽瓶颈的低比特量化通信机制，以降低传输模型梯度数据开销；张量分块与双流并行驱动的层次化流水线 All-Reduce 机制，以提升跨域同步时链路间通信的并行度与整体通信效率；前缀触发异步 All-Reduce 与预测误差修正机制，以减少迭代末端的同步阻塞。具体工作与创新点如下：

*工作一（第 3 章）：面向跨域带宽瓶颈的低比特量化通信机制。* 针对跨域链路传输大模型梯度的高开销问题，本文提出基于 k-bit 随机舍入量化的分布式优化器，支持可配置位宽 $b in {1, 2, 4, 8, 16}$。在此基础上，设计配套的位打包（bit-packing）、量化梯度聚合与误差补偿（error compensation）机制，实现对量化张量的高效通信支持。在受限链路上，16-bit 至 1-bit 的量化最高可将通信数据量降至原始的 1/32，同时通过误差反馈维持收敛稳定性，在压缩率与优化质量之间提供可调折中。

与现有工作相比，本方法通过系统的量化机制设计与工程实现，将低比特量化通信机制成功应用于跨域分布式训练场景，并通过大规模实验验证了其在不同带宽约束下的性能提升与收敛表现，为跨域通信优化提供了实用且高效的解决方案。

*工作二（第 4 章）：张量分块与双流并行驱动的层次化流水线 All-Reduce。* 针对"域内快、域间慢"的分层互联特征，本文提出将层次化 All-Reduce 的"域内 Reduce-Scatter—域间 All-Reduce—域内 All-Gather"三个阶段分解为可流水化执行的分块调度策略，并通过双 CUDA 流并行驱动域内与域间通信的并发执行，充分利用域内高速带宽对跨域同步延迟的掩盖窗口，显著提升跨域同步的重叠度与整体通信效率。

与现有工作相比，本方法首先提出创新的分块调度与双流并行机制，成功将流水线化 All-Reduce 从理论框架转化为高效的工程实现，并在跨域分布式训练场景中展示了其显著的性能提升，提供了针对分层拓扑的集合通信优化新思路。

*工作三（第 5 章）：前缀触发异步 All-Reduce 与预测误差修正机制的通信掩盖策略。* 针对高 RTT 环境下混合并行训练末尾的同步尾部问题，本文提出将 All-Reduce 的触发时机从反向传播完成后前移至前向传播中某一前缀层完成后立即启动，利用前向与后续反向传播之间的计算窗口覆盖跨域通信延迟；对尚未完成反向传播的参数引入预测误差修正，保证全局同步的统计等价性。该机制在"每迭代一次全局同步"的宏观语义下，将跨域 All-Reduce 从迭代关键路径末端剥离，有效改善端到端吞吐与收敛速度。

与现有工作相比，本方法在跨域混合并行训练中提出了以数据并行为主要并行策略架构，创新性地结合了流水线并行的特征，重新设计了同步通信的触发时机，并通过预测误差修正机制确保了训练语义的正确性，在高 RTT 条件下显著降低了同步尾部对整体效率的影响，提供了面向跨域分布式训练的重叠优化新范式。

== 论文结构安排

全文组织如下：

- *第 1 章*：介绍研究背景、问题挑战与本文贡献。
- *第 2 章*：综述云边端协同训练、分布式并行与系统框架、梯度压缩、集合通信调度、通信重叠与尾部优化等方向的代表性工作，并基于此展开分析，确定研究问题、研究路线。
- *第 3 章*：给出低比特量化分布式优化器与低比特位的通信机制的设计与实现，并讨论其在跨域链路上的适用性。
- *第 4 章*：提出并分析层次化流水线 All-Reduce 调度策略，给出工程集成与实验评估。
- *第 5 章*：提出通信受限场景下的混合并行通信掩盖策略，并在跨域约束下给出实验验证与分析。
- *第 6 章*：将低比特量化、层次化流水线通信与计算-通信掩盖整合为统一系统，并给出系统级最终实验与测试汇总。

#pagebreak()
