= 绪论

== 研究背景

近年来，以 Transformer 为代表的深度学习模型架构推动了大语言模型与基础模型的快速演进 @vaswaniAttentionAllYou2023。模型参数规模与训练数据规模的持续增长，使得训练过程越来越依赖大规模分布式并行：从早期的数据并行（Data Parallelism, DP）扩展到张量并行、流水线并行、专家并行等多种并行形态，并在系统侧形成了覆盖“计算、通信、内存”的一整套工程化支撑体系 @shoeybi2019megatron @lepikhin2020gshard @zero2020。

与此同时，训练数据与算力资源正在从“集中式单数据中心”走向“云-边-端协同”的跨域形态：数据可能分布在云端数据湖、边缘微型数据中心以及端侧设备；算力也呈现分散与异构，既包括云侧 GPU 集群，也包括边缘侧 GPU/CPU 节点甚至端侧加速器。云边端协同的驱动力包括数据就地处理、跨地域业务需求、合规约束以及资源弹性调度等。

在这一趋势下，分布式训练通信成为决定端到端效率的核心瓶颈之一：跨域链路（边缘↔云、边缘↔边缘、端↔边的接入网）通常带宽更低、RTT 更高且抖动更大；而域内链路（云内/边缘域内）则可能具有 RDMA/高性能以太网或 NVLink/PCIe 等高带宽互联。通信异构从“节点内/节点间”进一步扩展为“域内/域间/接入”三层差异，使得梯度同步更容易进入迭代关键路径，形成明显的同步尾部（tail latency），并放大 straggler 对整体吞吐的影响。

== 云边端协同分布式训练的通信挑战

在典型的同步训练框架中，梯度同步通常通过 All-Reduce（或其等价变体）实现 DP 组内的均值聚合。当训练跨域展开时，同步通信面临以下挑战：

跨域链路带宽受限会与大模型梯度规模共同放大通信代价：当跨域带宽仅有 10–30 Gb/s（甚至更低）时，传输时间会快速增长并与计算扩展形成强耦合，出现“加卡不加速”的现象。高 RTT 与链路抖动会显著加剧同步尾部，collective 启动、同步点排队与尾部等待在毫秒量级 RTT 下被持续放大，使系统吞吐对最慢节点高度敏感。在域内快、域间慢的分层互联中，扁平化 collective 难以匹配拓扑特性，常导致域内高速链路被域间慢链路拖累；采用“域内聚合—跨域同步—域内分发”的层次结构有助于减少跨域通信量并提升并行效率。在 DP+PP 等混合并行框架中，传统 bucket 级重叠在高 RTT 条件下仍可能无法覆盖迭代末尾阻塞，表明仍需更强的跨阶段重叠机制。

上述挑战表明：面向云边端协同的跨域分布式训练，需要从通信量、集合通信调度以及计算-通信重叠三个层面协同优化，才能在不牺牲训练语义的前提下提升端到端效率。

== 研究问题与目标

本文面向云边端协同的跨域分布式训练场景，围绕“如何在跨域低带宽、高 RTT、强抖动的约束下，降低梯度同步对关键路径的侵入”这一核心问题，聚焦以下三个目标：

具体而言，本文希望在不牺牲优化稳定性的前提下尽可能压缩跨域链路的有效传输字节数，同时利用域内/域间带宽不对称性构建与分层拓扑匹配的集合通信调度机制，并在混合并行与高 RTT 条件下将同步从迭代尾部前移到更长的可重叠窗口，以最大化尾部掩盖效果。

为便于刻画云边端协同的跨域网络特征，本文以跨数据中心训练作为典型强约束实例：其 WAN 带宽与 RTT 可代表边缘↔云或边缘↔边缘的跨域互联，并可在实验中通过网络整形复现。

== 本文主要工作与创新点

本文从“通信量—调度—重叠”三个层次对跨域分布式训练通信优化进行系统性研究，主要工作与创新点如下：

在通信量层面，本文提出面向跨域带宽瓶颈的低比特量化通信机制，基于 k-bit 随机舍入量化优化器（$b in {1, 2, 4, 8, 16}$）与配套通信流程，通过位打包和误差补偿显著压缩梯度传输体积，以缓解端↔边和边↔云受限链路的同步压力；在调度层面，本文针对域内快、域间慢的分层互联特征，提出张量分块与双流并行驱动的流水线层次化 All-Reduce，将“域内归约—域间同步—域内广播”拆解并流水化执行，提升跨域同步并行度与重叠度；在重叠层面，本文面向高 RTT 同步尾部提出前缀触发异步 All-Reduce 与预测误差修正机制，在保持“每迭代一次同步”宏观语义的前提下将同步前移到更早窗口，从而减少迭代末尾阻塞并改善 time-to-target。

== 论文结构安排

全文组织如下：

- *第 1 章*：介绍研究背景、问题挑战与本文贡献。
- *第 2 章*：综述云边端协同训练、分布式并行与系统框架、梯度压缩、集合通信调度、通信重叠与尾部优化等方向的代表性工作，并基于此展开分析，确定研究问题、研究路线。
- *第 3 章*：给出低比特量化分布式优化器与低比特位的通信机制的设计与实现，并讨论其在跨域链路上的适用性。
- *第 4 章*：提出并分析层次化流水线 All-Reduce 调度策略，给出工程集成与实验评估。
- *第 5 章*：提出通信受限场景下的混合并行通信掩盖策略，并在跨域约束下给出实验验证与分析。
- *第 6 章*：将低比特量化、层次化流水线通信与计算-通信掩盖整合为统一系统，并给出系统级最终实验与测试汇总。

#pagebreak()
