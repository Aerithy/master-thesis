= 国内外相关研究

本章围绕“云边端协同的跨域分布式训练通信优化”主题，综述相关研究脉络。为便于对齐本文的三项工作，本章按“并行训练与系统框架—跨域/跨数据中心训练—通信量压缩—集合通信调度—通信重叠与尾部优化—云边端协同训练范式”的逻辑组织。

== 分布式训练并行策略与系统框架

=== 数据并行与参数/状态分片系统

数据并行（Data Parallelism, DP）通过在不同计算节点上复制模型副本并对训练数据批次进行切分，是实现分布式扩展的最基础并行范式且极大地提升了训练吞吐量，但其关键成本来自每步迭代的梯度同步。随着大语言模型时代到来，庞大的模型参数量轻易突破单卡甚至单机节点的显存上限，传统的全复制 DP 难以为继。为此，微软 DeepSpeed @deepspeed2020 团队提出了 ZeRO（Zero Redundancy Optimizer）系列工作 @zero2020。ZeRO 通过将优化器状态、梯度和模型参数这三大显存开销来源在数据并行组内进行独立的分层切分保存（分别对应 ZeRO-1、ZeRO-2 与 ZeRO-3 阶段），从根本上消除了数据并行节点间的显存状态冗余，极大降低了显存占用。在此理念影响下，PyTorch 官方相继推出了原生的 Fully Sharded Data Parallel（FSDP）@fsdp2023 分布式训练框架技术。DeepSpeed 和 FSDP 逐渐演变成为当今学术界与工业界进行大规模语言模型训练时最为核心的底层并行系统基础设施。

从云边端协同角度看，由于边缘站点的计算与存储资源往往比云端数据中心更加局限，ZeRO 等分片优化机制使得显存有限的边缘节点也能够承载部分超大参数模型或较大的 batch 重载。这为“边缘侧与端侧参与统一的大模型训练与聚合”提供了基础系统可行性；但在跨域链路受限的大背景下，数据并行模式每轮更新所必备的庞大跨节点 All-Reduce 通信仍大概率成为阻碍端到端吞吐率的核心瓶颈，需进一步引入特定针对性的网络与通信调度优化。

=== 模型并行与细粒度张量切分机制

当单张加速卡的显存不但无法容下整个模型，甚至连单一模型层的张量以及相关的中间激活状态也难以完全保留时，便衍生出了对计算图进行细粒度切分的模型计算并行。NVIDIA 推出的 Megatron-LM @shoeybi2019megatron 训练开源骨架标志着大规模张量并行（Tensor Parallelism, TP）技术的大规模工业化落地。Megatron 创新地利用了 Transformer 结构的数学特征，将 Attention 和多层感知机（MLP）中的线性层权重矩阵沿着行和列同时执行跨设备的切分计算，实现了在 GPU 节点集内高效的分布式张量矩阵乘。

在后续的规模化发展中，为解决极长上下文序列训练带来的巨大显存暴涨，研究者们将切分维度进一步拓展：序列并行（Sequence Parallelism, SP）将 Transformer 结构中未被 TP 涉及的那些如 LayerNorm 和 Dropout 模块的部分按序切分以避免激活显存冗余 @usp2024sp；而面对数以百万计词元规模的输入，上下文并行（Context Parallelism, CP）则通过将注意力头的计算沿上下文环形拓扑分布式打散 @ulysses2026。此外，面对近年风靡的混合专家系统（Mixture of Experts, MoE）大模型架构，专家并行（Expert Parallelism, EP）@lepikhin2020gshard 则展示了极为高超的参数拓展效率——通过将不同类别的“专家子网络”静态或动态散列分布在网络中的不同设备节点上并依靠高速交换机进行词元级别的 All-To-All 分发路由，成功在只增加微量额外计算通信的前提下带来了整体模型容量的几何级数极速暴增。

=== 流水线并行与流水调度策略优化

流水线并行（Pipeline Parallelism, PP）通过将层级极深的大规模神经网络按序列划分出多个阶段（Stage），并将不同的 Stage 均等映射放置于不同的计算节点或设备组上，突破了内存墙与节点独立算力限制。为了削减不同级联 Stage 等待数据的闲置并隐藏空闲的气泡（Bubble）时间，GPipe 系统 @gpipe 等开创性地提出了微批次（Micro-batch）的切分输送机制。

为更高效拉高硬件利用效率、突破深层反向链条带来的峰值显存激增，PipeDream 系统 @pipedream 及其后续工作提出并确立了经典的 1F1B（One Forward One Backward）调度策略。它是目前主流流水线并发的基石范式之一，通过让流水设备交替进行当前微批次的前向与另一个微批次的反向阶段的计算投递，从而迅速释放已经过时的中间激活来收起峰值显存线。在 1F1B 的基础上，业界在近年不断推出深度的拓扑排列改进以逼近性能理论极限：交错 1F1B（Interleaved 1F1B）通过给每一台物理设备人为指派多个彼此不邻接的阶段负载，进一步通过多阶段重叠大幅削减了原本存在的流水首尾启动气泡空腔的固有比例 @narayanan2021efficient。而针对最终残留不可消除的气泡，诸如 ZeroBubble PP @zerobubble 与 DualPipe @she2025mip 等更为激进而先进的双向/三向层级掩盖交叉调度等前沿算法横空而出。它们通过在反向阶段进一步细分子执行块，或利用前后向双向流的算子互叠时间等错位手段，几乎把流水线上的设备空载损耗降至理论极限的“零气泡”。

在云边端协同的大规模调度拓扑场景中，由于需要进行极为频繁的激活及其对应特征梯度的逐层前向与后向传递投喂，流水线并行及其演进的系列并发切分方法，通常更适合在同一云内或是网络带宽极大且极度稳定的特定局部边缘域内采用，以避免在带宽严重劣化的跨域链路间反复进行高频激活层的大量传输。作为对比而言，在广域跨域层面的宏观网络下，采用只在每次模型完整迭代的末期进行单次最终梯度传输交互的数据并行范式无疑更为常见。

== 跨域与跨数据中心训练

跨数据中心/地理分布式训练可视为云边端协同中的"域间互联"的典型形态，其核心特征除物理距离远外，还包括网络带宽低、时延高、抖动大，以及可调度计算资源呈现出的不稳定性和异构性。与单机房或单集群训练不同，跨数据中心训练往往需要在 WAN 约束下协调多个自治域，因此，围绕跨域训练的研究通常同时关注"如何组织计算"和"如何组织通信"两个层面。

从系统框架看，近年来已经出现了面向跨数据中心流水线调度与训练框架的代表性研究。CrossPipe 针对跨数据中心训练提出更优的流水线调度策略，强调在强约束 WAN 下通过调度重排和阶段组织减轻通信尾部 @crosspipe2025；GeoPipe 探索了地理分布式 LLM 训练框架中的流水线并行增强，进一步说明了光网络或专线互联条件下跨机房流水线训练的可行性与收益 @geopipe2025；JEEVES 则从系统协调和任务编排角度研究跨 DC 训练的调度问题，体现出跨域训练不仅是通信优化问题，也是跨域资源协同问题 @jeeves2025。上述工作共同说明，跨域训练的关键不在于简单复制单域并行方案，而在于根据域间链路特征重新设计训练阶段划分、同步位置和执行顺序。

从训练范式看，跨数据中心训练还与低通信频率和弱同步优化紧密相关。经典的 FedAvg 将局部多步更新与周期性模型平均结合，在不频繁同步的情况下实现分布式训练 @mcmahan2017fedavg；Local SGD 进一步从理论和实践角度说明了“少通信、多本地更新”在收敛与泛化上的可行性 @stich2019local @localsgd_generalization2023；SCAFFOLD 则通过控制变量修正局部漂移，缓解了数据异构条件下的优化偏差 @KarimireddyKMRS20。沿着这一思路，OpenDiLoCo 将低通信频率训练推广到全球分布式场景 @opendiloco2024，而 Streaming DiLoCo 进一步强调通信与计算的重叠，试图在保留低通信频率优势的同时缩短同步尾部 @streaming_diloco2025。这类方法与本文的关系在于：它们说明在广域网络中，降低同步频率和缩短同步关键路径是提升系统效率的重要方向，但若要保持更严格的训练语义和更可控的收敛过程，仍需要对同步数据量、通信调度和尾部重叠做更细粒度的系统优化。

从更广义的宽域并行作业角度看，数据中心之间的优化问题涵盖深度学习等多个领域。PIXIDA 针对宽域数据分析任务提出了面向数据并行作业的优化机制，为理解 WAN 约束下的任务放置、作业切分与跨域传输提供了参考 @pixida2015。该类工作表明，跨域系统设计必须同时考虑网络拓扑、数据移动和任务执行代价，这一点与本文对云边端协同训练的建模方式是一致的：跨域链路上的通信是决定训练是否能够高效运行的核心约束。

综合上述研究可以看到，跨数据中心训练已经形成了三条相互关联但侧重点不同的技术路线：其一是以 CrossPipe、GeoPipe、JEEVES 为代表的跨域训练系统与调度框架，重点解决“怎么排”；其二是以 FedAvg、Local SGD、SCAFFOLD、OpenDiLoCo 为代表的低通信频率或弱同步范式，重点解决“怎么少传”；其三是以 Streaming DiLoCo 等工作为代表的通信-计算重叠方法，重点解决“怎么遮蔽尾部”。本文第 2 章后续章节中的三项工作分别对应上述技术路线中的后三个关键问题，即在保持跨域 DP 语义的前提下，进一步解决“传多少、怎么排、如何掩盖尾部”的系统化问题。

== 梯度压缩与通信量优化

梯度压缩通过减少每步需要传输的数据量来降低通信时间，是应对跨域带宽受限的第一道系统防线。现有研究大体可分为三类：量化压缩、稀疏化压缩与低秩压缩。三类方法的共同目标都是在尽可能保持收敛语义的前提下压缩梯度通信体积，但其信息保留方式、误差来源以及工程实现复杂度各不相同。

=== 量化与低比特表示

量化方法通过降低梯度数值精度来直接压缩传输字节数，是最直观也最易工程化的一类通信压缩策略。早期研究表明，深度学习训练可以采用低精度数值表示：Deep Learning with Limited Numerical Precision 从训练数值稳定性的角度说明了低精度表示的可行性 @gupta2015deep。随后，1-bit stochastic gradient descent 将梯度直接压缩到符号级别，在分布式训练中证明了极低位宽通信的潜力 @seide20141bit。QSGD 进一步从随机量化的角度给出了具有理论收敛保证的通信最优量化框架 @alistarh2017qsgd；TernGrad 则使用三值梯度表示，在保持较低误差的同时缓解了通信瓶颈 @wen2017terngrad。

在更贴近大模型训练的场景中，量化方法通常不再仅仅追求极端低比特，而是强调精度、收敛和工程效率之间的平衡。对于跨域训练而言，这一平衡尤为重要：链路带宽本身就是瓶颈，如果压缩率不足以覆盖 WAN 传输成本，量化就难以体现系统收益；但若位宽过低，又容易引入较大的量化噪声并放大收敛不稳定性。因此，围绕随机舍入、动态缩放与误差补偿的低比特量化框架，逐步成为当前跨域同步压缩的主流方向。

=== 稀疏化与误差反馈

稀疏化方法通过只传输 Top-$k$ 或随机采样的一部分梯度元素来减少通信量，但会引入偏差或方差增大 @aji2017sparse。为了缓解这种信息损失，研究者提出了带记忆的稀疏化方案：Sparsified SGD with Memory 通过维护残差累积未发送信息，从而改善稀疏传输下的优化轨迹 @stich2018sparsified。Deep Gradient Compression 进一步将动量修正、局部裁剪、稀疏化与延迟补偿组合为完整系统，展示了在大规模分布式训练中实现数量级带宽压缩的可行性 @dgc2018。后续研究还进一步从理论与实践上分析了误差反馈在压缩 SGD 中的关键作用，例如 Error Feedback Fixes SignSGD and other Gradient Compression Schemes 说明误差反馈能够修正符号压缩等方案的偏差，使其恢复稳定性 @errorfeedback_signsgd2019；Step-Ahead Error Feedback 和 Detached Error Feedback 则继续将误差补偿扩展到更灵活的时序与随机稀疏设置中 @stepahead_error_feedback2021 @detached_error_feedback2022。

在云边端协同场景中，误差反馈对跨域受限链路尤为重要：当端↔边、边↔云链路带宽波动时，误差反馈可作为一种“残差信道”，将被压缩或延迟的梯度信息在后续迭代中逐步补偿，从而在系统效率与优化稳定性之间提供可控折中。

=== 自适应压缩与系统协同

为了在不同网络与梯度分布下稳定获益，研究还探索了更自适应的稀疏压缩与系统协同设计。例如 PacTrain 将剪枝与自适应稀疏梯度压缩结合，用于更高效的集合通信 @pactrain2025。误差反馈也被扩展到更一般的预条件器与优化器状态压缩中 @zhang2023errorfeedback。

=== 低秩与混合压缩

除量化和稀疏化外，低秩压缩也是大规模分布式训练中的一条重要路线。PowerSGD 将梯度矩阵近似分解为低秩因子，只交换近似秩分解所需的子空间信息，从而在保持较高精度的同时显著降低通信量 @powersgd2019。与逐元素量化或稀疏化相比，低秩压缩更适合具有明显矩阵结构的梯度张量，尤其适用于 Transformer 中的线性层参数更新。但这类方法对矩阵结构与秩近似质量更敏感，通常需要与误差反馈或分层同步结合，才能在复杂训练任务中稳定发挥作用。

综合来看，现有压缩方法已经从“单纯减少发送比特数”发展为“压缩策略、误差补偿与训练系统协同设计”的复合问题。本文第 3 章聚焦低比特量化路线：在尽量保持训练语义的前提下，将跨域同步通信的字节规模压缩到更低水平，并在后续章节与“分层调度”和“重叠”机制组合，以适配云边端跨域约束。

== 集合通信优化与层次化调度

All-Reduce 是 DP 中最关键的集合通信原语之一，其实现方式直接决定了通信带宽利用率、同步尾部和系统可扩展性。经典的集合规约研究已经较早指出，ring、tree 与递归倍增等算法在不同拓扑和消息规模下具有不同的时间代价 @rabenseifner2004。到了大规模深度学习阶段，这一问题被进一步工程化：Horovod 将 ring-based reduction 作为主流实现路径，证明了简单易用的框架接口与高效的集合通信可以兼得 @horovod2018；NCCL 则成为 GPU 集群中的事实标准，为多 GPU 训练提供了高度优化的底层通信库 @nccl2。

随着硬件异构性、拓扑层次性和训练负载复杂度不断上升，研究者开始从“如何执行 All-Reduce”转向“如何自动生成和优化集合通信原语”。Blink 通过 packing spanning trees 来生成更适合异构网络的通信原语，表明树形结构在某些场景下能够优于固定实现的 NCCL @blink2019；TACCL 则进一步将集合通信优化抽象为算法合成问题，利用 communication sketches 指导 synthesizer 自动生成适配特定拓扑的 collective algorithm，并在 DGX-2 和 NDv2 上证明其可显著超越 NCCL @taccl2021。此类工作说明，集合通信不再只是固定库调用，而是一个可被拓扑感知、可被自动合成的系统设计空间。

面向训练系统本身，研究者也开始关注集合通信与训练语义、尾部鲁棒性和压缩机制的联合优化。OptiReduce 针对云环境中的 straggler、拥塞和梯度丢包问题，提出了尾部最优的 AllReduce 设计，并通过不可靠传输、Transpose AllReduce 和 Hadamard 变换等机制在性能与精度之间取得折中 @optireduce2023。BytePS-Compress 则从"压缩 + 参数服务器"的角度，将双向梯度压缩、CPU 端压缩/解压管线化与高并发传输结合起来，展示了压缩通信与系统实现的协同收益 @bytepscompress2021。本文的研究重点在于充分利用已有通信原语：在跨域、域内带宽高度不对称的前提下，针对分层 All-Reduce 的阶段解耦与调度重排，提升已有原语在云边端协同场景中的有效利用率。

跨域训练研究进一步表明，仅有层次化还不够：当域间 RTT 高且通信启动开销显著时，需要通过流水线化、分块、双流并发等方式提高并行度并隐藏启动开销 @crosspipe2025。本文第 4 章从工程可实现的角度给出分块与双流的层次化流水线调度策略，并在实验设置中以“域内/域间链路”参数来对齐云边端协同拓扑。

== 通信重叠与同步尾部优化

在常规数据中心环境中，框架通常通过 bucket 化在反向传播中逐步启动 All-Reduce，以便让后续层的反向计算掩盖前面 bucket 的通信时间。这类做法本质上属于经典的计算-通信重叠（computation-communication overlap）范式，目标是在不改变同步语义的前提下，将通信从关键路径中尽可能挤出。对应到工程实现，训练框架往往依赖梯度分桶、通信流与计算流并发、以及后端通信库的异步接口来实现重叠。然而，当训练跨越 WAN 链路时，高 RTT、抖动和带宽不稳定会显著拉长通信尾部，导致最后若干 bucket 无法被剩余计算完全掩盖，最终形成同步尾部和迭代末端空转。

围绕这一问题，研究者首先从“减少同步频率”这一方向寻找折中。FedAvg 将本地多步更新与周期性模型平均结合，在一定程度上减少了全局同步的次数 @mcmahan2017fedavg；Local SGD 则从理论上说明了少通信、多本地更新的收敛可行性 @stich2019local @localsgd_generalization2023。进一步地，SCAFFOLD 通过控制变量修正局部漂移，缓解了数据异构条件下因少同步带来的优化偏差 @KarimireddyKMRS20。这类方法与跨域训练高度相关，因为它们指出：只要同步频率足够低，即使跨域链路较慢，系统仍有机会把绝大多数计算放在本地完成，从而将通信成本降到可接受水平。

在更贴近跨域大模型训练的系统中，低通信频率与计算重叠开始被显式结合。OpenDiLoCo 以全球分布式低通信训练为目标，探索了在跨地域环境下维持训练可行性的系统框架 @opendiloco2024；Streaming DiLoCo 则进一步强调通信与计算的重叠，通过流式执行减少同步等待，使得跨域通信不必完全阻塞本地训练过程 @streaming_diloco2025。与之相关的 Compensated Overlap-FedAvg 则从联邦学习视角将“局部训练 + 重叠补偿”结合起来，说明即使在弱同步条件下，也可以借助补偿机制控制误差积累 @compensatefedavg。上述工作共同表明，在广域或跨域环境中，单纯依赖传统 bucket overlap 往往不足以消除尾部，必须进一步引入低频同步、补偿更新或流式重叠等策略，才能稳定释放系统吞吐。

除降低频率外，另一条关键思路是改变同步触发时机与执行位置，将不可避免的通信从迭代尾部迁移到更早的可重叠窗口中。这个方向的核心在于：只要同步语义允许，梯度同步可以在所有反向计算完成前的更早前缀阶段启动，从而把余下通信尽可能隐藏在后续计算之下。与本文最接近的相关工作是 Streaming DiLoCo 这类以重叠通信为核心思想的系统，以及围绕尾部优化的集合通信研究 @streaming_diloco2025 @optireduce2023。本文第 5 章提出的“前缀触发的异步 All-Reduce + 预测误差修正”机制，正是沿着这一方向进一步推进：它不改变“每迭代一次同步”的宏观训练语义，而是通过提前触发和误差修正，将跨域同步从关键路径尾部剥离，并尽可能转化为可被计算掩盖的后台通信。

综合来看，通信重叠与同步尾部优化已经形成了从“bucket overlap”到“低通信频率”，再到“前缀触发与尾部剥离”的连续谱系。本文第 5 章正是在这一谱系上，面向云边端协同的跨域训练场景，聚焦高 RTT 与强抖动条件下同步尾部难以隐藏的问题，设计出更适合跨域链路约束的重叠优化机制。

== 云边端协同训练范式与研究空白

=== 云边端协同训练问题

结合本文的背景，可以看到，云边端协同训练是一种跨域资源联合训练：

这种跨域性首先体现在网络层：域内链路通常高带宽、低时延，而域间链路普遍低带宽、高 RTT 且抖动更大；其次体现在数据层：训练样本天然分布在云侧数据湖、边缘站点与端侧来源，不宜也不总能集中回传；再次体现在算力层：参与训练的设备来自不同自治域，调度能力和负载上限差异显著。

因此，云边端协同训练的主要矛盾在于如何在异构域间链路约束下组织同步与并行。这一定义也给出本章的阶段性结论：需要先回答“跨域并行策略如何选择”，再讨论“在既定策略下如何做通信优化”。

=== 云边端场景下的跨域训练策略的选择

综合并行训练与跨域训练研究，可将策略选择归结为两个判断维度：跨域通信对象是否随 batch 增长，以及该通信是否持续位于关键路径。

当采用跨域流水线并行（Cross-domain PP）时，模型 stage 会跨域切分，域间通信对象主要是激活及其反向梯度；这一方案在参数规模受限场景中有助于继续扩展模型，但跨域通信与样本流强耦合，WAN 抖动容易沿流水线级联放大。相比之下，跨域数据并行（Cross-domain DP）将前反向计算留在各域本地，域间交互集中为梯度或参数同步，对网络抖动更鲁棒，但前提是同步路径具有足够高效的通信实现。

本文通过量化推导给出更具体的比较关系。设全局 batch 为 $B$, 参数规模为 $P$, 每参数通信字节为 $alpha$, 跨域切分时每样本跨域激活字节为 $A$：

跨域 DP 的通信量近似为 $V_("DP") approx alpha P$，在模型固定时对 $B$ 近似不敏感；而跨域 PP 的通信量近似为 $V_("PP") approx Theta(B * A)$，会随 $B$ 线性增长。

据此可得到如下选择原则：

- 当目标是继续突破单域模型容量上限，且域间链路相对稳定时，可考虑跨域 PP；
- 当目标是在受限 WAN 条件下保持稳定吞吐与训练语义可控时，应优先跨域 DP。

在大 batch 训练下，跨域 PP 的 WAN 压力会持续放大；而跨域 DP 的固定同步开销更容易被计算摊薄。因此，许多系统实践逐步收敛到“跨域 DP + 域内PP”这一分层组织方式：跨域层面优先保证稳定吞吐，域内层面再用流水线并行扩展模型容量与设备利用率 @gpipe @pipedream。

在此基础上，本文进一步说明为何采用跨域数据并行而非跨域流水线并行。本文的问题设定是“云边端协同下的大规模数据训练”，核心目标是在真实跨域链路约束下提升端到端吞吐并保持优化语义稳定。本文选择跨域 DP 的原因主要有三点：

- 数据局部性：各域可利用本地数据独立完成前反向计算，避免样本与高频激活长期占用 WAN；
- 关键路径可控：域间交互收敛为同步阶段，便于对关键路径进行可测量、可治理的优化；
- 与后续方法匹配：第 3 章、第 4 章、第 5 章分别优化“同步数据量、同步调度效率、同步尾部重叠”，三者围绕同一域间同步对象形成协同闭环。

本文将流水线并行定位为域内扩展手段：在单域内利用高速互联做 PP/TP，在跨域层面采用 DP 语义，以同时兼顾模型规模扩展、网络鲁棒性与工程可落地性。

=== 本文后续研究路线

在以DP作为基础架构下的云边端协同训练既可以采用集中式跨域同步（强调全局一致性），也可以采用更弱同步或更强异步的范式。例如 Local SGD 通过降低通信频率以减少开销，并在部分设置下获得较好的通信-收敛折中 @stich2019local；联邦学习（Federated Learning）@mcmahan2017fedavg 强调数据不出端，并在非独立同分布与系统异构下发展出多种控制偏差与加速收敛的算法（如 SCAFFOLD）@KarimireddyKMRS20。

然而，在“云-边-端协同的大模型训练”这一目标下，系统通常希望在可控的语义约束下保持较强的一致性（例如每步一次跨域同步或周期性强同步），以避免优化轨迹偏离带来的不可控风险；同时还希望尽可能复用主流深度学习框架与通信后端，降低工程集成成本。因此，仍需要面向分层拓扑与跨域链路的通信优化：

- 在跨域同步不可避免时，如何进一步降低通信体积并控制误差累积？
- 在域内/域间带宽高度不对称时，如何设计集合通信调度，优化集合通信执行时间？
- 在高 RTT 场景下，如何将同步从尾部迁移到可重叠窗口，并用误差修正保持稳定语义？

=== 本文通信优化系统的整体论述：从单点优化到端到端协同

为回答上述问题，本文构建了一个面向云边端跨域协同训练的通信优化系统。该系统以“跨域 DP（域间）+ 多维混合并行（域内 DP/TP/PP）”为执行底座，在统一训练语义下把通信优化拆解为三个可组合组件，并通过接口与时序协同形成闭环。

*组件一：低比特量化通信（第 3 章）*  
在不改变训练主流程的前提下，对跨域同步张量进行低比特表示与误差补偿，将跨域传输字节数显著压缩。其系统角色是降低通信量，直接降低 WAN 有效负载与拥塞概率，为后续调度与重叠创造更大的时间窗口。

*组件二：分层集合通信与流水化调度（第 4 章）*  
针对“域内快、域间慢”的网络事实，设计域内/域间分层 All-Reduce，并在域间阶段采用分块、流水、并发传输策略，提升慢链路利用率并降低启动开销占比。其系统角色是提升集合通信效率，把已经压缩后的通信数据以更匹配拓扑的方式传输，缩短同步主干时长。

*组件三：DP+PP 混合并行下的计算-通信掩盖（第 5 章）*  
结合域内大模型多维混合并行配置，重构跨域同步触发时机与执行路径，将部分同步从迭代尾部前移到可重叠区间，并通过预测/误差修正维持优化稳定。其系统角色是增加计算通信掩盖的空间，把剩余通信进一步掩盖到计算过程中，减少 GPU 空等。

从系统视角看，三者满足明确的协同顺序与叠加收益关系：

- 首先，量化减少梯度通信通信量；
- 其次，分层流水调度对通信算法进行合理调度提升带宽利用率；
- 最后，重叠机制去除无法掩盖的通信同步尾部。

因此，本文聚焦于云边端跨域协同训练中的端到端瓶颈治理问题：在保持跨域 DP 语义一致性与工程可部署性的前提下，通过系统性优化通信体积、调度效率与通信重叠来缓解"带宽受限、时延偏高、抖动显著"带来的训练吞吐下降与尾部阻塞。

综上，本文三项工作在统一框架下协同运作：第 3 章降低跨域通信体积，第 4 章提升分层调度与带宽利用率，第 5 章降低同步尾部并提升重叠程度，最终形成一套可落地、可组合、可扩展的云边端跨域训练通信优化系统。

#pagebreak()
