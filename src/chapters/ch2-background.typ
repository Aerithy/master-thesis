= 国内外相关研究

本章围绕“云边端协同的跨域分布式训练通信优化”主题，综述相关研究脉络。为便于对齐本文的三项工作，本章按“并行训练与系统框架—跨域/跨数据中心训练—通信量压缩—集合通信调度—通信重叠与尾部优化—云边端协同训练范式”的逻辑组织。

== 分布式训练并行策略与系统框架

=== 数据并行与参数/状态分片

数据并行（DP）通过复制模型并切分数据批次实现扩展性，但其关键成本来自每步梯度同步。为降低显存占用并支撑更大模型，研究与工业界提出了多种参数/优化器状态分片策略，例如 ZeRO 系列工作通过对优化器状态、梯度与参数进行分层分片，实现更高的内存效率 @zero2020。PyTorch Distributed 与 Fully Sharded Data Parallel（FSDP）提供了工程化的分片并行支持与规模化经验总结 @pytorch_distributed2020 @fsdp2023。

从云边端协同角度看，分片与内存优化使得边缘节点也能承载更大模型或更大 batch，从而为“边缘侧参与训练与聚合”提供系统基础；但在跨域链路受限时，DP 的同步通信仍可能成为端到端瓶颈，需进一步引入通信优化。

=== 模型并行与流水线并行

当模型规模超过单设备显存时，常采用张量并行、流水线并行等方式分解计算。Megatron-LM 展示了在大规模 GPU 集群上训练多十亿参数语言模型的系统实践 @shoeybi2019megatron；GShard 等工作探索了更大规模模型的自动切分与条件计算 @lepikhin2020gshard。

流水线并行通过将网络层划分为多个 stage，并以 micro-batch 的方式串行注入、并行执行。GPipe 与 PipeDream 是两类代表性流水线并行系统：前者强调高效的流水线执行与可扩展性，后者提出更一般化的流水线并行与调度策略 @gpipe @pipedream。近年来还有以减少流水线气泡为目标的改进工作，例如 Zero Bubble Pipeline Parallelism @zerobubble。

在云边端协同场景中，流水线并行通常更适合在同一域内（如边缘域内或云内）使用，以避免跨域传输激活带来的高频通信；而跨域层面更常采用 DP 语义，只在迭代末进行一次梯度同步，从而将跨域通信频率降到最低。

== 跨域与跨数据中心训练

跨数据中心/地理分布式训练可视为云边端协同中的“域间互联”极端形态：带宽相对受限、RTT 与抖动显著，且网络与计算资源分散。围绕跨域训练的系统问题，近年来出现了面向跨数据中心流水线调度与训练框架的研究，例如 CrossPipe 针对跨数据中心训练提出更优的流水线调度策略 @crosspipe2025；GeoPipe 探索了地理分布式 LLM 训练框架中的流水线并行增强 @geopipe2025；JEEVES 从调度视角研究跨 DC 训练的系统协调问题 @jeeves2025。

除深度学习训练外，宽域数据分析任务也积累了大量关于跨域数据并行作业优化的经验，例如 PIXIDA 针对宽域数据分析任务的优化机制为理解 WAN 约束下的并行作业提供了参考 @pixida2015。

总体而言，跨域训练研究揭示了两个核心规律：一是跨域通信通常成为关键路径瓶颈；二是“调度 + 通信压缩 + 重叠”往往需要协同设计，单点优化难以稳定获得收益。本文第 4–5 章的调度与重叠机制与这一脉络对齐，并进一步强调云边端分层拓扑下的工程可落地性。

=== 云-边-端协同训练中的并行策略选择与“域间训练”化简

云-边-端协同训练面对三类同时存在的异构性：*算力异构*（云侧 GPU 集群 vs. 边缘小集群/端侧设备）、*数据异构与就地约束*（数据难以集中、合规限制）、以及 *网络异构*（域内高带宽低时延、域间低带宽高 RTT 且抖动大）。在此背景下，“采用什么并行策略”本质上就是在 *计算可扩展性*、*跨域通信代价* 与 *优化语义稳定性* 之间做系统性折中。结合跨域训练（尤其是跨数据中心/WAN 约束）研究的经验，可将这一选择问题概括为以下四点：

1) *现有的并行策略（训练系统设计空间）*
	- *数据并行（DP）与其分片变体（ZeRO/FSDP 等）*：跨节点复制模型、切分数据，核心通信是梯度或参数/状态同步；优势是实现成熟、与数据就地天然兼容，但同步开销可能在受限链路上进入关键路径。
	- *模型并行*：包括张量并行（TP）与流水线并行（PP）。TP 需要高频的跨设备张量通信，通常依赖域内高速互联；PP 以 micro-batch 推进，跨 stage 需要传输激活/梯度，通信路径是否跨域会决定其可行性。
	- *同步语义的松弛*：从 *全同步*（每步全局屏障）到 *部分异步/周期同步*（如 Local SGD）再到 *完全异步*。松弛同步可以降低通信频率，但会引入梯度陈旧（staleness）和模型漂移（drift）风险，尤其在数据/算力异构时更需要谨慎。

2) *在云边端中，为什么可以将其转化为“域间训练”的问题*
	- 从拓扑上看，云-边-端通常可抽象为多个“域”（云域、若干边缘域、端侧群体），域内可提供相对稳定的高带宽互联，而域间链路（边↔云、边↔边、端↔边）往往呈现 WAN 特征：带宽显著更低、RTT 更高且波动更大。
	- 从系统关键路径看，决定吞吐的不再是“总通信量”本身，而是 *哪些通信跨域且落在强依赖链路上*。因此把云边端抽象为“域间训练”（inter-domain training）问题，可以把优化重点收敛到：如何在域间慢链路上减少数据搬运、降低同步尾部、并提升计算-通信重叠。
	- 从方法迁移上看，跨数据中心训练可视为云边端协同中的“域间互联极端形态”，其经验（例如 WAN 下 PP vs DP 的取舍、同步尾部的治理方式）可直接迁移到云边端：域间链路的一阶瓶颈性质是相同的。
	- 从“通信量随规模的增长规律”看，域间训练的核心差异可以被形式化刻画。考虑 $D$ 个域（云域/边缘域等），每个域处理本地 batch 为 $b$，则全局 batch 为 $B = D dot b$。令 $P$ 为模型参数规模（按参数个数或字节计），$alpha$ 为每次同步每个参数需要通信的字节数（由精度/压缩决定），$A$ 为当流水线切分跨越域间时、每个样本需要跨域传输的激活（含反向激活梯度）字节数。则：
		+ 若跨域采用数据并行（域间 DP），跨域通信主要来自每步一次的梯度同步，其通信量近似
		  $ V_"DP" approx alpha dot P $
		  即在模型固定时，$V_"DP"$ 随 $B$ 增加近似不变。
		+ 若流水线并行的切分跨越域间（跨域 PP），则迭代内跨域通信量随样本数线性增长：
		  $ V_"PP" approx Theta(B dot A) $
		  这意味着随着全局 batch 增大，跨域 PP 的流量与代价会同步放大。

3) *云边端场景下的协同训练中，通常采用什么样的并行策略*
	- 一个常见、工程上更稳健的骨架是 *域间 DP + 域内（TP/PP/分片）* 的层次化并行：域内利用高速互联做模型切分与高频通信，域间只做低频、语义明确的同步（通常是梯度/参数聚合）。
	- 原因在于：若把 PP 的 stage 边界跨越域间链路，则需要频繁传输激活/梯度，这类传输不仅量大，而且处在严格的 stage 依赖链路上，容易被高 RTT 与抖动放大并级联阻塞，使迭代时间对网络波动高度敏感；而域间 DP 将前/后向计算局限在域内，把域间交互压缩到同步阶段，更符合“数据就地 + 网络受限”的约束。
	- 在同步语义上，很多系统会在“全同步”与“弱同步”之间折中：例如减少同步频率（Local SGD/周期同步）以摊薄通信，但同时需要额外的漂移控制/误差修正机制来维持稳定收敛。

4) *在本文的解决方案中，为什么最后采取的是域间数据并行策略*
	- *避免域间激活搬运*：本文面向的跨域链路具备高 RTT、带宽受限与抖动特征。若采用跨域 PP，会导致样本/激活沿流水线跨域流动，既产生入口汇聚瓶颈，也把域间通信放到强依赖的关键路径上；相对地，域间 DP 将激活留在域内，只在迭代末/同步阶段跨域交换梯度，更契合云边端数据就地约束。
	- *对网络抖动更鲁棒*：PP 的跨域依赖会把任一域的短暂变慢放大为全流水线停顿；DP 的跨域交互更集中、更容易在系统层面做调度/重叠，从而降低抖动对吞吐的放大效应。
	- *从通信时延下界看更易被“摊薄/掩盖”*：设域间有效带宽为 $"BW"_("inter")$、域间往返时延为 $"Lat"_("inter")$，则通信耗时的一个粗略下界可写为
		$ T_"comm" approx "Lat"_("inter") + V / "BW"_("inter") $。
		结合上述通信量刻画可得：
		+ 域间 DP：$ T_"comm"^"DP" approx "Lat"_("inter") + (alpha dot P) / "BW"_("inter") $（与 batch 大小弱相关）；
		+ 跨域 PP：$ T_"comm"^"PP" approx "Lat"_("inter") + (c dot B dot A) / "BW"_("inter") $（随 $B$ 线性增长，其中 $c$ 与跨域切分数量、前/后向方向等常数有关）。
		因此在云边端希望通过扩大局部 batch、增强域内计算并行来提升吞吐时，域间 DP 的“固定同步成本”更容易被每步计算量摊薄，并进一步通过调度/重叠策略从关键路径中剥离。
	- *便于与后续通信优化组合*：本文三章工作分别从通信体积（第 3 章）、层次化调度与流水线 All-Reduce（第 4 章）、以及同步尾部重叠（第 5 章）切入。它们都更自然地作用在“域间梯度同步”这一明确的瓶颈上：在确定域间 DP 骨架后，可以进一步用分层调度提升带宽利用率，并将 All-Reduce 从迭代尾部迁移到更早的可重叠窗口，以减少尾部等待（第 5 章的前缀触发异步 All-Reduce 及误差修正思路与跨域同步尾部问题直接对齐）。

== 梯度压缩与通信量优化

梯度压缩通过减少每步需要传输的数据量来降低通信时间，主要路线包括稀疏化与量化两类。

=== 稀疏化与误差反馈

稀疏化方法通过只传输 Top-$k$ 或随机采样的一部分梯度元素来减少通信量，但会引入偏差或方差增大。Sparsified SGD with Memory 提出了带记忆的稀疏化机制，通过误差反馈（error feedback）累积未发送信息以改善收敛 @stich2018sparsified。后续研究进一步从理论与实践上分析了误差反馈在压缩 SGD 中的关键作用，例如 Error Feedback Fixes SignSGD 等工作说明误差反馈能够修正符号压缩等方案的偏差，使其恢复稳定性 @errorfeedback_signsgd2019。

在云边端协同场景中，误差反馈对跨域受限链路尤为重要：当端↔边、边↔云链路带宽波动时，误差反馈可作为一种“残差信道”，将被压缩或延迟的梯度信息在后续迭代中逐步补偿，从而在系统效率与优化稳定性之间提供可控折中。

=== 自适应压缩与系统协同

为了在不同网络与梯度分布下稳定获益，研究还探索了更自适应的稀疏压缩与系统协同设计。例如 PacTrain 将剪枝与自适应稀疏梯度压缩结合，用于更高效的集合通信 @pactrain2025。误差反馈也被扩展到更一般的预条件器与优化器状态压缩中 @zhang2023errorfeedback。

本文第 3 章聚焦低比特量化路线：在尽量保持训练语义的前提下，将跨域同步通信的字节规模压缩到更低水平，并在后续章节与“分层调度”和“重叠”机制组合，以适配云边端跨域约束。

== 集合通信优化与层次化调度

All-Reduce 是 DP 中最关键的集合通信原语之一，其实现方式（如 ring、tree 以及分层变体）决定了通信带宽利用率与同步尾部。对于域内快、域间慢的分层拓扑，层次化 All-Reduce 通过“域内聚合—域间同步—域内分发”减少跨域通信量，并将跨域同步集中在少量代表进程之间，从而更好地匹配带宽不对称性。

跨域训练研究进一步表明，仅有层次化还不够：当域间 RTT 高且通信启动开销显著时，需要通过流水线化、分块、双流并发等方式提高并行度并隐藏启动开销 @crosspipe2025。本文第 4 章从工程可实现的角度给出分块与双流的层次化流水线调度策略，并在实验设置中以“域内/域间链路”参数来对齐云边端协同拓扑。

== 通信重叠与同步尾部优化

在常规数据中心环境中，框架常通过 bucket 化在反向传播中逐步启动 All-Reduce 以实现计算-通信重叠；但在 WAN 环境下，高 RTT 可能使得最后几个 bucket 无法被剩余计算掩盖，形成同步尾部。围绕“低通信频率”与“重叠通信”的设计，近年来出现了多类跨域训练方法与系统，例如 OpenDiLoCo 以低通信频率为目标探索全球分布式训练框架 @opendiloco2024，而 Streaming DiLoCo with overlapping communication 进一步强调通信与计算的重叠以提升系统效率 @streaming_diloco2025。

除低通信频率路线外，另一条关键思路是改变同步触发时机：将一次关键的同步从迭代末尾迁移到更早的可重叠窗口中，从而缩短尾部。本文第 5 章采用“前缀触发的异步 All-Reduce + 预测误差修正”的机制，目标正是将跨域同步从关键路径尾部剥离，并在保持“每迭代一次同步”的宏观语义下获得更稳定的系统收益。

== 云边端协同训练范式与研究空白

云边端协同训练既可以采用集中式跨域同步（强调全局一致性），也可以采用更弱同步或更强异步的范式。例如 Local SGD 通过降低通信频率以减少开销，并在部分设置下获得较好的通信-收敛折中 @stich2019local；联邦学习（Federated Learning）强调数据不出端，并在非独立同分布与系统异构下发展出多种控制偏差与加速收敛的算法（如 SCAFFOLD）@KarimireddyKMRS20。

然而，在“云-边-端协同的大模型训练”这一目标下，系统通常希望在可控的语义约束下保持较强的一致性（例如每步一次跨域同步或周期性强同步），以避免优化轨迹偏离带来的不可控风险；同时还希望尽可能复用主流深度学习框架与通信后端，降低工程集成成本。因此，仍需要面向分层拓扑与跨域链路的通信优化：

- 在跨域同步不可避免时，如何进一步降低通信体积并控制误差累积？
- 在域内/域间带宽高度不对称时，如何设计层次化与流水线化的集合通信调度？
- 在高 RTT 场景下，如何将同步从尾部迁移到可重叠窗口，并用误差修正保持稳定语义？

本文的三项工作分别对上述问题给出回答，并在统一的跨域训练视角下形成互补：第 3 章降低跨域通信体积，第 4 章提升分层调度与带宽利用率，第 5 章降低同步尾部并提升重叠程度。

#pagebreak()
