= 分布式优化器低比特量化方法

== 引言

在分布式深度学习训练中，梯度通信开销已成为制约训练效率的关键瓶颈。传统的 AllReduce 操作需要在各个计算节点间传输完整的 32 位浮点梯度，在大规模模型和多节点环境下，通信时间往往占据训练过程的主要部分。为降低通信开销，本文引入了 *统一 k-bit 随机舍入量化方法*，将梯度压缩至可配置的低比特表示（$b in {1, 2, 4, 8, 16}$），可在压缩率与收敛稳定性之间进行灵活折中。

=== 与云边端协同的关联

云边端协同训练的一个典型形态是：端侧设备持续产生数据，边缘侧承担就近训练/聚合或作为训练入口，云侧提供大规模算力与全局同步能力。在该场景中，训练通信呈现明显的“分层链路”特征：边缘/云内链路可使用较高带宽的局域互联，而端到边、边到云往往受限于接入网与广域网，表现为更低带宽、更高 RTT 以及更大的抖动。

在这种分层拓扑下，梯度同步的瓶颈常由跨域链路主导，尤其当端侧或边缘侧参与数据并行（DP）时，同步梯度需要跨“端→边”或“边→云”链路传输。低比特量化的价值因此不仅体现在跨数据中心场景，也天然适用于云边端协同：它以最小的语义改动降低跨域链路上的通信体积，从而直接缓解接入/广域链路的带宽瓶颈，并降低通信排队与尾部等待对端到端时延的放大效应。

=== 分布式训练中的通信瓶颈

随着深度学习模型规模的不断增长，单机训练已无法满足大规模模型的需求。以 GPT-3 为例，其参数量达到 1750 亿，模型大小超过 350GB，单次前向传播就需要数百 GB 的显存。这使得分布式训练成为必然选择。然而，在分布式训练过程中，梯度同步通信成为了新的性能瓶颈。

在标准的数据并行训练模式下，每个 worker 节点独立完成前向和反向传播，然后通过 AllReduce 操作同步梯度。对于包含 $N$ 个参数的模型，每次迭代需要传输的数据量为：

$ "Communication Volume" = N times "sizeof"("float32") = N times 4 "bytes" $

以 ResNet-50（约 2560 万参数）为例，每次迭代需要传输约 97.7 MiB 数据。而对于 GPT-3 规模的模型，单次通信量约为 651.9 GiB（约 667,572 MiB），在带宽受限的跨数据中心场景下，通信时间可能占据总训练时间的 80% 以上。

#figure(
  image("../../image/ch3-comm-bottleneck.svg", width: 96%),
  caption: [大规模分布式训练中的通信瓶颈]
) <fig:comm-bottleneck>

=== 现有梯度压缩方法的局限性

为降低通信开销，学术界和工业界已经提出了多类梯度压缩方法，但在跨域受限链路场景中仍存在明显短板。以梯度稀疏化为例，尽管 Top-k 或阈值筛选能够在数值上显著压缩传输体积，但额外的稀疏索引编码与传输会引入新的开销，并在低带宽、高 RTT 环境中放大同步尾部，导致收敛效率下降。低精度量化路径则具有更好的工程可用性，FP16 在工业训练中已较成熟，但压缩收益通常只有 2 倍；INT8 虽可进一步提升到约 4 倍，却对量化尺度、裁剪区间和反量化策略更敏感，稍有不当就可能损伤收敛稳定性。固定阈值量化方法在实现上最为直接，但由于缺乏对层间梯度尺度差异和训练阶段动态变化的自适应机制，往往难以同时满足高压缩率与稳定优化这两个目标。

相比之下，本文采用的统一 k-bit 量化方法结合了 Adam 风格的动量归一化与随机舍入策略，能够在保证收敛性的同时支持多档压缩精度：1-bit 强压缩、2/4-bit 均衡压缩、8/16-bit 低扰动压缩。

== 问题分析与研究思路

=== 算法核心思想

统一 k-bit 量化方法借鉴了 Adam 优化器的动量机制，通过维护梯度的一阶矩估计 $m$ 和二阶矩估计 $v$，将连续梯度映射到 $2^b$ 个离散量化级别。该方法并非简单的线性截断，而是先利用指数移动平均保留历史梯度统计，再通过 $m$ 与 $v$ 的比值进行自适应归一化，使不同层、不同阶段的梯度都能被映射到统一数值区间；在此基础上，采用相邻量化级间的随机舍入来维持无偏性，并将本轮量化残差注入后续迭代，形成可持续的误差补偿通道。由此，算法在“压缩强度”与“优化稳定性”之间建立了更稳健的平衡关系。

#figure(
  image("../../image/ch3-kbit-quantization-workflow.png", width: 96%),
  caption: [统一 k-bit 随机舍入量化核心工作流程]
) <fig:onebit-adam-workflow>

=== 数学表达

设第 $t$ 次迭代的梯度为 $g_t$，量化比特宽度为 $b$，量化过程可表示为：

$ m_t &= beta dot m_(t-1) + (1-beta) dot g_t \
v_t &= beta dot v_(t-1) + (1-beta) dot abs(g_t) \
u_t &= "clip"(1/2 (m_t / (v_t + epsilon) + 1), 0, 1) \
s &= 2^b - 1 \
y_t &= s times u_t \
l_t &= floor(y_t), quad alpha_t = y_t - l_t \
z_t &tilde "Bernoulli"(alpha_t), quad q_t = l_t + z_t
$

其中，$beta$ 表示动量系数（默认取 0.999），$epsilon$ 表示数值稳定项（默认取 $10^(-8)$），$b in {1, 2, 4, 8, 16}$ 表示量化位宽，而 $q_t in {0, 1, ..., 2^b-1}$ 则对应离散化后的量化索引。

随机舍入后的归一化估计值为 $hat(u)_t = q_t / s$，其对应的对称区间表示为 $hat(g)_t = 2 hat(u)_t - 1$。由于 $E[z_t] = alpha_t$，可得 $E[hat(u)_t] = u_t$，从而保证量化无偏性。

== 详细方案设计与实现

=== 量化过程

梯度量化算法的核心流程如@algo:quantization 所示。该算法接收待量化的梯度张量列表作为输入，输出压缩后的 k-bit 张量列表。

#figure(
  table(
    columns: (100%,),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*算法 1：统一 k-bit 随机舍入梯度量化算法*]),
    table.hline(stroke: 0.5pt),
    [
      *输入：* 梯度张量列表 $G = {g_1, g_2, ..., g_n}$，位宽 $b$，动量系数 $beta$，误差补偿标志 $"comp_flag"$ \
      *输出：* 压缩张量列表 $C = {c_1, c_2, ..., c_n}$
    ],
    table.hline(stroke: 0.5pt),
    [
      #set par(leading: 0.65em)
      #v(0.3em)
      #h(-2em) *1:*  *for* 每个梯度张量 $g_i in G$ *do* \
      *2:*  #h(1.5em) *if* 动量状态未初始化 *then* \
      *3:*  #h(3em) $m_i <- g_i times (1 - beta)$ #h(3em) ▷ 初始化一阶矩 \
      *4:*  #h(3em) $v_i <- abs(g_i) times (1 - beta)$ #h(3em) ▷ 初始化二阶矩 \
      *5:*  #h(1.5em) *else* \
      *6:*  #h(3em) $m_i <- beta times m_i + (1-beta) times g_i$ #h(3em) ▷ 更新一阶矩 \
      *7:*  #h(3em) $v_i <- beta times v_i + (1-beta) times abs(g_i)$ #h(3em) ▷ 更新二阶矩 \
      *8:*  #h(1.5em) *end if* \
      *9:*  #h(1.5em) $u_i <- "clip"((m_i \/ (v_i + epsilon) + 1) \/ 2, 0, 1)$ #h(3em) ▷ 归一化到 $[0, 1]$ \
      *10:* #h(1.5em) *if* $"comp_flag" = "True"$ *then* \
      *11:* #h(3em) $u_i <- "clip"(u_i + delta_i, 0, 1)$ #h(3em) ▷ 误差补偿 \
      *12:* #h(1.5em) *end if* \
      *13:* #h(1.5em) $s <- 2^b - 1; y_i <- s times u_i; l_i <- floor(y_i)$ #h(3em) ▷ 定位相邻量化级 \
      *14:* #h(1.5em) $alpha_i <- y_i - l_i; z_i tilde "Bernoulli"(alpha_i)$ #h(3em) ▷ 随机舍入 \
      *15:* #h(1.5em) $q_i <- l_i + z_i$ #h(3em) ▷ 得到 k-bit 量化索引 \
      *16:* #h(1.5em) $delta_i <- delta_i + (u_i - q_i \/ s)$ #h(3em) ▷ 更新误差累积项 \
      *17:* #h(1.5em) $c_i <- "PackBits"(q_i, b)$ #h(3em) ▷ 按位宽打包压缩 \
      *18:* *end for* \
      *19:* *return* $C$
      #v(0.3em)
    ],
    table.hline(),
  ),
  caption: [统一 k-bit 随机舍入梯度量化算法流程]
) <algo:quantization>

#h(0em) 从实现逻辑看，算法首先在第 2-8 行完成一阶矩与二阶矩的动量维护，以指数移动平均方式持续吸收历史梯度统计；随后在第 9 行通过二者比值进行自适应归一化，将不同尺度的梯度映射到统一的 $[0, 1]$ 区间。若启用补偿机制，第 10-12 行会把历史量化残差 $delta$ 回注到当前归一化值并进行截断约束，从而抑制误差累积。第 13-15 行执行基于伯努利采样的随机舍入，得到离散索引 $q_i in {0, 1, ..., 2^b-1}$，该过程保障了量化的统计无偏性；最后在第 17 行按位宽进行紧凑打包，减少通信与存储开销。

=== 位打包优化

为进一步减少内存占用，本实现采用通用位打包技术，将量化索引按比特流连续存储。对于位宽 $b$，每个量化值占用 $b$ bit，原始 float32 梯度可获得理论压缩比 $32 / b$。

具体实现时，系统首先执行填充对齐：当总 bit 数不是 8 的整数倍时，在末尾补零到完整字节边界，以保证后续字节级读写的正确性。随后构造位权向量 $bold(w)$，用于将 bit 序列映射到字节位置。

$ bold(w) = [2^7, 2^6, 2^5, 2^4, 2^3, 2^2, 2^1, 2^0] = [128, 64, 32, 16, 8, 4, 2, 1] $

#figure(
  image("../../image/ch3-bit-packing.png", width: 98%),
  caption: [位打包与解包过程示意图]
) <fig:bit-packing>

在此基础上，量化索引会被展开为 bit 流并按字节分组，与位掩码逐元素运算后得到 uint8 表示：

$ "PackedByte" = sum_(i=0)^7 "bit"_i times 2^(7-i), quad "bit"_i in {0, 1} $

该方法适用于不同位宽。若忽略对齐与元数据开销，通信量相对 float32 的理论压缩比分别为：1-bit 为 $32 times$，2-bit 为 $16 times$，4-bit 为 $8 times$，8-bit 为 $4 times$，16-bit 为 $2 times$。

因此，该方法可在强压缩（1/2-bit）与高保真（8/16-bit）之间灵活切换，满足不同网络条件下的训练需求。

=== 反量化与聚合

在接收端，各节点需要将来自所有 worker 的压缩梯度解包、聚合并恢复到可用于优化器更新的梯度空间。首先，压缩字节会被恢复为离散索引张量 $q in {0, 1, ..., 2^b-1}$；实现上通过位运算完成高效解包，即将每个 uint8 字节展开为 8 位矩阵，再与位掩码 $bold(w) = [128, 64, 32, 16, 8, 4, 2, 1]$ 做按位与，并按位宽 $b$ 重组成量化索引序列。该过程可写为：

$ q = "UnpackBits"("PackedBytes", b), quad q in {0, 1, ..., 2^b-1}^N $

完成解包后，系统收集来自所有 $W$ 个 worker 的量化索引并执行逐元素累加：

$ Q_"sum" = sum_(w=1)^W q^((w)) $

其中 $q^((w)) in {0, 1, ..., 2^b-1}^N$ 表示第 $w$ 个节点的量化索引，$Q_"sum"$ 则对应参数维度上的离散级别和。随后进入反归一化阶段，将聚合后的离散值映射回原始梯度空间。设 $s = 2^b - 1$，其逆映射可写为：

$ g_"recovered" = 2 times ((Q_"sum" \/ W) \/ s) - 1 $

该公式将离散索引均值线性映射回 $[-1, 1]$ 区间，从而恢复梯度的符号与相对尺度信息。最终得到的 $g_"recovered"$ 可作为梯度的无偏估计参与参数更新。

=== 集成到分布式训练框架

统一 k-bit 量化方法已无缝集成到分布式训练流程中，替代了传统的 AllReduce 通信模式。整体架构设计遵循模块化原则，主要包含以下组件：

==== 训练循环集成

在模型的前向传播和反向传播完成后，梯度同步阶段根据配置选择是否启用量化压缩，如@algo:training-loop 所示：

#figure(
  table(
    columns: (100%,),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*算法 2：集成量化的分布式训练循环*]),
    table.hline(stroke: 0.5pt),
    [
      #set par(leading: 0.65em)
      #v(0.3em)
      #h(-2em) *1:*  *for* 每个训练批次 *do* \
      *2:*  #h(1.5em) 前向传播：$y = f(x; theta)$ \
      *3:*  #h(1.5em) 计算损失：$cal(L) = "Loss"(y, y_"true")$ \
      *4:*  #h(1.5em) 反向传播：$gradient <- nabla_theta cal(L)$ \
      *5:*  #h(1.5em) *if* 启用量化压缩 *then* \
      *6:*  #h(3em) $gradient <- "QuantizedAdamReduce"(gradient, b)$ \
      *7:*  #h(1.5em) *else* \
      *8:*  #h(3em) $"AllReduce"(gradient)$ \
      *9:*  #h(1.5em) *end if* \
      *10:* #h(1.5em) 参数更新：$theta <- theta - eta times gradient$ \
      *11:* *end for*
      #v(0.3em)
    ],
    table.hline(),
  ),
  caption: [集成统一 k-bit 量化的分布式训练流程]
) <algo:training-loop>

==== 量化通信流程

QuantizedAdamReduce 函数采用分支通信策略：当 $b < 16$ 时使用 A2A 路径完成低比特压缩通信；当 $b = 16$ 时不走压缩聚合路径，直接执行 AllReduce。其内部实现如@algo:reduce 所示：

#figure(
  table(
    columns: (100%,),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*算法 3：QuantizedAdamReduce 通信流程*]),
    table.hline(stroke: 0.5pt),
    [
      *输入：* 梯度张量列表 $G$，位宽 $b$，全局进程组 $"WorldGroup"$ \
      *输出：* 聚合后的梯度张量列表 $G'$
    ],
    table.hline(stroke: 0.5pt),
    [
      #set par(leading: 0.65em)
      #v(0.3em)
      #h(-2em) *1:* *if* $b < 16$ *then* \
      *2:*  #h(1.5em) 初始化量化器：$Q <- "KBitAdamQuantizer"(b)$ \
      *3:*  #h(1.5em) 量化梯度：$C <- Q."quantize"(G)$ \
      *4:*  #h(1.5em) *for* 每个压缩张量 $c_i in C$ *do* \
      *5:*  #h(3em) 分配 A2A 接收缓冲区：$"RecvBuf" <- "allocate_a2a"(|c_i|)$ \
      *6:*  #h(3em) A2A 通信：$"AllToAll"("RecvBuf", c_i, "WorldGroup")$ \
      *7:*  #h(3em) 分片反量化并规约：$g'_i <- Q."dequantize_and_reduce"("RecvBuf")$ \
      *8:*  #h(3em) 更新梯度：$G'[i] <- g'_i$ \
      *9:*  #h(1.5em) *end for* \
      *10:* #h(1.5em) *return* $G'$ \
      *11:* *else* \
      *12:* #h(1.5em) 直接同步：$G' <- "AllReduce"(G, "WorldGroup")$ \
      *13:* #h(1.5em) *return* $G'$ \
      *14:* *end if*
      #v(0.3em)
    ],
    table.hline(),
  ),
  caption: [QuantizedAdamReduce 函数实现流程]
) <algo:reduce>

该设计在工程层面兼顾了透明性与可扩展性。对上层训练代码而言，量化路径几乎是低侵入接入，通常只需要通过配置开关即可启用或关闭；在模块演进层面，量化器作为相对独立的组件存在，便于后续替换其他压缩策略并开展横向对比实验，因此能够较好地支撑后续算法迭代。

#figure(
  image("../../image/ch3-kbit-system-architecture.png", width: 98%),
  caption: [统一 k-bit 随机舍入量化在分布式框架中的集成架构]
) <fig:system-arch>

== 实验与验证

=== 实验设置

针对“对比方法不足、网络条件单一、模型覆盖不够”的问题，本节将实验设计扩展为三条主线：

#h(-2em) *主线 A（方法对比）*：在统一训练脚本与并行配置下，横向比较 FP32 All-Reduce、FP16 All-Reduce、INT8 量化、稀疏化（Top-k）以及本文统一 k-bit 随机舍入量化。

#h(-2em) *主线 B（网络分组）*：覆盖从数据中心内高带宽到云边端跨域低带宽的多组链路条件，显式考察带宽、RTT 与抖动变化对通信尾部与吞吐的影响。

#h(-2em) *主线 C（多模型端到端）*：在视觉、语言和大模型三个层面评估端到端训练收益，避免结论只对单一模型成立。

其中，网络环境分为环境 A（数据中心内高带宽、低 RTT）与环境 B（云边端跨域、受限带宽）两大类。环境 B 采用网络整形（限速、RTT 注入与抖动注入）复现实网约束，用于验证低比特通信在跨域链路上的稳定增益。

// Data source: image/csv/ch3_tab_ced_links.csv
#figure(
  table(
    columns: (auto, auto, auto),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*链路*], [*带宽（示例）*], [*RTT（示例）*]),
    table.hline(stroke: 0.5pt),
    [端→边（接入网）], [10–200 Mb/s], [5–30 ms],
    [边→云（广域网）], [10–30 Gb/s], [30–100 ms],
    [边/云域内（局域互联）], [100–200 Gb/s], [< 1 ms],
    table.hline(),
  ),
  caption: [云边端协同场景的分层链路参数（用于实验设置与仿真）]
) <tab:ced-links>

#figure(
  table(
    columns: (auto, auto, auto),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*对比类别*], [*方法*], [*实现说明*]),
    table.hline(stroke: 0.5pt),
    [无压缩基线], [FP32 All-Reduce], [标准分布式同步路径，作为准确性与稳定性参考],
    [低精度基线], [FP16 All-Reduce], [工业常用低精度同步，压缩比约 2x],
    [量化基线], [INT8 量化同步], [固定 8-bit 量化方案，压缩比约 4x],
    [稀疏化基线], [Top-k + 误差反馈], [稀疏通信代表方案，关注索引开销与尾部效应],
    [本文方法], [统一 k-bit（1/2/4/8/16）], [动量归一化 + 随机舍入 + 位打包],
    table.hline(),
  ),
  caption: [对比实验方法集合与实现口径]
) <tab:method-baselines>

#figure(
  table(
    columns: (auto, auto, auto, auto),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*分组*], [*目标场景*], [*带宽区间*], [*RTT 区间*]),
    table.hline(stroke: 0.5pt),
    [N1], [机房/数据中心内], [100–200 Gb/s], [< 1 ms],
    [N2], [跨可用区或同城跨域], [10–30 Gb/s], [5–30 ms],
    [N3], [边→云广域链路], [1–10 Gb/s], [30–100 ms],
    [N4], [端→边接入受限链路], [10–200 Mb/s], [5–30 ms],
    table.hline(),
  ),
  caption: [多网络条件实验分组]
) <tab:network-groups>

#figure(
  table(
    columns: (auto, auto, auto, auto),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*模型*], [*任务类型*], [*端到端指标*], [*对比重点*]),
    table.hline(stroke: 0.5pt),
    [ResNet-50], [视觉分类], [吞吐指标（items/s）、收敛轮数、目标精度时间], [低比特量化对吞吐与最终精度的影响],
    [BERT-Large], [语言理解], [吞吐指标（items/s）、loss-vs-time、最终验证集指标], [中等规模语言模型的稳定性与速度平衡],
    [LLaMA-7B], [自回归语言建模], [吞吐指标（items/s）、time-to-target loss、端到端训练时长], [大模型场景下通信压缩收益与收敛可行性],
    table.hline(),
  ),
  caption: [多模型端到端实验矩阵]
) <tab:multi-model-e2e>

#h(-2em) *评价指标*：评价维度覆盖系统与优化两侧。系统侧统计每步通信字节数、通信耗时、P95/P99 尾部等待和 GPU 利用率；优化侧统计 loss-vs-step、loss-vs-time、目标精度/目标 loss 到达时间（time-to-target），用于联合评估“是否更快且是否稳定”。本文统一以吞吐指标（items/s）表示吞吐：视觉任务对应 samples/s，语言任务对应 tokens/s。

=== 实验结果与分析

==== 不同网络条件下的量化收益

基于现有数据（@fig:experiments-result），统一 k-bit 方法在带宽下降时表现出更显著的吞吐优势。以 $b=4$ 为例，相对 FP32 的吞吐增益在 10,000 Mb/s、1,000 Mb/s、100 Mb/s 三档带宽下分别约为 $1.28 times$、$1.58 times$ 与 $2.43 times$；这说明在跨域受限链路中，通信压缩能够有效降低同步等待并改善端到端效率。

从收敛侧看，8-bit 与 4-bit 在最终 loss 上与 FP32 接近，而 1-bit 在极低带宽下存在更明显的精度损失，体现出“位宽越低，压缩更强但优化扰动更大”的典型权衡。

从机制上看，上述结果与通信时延分解模型一致。在每步通信时延可写为 $T_"comm" approx T_"startup" + V/{B W}$ 的近似条件下，低比特量化主要作用于 $V/{B W}$ 项：当带宽较高时，$T_"startup"$ 占比上升，压缩对总时延的边际收益有限；当带宽下降时，$V/{B W}$ 项快速放大，压缩收益随之显著上升。因此，同一压缩策略在 N1/N2 与 N3/N4 网络区间下表现出的增益差异并非偶然，而是链路受限程度变化导致的结构性结果。

进一步地，4-bit 在多数网络条件下呈现“收益-稳定性”较优平衡，说明其在本实验设置下能够同时满足两类约束：一方面压缩率足以降低跨域同步等待，另一方面量化噪声未显著破坏优化轨迹。相对地，1-bit 在极低带宽下虽然系统侧收益更高，但会引入更强估计扰动，导致最终 loss 与收敛速度出现偏移。这一现象支持本文在方法选择上的核心结论：跨域受限场景并不总是“压缩越强越好”，而应在通信收益与优化稳定之间做联合选择。

该组实验还揭示了一个工程意义上的阈值效应：当网络从“可接受带宽”进一步退化到“通信主导迭代时间”后，压缩策略对端到端性能的影响会从“次要优化项”转变为“主导优化项”。这意味着在云边端真实部署中，压缩策略不应固定为单一位宽，而应根据链路状态和任务阶段进行策略切换，例如预热阶段采用保守位宽、瓶颈阶段采用激进位宽，以获得更稳定的全程收益。

综合上述分析，本组结果不仅验证了统一 k-bit 在受限网络下可显著改善系统吞吐，也支持了“按链路条件选择位宽”的方法论。结论层面可归纳为：低比特量化在跨域链路受限时具备明确有效性，但其最优配置依赖网络条件与训练稳定性目标，需通过系统侧指标与优化侧指标联合决策。

==== 与其他压缩方法的对比结果

除位宽内部对比外，本章补充采用 @tab:method-baselines 中的方法集合进行横向评估。对比实验统一报告以下三类结果：

#h(-2em) *系统效率*：迭代时间、通信时间占比、P95/P99 尾部等待与 GPU 利用率；

#h(-2em) *收敛质量*：相同 step 下的验证指标、相同 wall-clock 时间下的 loss 下降速度；

#h(-2em) *综合收益*：达到同等目标精度的总训练时长（time-to-target）。

该对比用于评估两个方面：统一 k-bit 相比固定精度量化（FP16/INT8）是否具备更好的“可调压缩-收敛平衡”，以及相较稀疏化方案是否在跨域链路下具有更低的索引开销与更稳定的尾部时延。

#figure(
  image("../../image/ch3-method-comparison.svg", width: 98%),
  caption: [统一 k-bit 与主流压缩方法的端到端性能对比]
) <fig:method-comp>

如@fig:method-comp 所示，在统一训练配置下，统一 k-bit（4-bit）相较 FP32 的吞吐提升约为 $1.35 times$（6216 vs 4602，单位为 items/s），通信时间占比由 41.2% 降至 22.5%，P95 尾部时延由 168.0 ms 降至 96.8 ms。与 FP16、INT8 及 Top-k 基线相比，统一 k-bit 在吞吐与尾部稳定性上均取得更优平衡，说明其在跨域受限链路下更能稳定释放系统吞吐。

从对比结构上看，四类基线方法分别对应不同技术路径：FP16/INT8 属于固定精度量化，Top-k 属于稀疏化压缩，统一 k-bit 则采用“动量归一化 + 随机舍入 + 位打包”的一体化方案。实验结果显示，统一 k-bit 在通信占比与尾部时延两个关键指标上同时占优，说明该方案并非仅在单一指标上“以收敛换速度”或“以速度换稳定”，而是在系统与优化目标之间实现了更可控的折中。

其原因可从两方面解释。第一，固定精度量化（如 FP16/INT8）虽然实现成熟，但压缩率上限受限，在跨域低带宽场景下难以继续压缩尾部；第二，稀疏化方案虽然名义压缩率较高，但索引维护、打包与重组开销会在高 RTT 条件下被放大，且稀疏模式波动会增加尾部抖动。统一 k-bit 通过连续位流打包与无偏随机舍入，降低了索引型开销与偏差积累风险，因此在 P95 尾部时延上更稳定。

从训练稳定性角度看，final loss 的差异幅度处于可控范围，表明系统侧收益并非由显著牺牲优化行为换取。尤其在与 Top-k 路径比较时，统一 k-bit 在尾部时延和最终损失上均表现更稳，支持本文关于“量化路径在跨域场景中更具工程可部署性”的结论。

本组对比还给出一个可落地的实践建议：在跨域链路受限且对收敛质量有约束的训练任务中，优先选择具备误差补偿与可调位宽能力的量化方案；对于极端带宽受限但可容忍收敛扰动的场景，再考虑更激进压缩配置。该建议与本文方法设计目标一致，即在保障训练语义可控的前提下释放通信优化收益。

==== 多模型端到端训练结果

针对 @tab:multi-model-e2e 的三类模型，本章以端到端训练耗时与 time-to-target 作为主指标。实验重点不是单次通信 micro-benchmark，而是完整训练流程中的真实收益，包括前后向计算、梯度同步、优化器更新与数据流水线等环节共同作用后的整体加速比。

该设置有助于降低“仅在单模型或单一网络条件下成立”的结论偏差，并为后续章节的系统级优化提供统一基线。

#figure(
  image("../../image/ch3-multi-model-e2e.svg", width: 98%),
  caption: [统一 k-bit 在多模型端到端训练中的收益]
) <fig:multi-model-e2e>

从@fig:multi-model-e2e 可见，统一 k-bit 在 ResNet-50、BERT-Large 与 LLaMA-7B 三类负载上均带来稳定端到端收益：time-to-target 分别由 128/214/680 min 降至 95/158/492 min；吞吐由 4620/3090/980 提升至 6180/4350/1320（单位为 items/s）。该结果表明，本文方法对不同规模与不同训练形态具有较好的一致性增益。

该组实验的核心价值在于跨任务一致性验证。视觉分类、语言理解与自回归建模在数据访问模式、梯度统计特征与训练稳定性敏感点上存在显著差异；若某通信优化仅对单一任务有效，通常难以在三类任务中同时保持正增益。当前结果显示三类任务的 time-to-target 均明显缩短，说明统一 k-bit 的收益来源主要是“通信路径效率提升”这一跨任务共性，而非某一模型结构的偶然匹配。

进一步比较三类任务的收益幅度可以看到，大模型与长序列任务的绝对节省时间更为显著。这与其更高通信负载和更长训练时间窗口相一致：当通信在总时长中的占比较高时，压缩策略对 wall-clock 的改善更容易累积为可观的总时长缩减。换言之，统一 k-bit 在“通信主导型工作负载”中的边际收益更大，这一趋势与前述网络受限实验中的结论相互印证。

同时，该组结果并未显示任务切换导致的稳定性失效，说明所采用的误差补偿机制在不同任务分布下具备一定鲁棒性。对工程部署而言，这一性质非常关键：它意味着策略可在同一系统中服务多类训练任务，而无需为每类任务完全重写通信路径，降低了运维和调参成本。

需要指出的是，不同任务之间的最优位宽可能并不完全一致。本章给出的统一配置验证了“可行且有效”，但若追求最优收益，仍可在保持统一框架的前提下按任务进行位宽微调。该观察不削弱本文结论，反而进一步说明本文方法的优势在于提供了可调、可扩展的统一通信优化底座。

综合来看，本组实验从端到端层面支撑了本文方法的通用性结论：统一 k-bit 并非仅在单一模型上成立，而是在多任务、多负载条件下均可稳定降低 time-to-target，并保持可接受的优化行为偏差。

#figure(
  image("../../image/ch3-experiment-results.svg", width: 98%),
  caption: [统一 k-bit 量化方法的收敛性与训练加速比验证]
) <fig:experiments-result>

== 本章小结

本章详细介绍了统一 k-bit 随机舍入梯度量化压缩方法。首先，从分布式训练的通信瓶颈出发，阐述了低比特量化的必要性和现有方法的局限性。随后，深入分析了基于动量归一化与随机舍入的统一量化原理，包括动量维护、自适应归一化和误差补偿机制。接着，详细描述了算法的实现细节，涉及按位宽位打包、反量化聚合以及其在分布式训练框架中的集成方案。最后，本章从跨方法对比、多网络条件分组和多模型端到端训练三个维度组织实验分析，展示了该方法在通信受限场景下兼顾吞吐提升与收敛稳定性的潜力。

#pagebreak()