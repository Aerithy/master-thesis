#import "../../template/src/algorithm.typ": pseudocode-list
= 分布式优化器低比特量化方法

== 引言

在分布式深度学习训练中，梯度通信开销已成为制约训练效率的关键瓶颈。传统的 AllReduce 操作需要在各个计算节点间传输完整的 32 位浮点梯度，在大规模模型和多节点环境下，通信时间往往占据训练过程的主要部分 @alistarh2017qsgd。为降低通信开销，本文引入了 *k-bit随机舍入量化方法*，将梯度压缩至可配置的低比特表示（$b in {1, 2, 4, 8, 16}$），可灵活调整压缩率，使其与收敛稳定性之间进行折中；其次，引入误差补偿机制与聚合算法来维持模型的优化稳定性，缓解由于量化带来的累计噪声并且实现量化后数据在各节点间的高效聚合。

=== 与云边端协同的关联

云边端协同训练的一个典型形态是：端侧设备持续产生数据，边缘侧承担就近训练/聚合或作为训练入口，云侧提供大规模算力与全局同步能力。在该场景中，训练通信呈现明显的“分层链路”特征：边缘/云内链路可使用较高带宽的局域互联，而端到边、边到云往往受限于接入网与广域网，表现为更低带宽、更高 RTT 以及更大的抖动。

在这种分层拓扑下，梯度同步的瓶颈常由跨域链路主导，尤其当端侧或边缘侧参与数据并行（DP）时，同步梯度需要跨“端→边”或“边→云”链路传输。低比特量化的价值因此不仅体现在跨数据中心场景，也天然适用于云边端协同：它以最小的语义改动降低跨域链路上的通信体积，从而直接缓解接入/广域链路的带宽瓶颈，并降低通信排队与尾部等待对端到端时延的放大效应。

=== 分布式训练中的通信瓶颈

随着深度学习模型规模的不断增长，单机训练已无法满足大规模模型的需求。以 GPT-3 为例，其参数量达到 1750 亿，模型大小超过 350GB，单次前向传播就需要数百 GB 的显存 @brown2020gpt3。这使得分布式训练成为必然选择。然而，在分布式训练过程中，梯度同步通信成为了新的性能瓶颈。

在标准的数据并行训练模式下，每个 worker 节点独立完成前向和反向传播，然后通过 AllReduce 操作同步梯度。对于包含 $N$ 个参数的模型，每次迭代需要传输的数据量为：

$ "Communication Volume" = N times "sizeof"("float32") = N times 4 "bytes" $

以 ResNet-50（约 2560 万参数）为例，每次迭代需要传输约 97.7 MiB 数据。而对于 GPT-3 规模的模型，单次通信量约为 651.9 GiB（约 667,572 MiB），在带宽受限的跨数据中心场景下，通信时间可能占据总训练时间的 80% 以上。

#figure(
  image("../../image/ch3-comm-bottleneck.svg", width: 96%),
  caption: [大规模分布式训练中的通信瓶颈]
) <fig:comm-bottleneck>

=== 现有梯度压缩方法的局限性

为降低通信开销，学术界和工业界已经提出了多类梯度压缩方法，但在跨域受限链路场景中仍存在明显短板。以梯度稀疏化为例，尽管 Top-k 或阈值筛选能够在数值上显著压缩传输体积，但额外的稀疏索引编码与传输会引入新的开销，并在低带宽、高 RTT 环境中放大同步尾部，导致收敛效率下降 @aji2017sparse。低精度量化路径则具有更好的工程可用性，FP16 与 BF16 涉及的混合精度训练方法在工业训练中已较成熟，但压缩收益通常只有 2 倍，且其目的通常是为了提升模型本身的计算速度而非提升通信效率；INT8 虽可进一步提升到约 4 倍，却对量化尺度、裁剪区间和反量化策略更敏感，若参数配置不当就可能损伤收敛稳定性 @alistarh2017qsgd。固定阈值量化方法在实现上最为直接，但由于缺乏对层间梯度尺度差异和训练阶段动态变化的自适应机制，往往难以同时满足高压缩率与稳定优化这两个目标 @wen2017terngrad。

相比之下，本文采用的 k-bit 量化方法结合了 Adam 风格的动量归一化与随机舍入策略，能够在保证收敛性的同时支持多档压缩精度：1-bit 强压缩、2/4-bit 均衡压缩、8/16-bit 低扰动压缩 @gupta2015deep。

== 问题分析与研究思路

=== 跨域训练中梯度通信的困境

在分布式训练中，梯度同步是数据并行的核心操作，但跨域环境下的通信特征与单机房训练有本质区别。云边端协同训练中，端侧和边缘侧节点通过"端→边"或"边→云"的跨域链路进行梯度交换，这些链路普遍表现为低带宽、高 RTT、高波动性。

具体而言，跨域梯度通信面临三重困境：

第一，通信数据量庞大且固定。以 ResNet-50（约 2560 万参数）为例，每次迭代需传输约 97.7 MiB 数据；GPT-3 规模模型的单步梯度同步量达 600+ GiB。在低带宽链路下（如端→边接入网 10-200 Mb/s），即使单次通信也可能消耗数十秒甚至更长；而在高带宽局域网中（100+ Gb/s）仅需毫秒级。这导致跨域链路成为明显的计算-通信不对称瓶颈：计算可在本地完成，但通信必须经过带宽受限的跨域路径，造成迭代末端的同步阻塞。第二，现有量化方案的局限性各异。固定精度量化（FP16/INT8）虽然工程成熟，但压缩率上限固定（通常 2-4 倍），无法应对极端低带宽场景；稀疏化方案（Top-k）虽然压缩率更高，但需维护索引、编码、解包等额外开销，并且大多数的Top-k方案不支持高效的规约方法。在高 RTT 条件下这些开销会被显著放大，反而可能增加同步尾部；低秩压缩对梯度矩阵结构敏感，难以在异构网络层间梯度差异大的场景中保持稳定性。更关键的是，现有方法通常采用静态量化参数，不能根据链路条件动态调整压缩强度。第三，量化噪声与收敛稳定性的权衡困难。低比特量化必然引入量化误差，若压缩位宽过低（1-2 bit），量化噪声会显著破坏梯度信息，导致训练末端振荡甚至发散；若位宽过高（16 bit 接近 FP16），压缩收益有限。而不同任务、不同训练阶段对量化容忍度不同：视觉分类相对鲁棒，语言模型对梯度精度更敏感。缺乏误差补偿机制的方案难以平衡这一矛盾。

=== 现有量化方法的不足与启示

综合现有方案，可以发现它们各有所长但均存在明显短板。固定精度量化提供了稳定性，但在极限压缩场景下效果递减；稀疏化提供了高理论压缩率，但工程复杂度高且在 WAN 中开销反而放大；低秩方法在某些模型结构上高效，但通用性受限；现有方法工程上缺乏自适应调整压缩策略的机制来在网络状态变化时动态调整策略；此外，大多数方法仅关注压缩算法优化，缺乏对压缩算法和通信算法的协同设计。这为跨域分布式训练通信压缩策略提供了一个明确的研究方向。设计一种可调、低开销的量化方案，能够在不同位宽之间灵活切换来适应云边端环境下不同的网络链路条件，并通过结合完善的误差补偿机制来保证收敛的稳定性，同时实现专用于低位宽的集合通信算法来满足量化后的数据的高效传输，从而实现在云边端环境下对通信量化策略进行高效的端到端部署。

=== 本文核心研究问题

基于上述分析，本章聚焦于以下核心问题：

（1）如何设计一种可调、低开销的梯度量化方案，能在不同位宽（1-16 bit）之间灵活切换，使系统能根据链路条件自主选择最优压缩强度，而不是被动采用固定参数。

（2）如何通过误差补偿保证收敛稳定性，使得低比特量化不会过度破坏梯度信息，即使在 1-4 bit 的强压缩条件下也能维持可接受的优化轨迹。

（3）如何最小化量化与打包的工程开销，在不侵入现有分布式训练框架的前提下，实现高效的端到端部署。

=== 算法核心思想与解决方案

针对上述问题，本文提出的分布式低比特量化方法（后简称为 k-bit 量化方法）借鉴了 Adam 优化器的动量机制，通过维护梯度的一阶矩估计 $m$ 与二阶矩估计 $v$，将连续的浮点梯度映射至 $2^b$ 个离散量化级别。该方法首先利用指数移动平均（EMA）平滑历史梯度的统计特性，随后通过一阶矩与二阶矩的比值实现自适应归一化，这一设计使得训练过程中不同网络层以及不同迭代周期的梯度均能被动态映射至统一的数值区间，适应梯度尺度的变化。在此基础上，本方法采用相邻量化级别间的随机舍入（Stochastic Rounding）以确保量化分布的无偏性，并将当前迭代产生的局部量化残差保存，作为补偿项注入后续迭代过程。这一误差补偿通道有效抑制了低比特截断带来的累积量化噪声，使得算法在降低传输开销与维持模型优化稳定性之间构建了可控的平衡。此外，本方法通过重新设计面向低比特的集合通信，使得量化数据得以高效的进行节点间规约

k-bit 量化的整体工作流程如@fig:kbit-adam-workflow 所示，包括动量维护、自适应归一化、随机舍入与残差补偿四个关键步骤。这一流程在每次迭代中循环执行，确保量化误差被逐步补偿而不会累积。

=== 数学表达

设第 $t$ 次迭代的梯度为 $g_t$，量化比特宽度为 $b$，量化过程可表示为：

$ m_t &= beta dot m_(t-1) + (1-beta) dot g_t \
v_t &= beta dot v_(t-1) + (1-beta) dot abs(g_t) \
u_t &= "clip"(1/2 (m_t / (v_t + epsilon) + 1), 0, 1) \
s &= 2^b - 1 \
y_t &= s times u_t \
l_t &= floor(y_t), quad alpha_t = y_t - l_t \
z_t &tilde "Bernoulli"(alpha_t), quad q_t = l_t + z_t
$

其中，$beta$ 表示动量系数（默认取 0.999），$epsilon$ 表示数值稳定项（默认取 $10^(-8)$），$b in {1, 2, 4, 8, 16}$ 表示量化位宽，而 $q_t in {0, 1, ..., 2^b-1}$ 则对应离散化后的量化索引。

随机舍入后的归一化估计值为 $hat(u)_t = q_t / s$，其对应的对称区间表示为 $hat(g)_t = 2 hat(u)_t - 1$。由于 $E[z_t] = alpha_t$，可得 $E[hat(u)_t] = u_t$，从而保证量化无偏性。

== 详细方案设计与实现

#figure(
  image("../../image/ch3-kbit-quantization-workflow.svg", width: 96%),
  caption: [k-bit随机舍入量化核心工作流程]
) <fig:kbit-adam-workflow>

=== 量化过程

梯度量化算法的核心流程如@algo:quantization 所示。该算法接收待量化的梯度张量列表作为输入，输出压缩后的 k-bit 张量列表。

#figure(
  kind: "algorithm",
  placement: top,
  pseudocode-list(booktabs: true, numbered-title: [k-bit随机舍入梯度量化算法], full: true)[
    - *输入：梯度张量列表$G = {g_1, g_2, ..., g_n}$，位宽$b$，动量系数$beta$，误差补偿标志$"comp_flag"$*
    - *输出：压缩张量列表$C = {c_1, c_2, ..., c_n}$*

    + *for* 每个梯度张量$g_i in G$ *do*
      + *if* 动量状态未初始化 *then*
        + $m_i <- g_i times (1 - beta)$
        + $v_i <- abs(g_i) times (1 - beta)$
      + *else*
        + $m_i <- beta times m_i + (1-beta) times g_i$
        + $v_i <- beta times v_i + (1-beta) times abs(g_i)$
      + *end if*
      + $u_i <- "clip"((m_i \/ (v_i + epsilon) + 1) \/ 2, 0, 1)$
      + *if* $"comp_flag" = "True"$ *then*
        + $u_i <- "clip"(u_i + delta_i, 0, 1)$
      + *end if*
      + $s <- 2^b - 1; y_i <- s times u_i; l_i <- floor(y_i)$
      + $alpha_i <- y_i - l_i; z_i tilde "Bernoulli"(alpha_i)$
      + $q_i <- l_i + z_i$
      + $delta_i <- delta_i + (u_i - q_i \/ s)$
      + $c_i <- "PackBits"(q_i, b)$
    + *end for*
    + *return* $C$
  ]
) <algo:quantization>

#h(0em) 从实现逻辑看，算法首先在第 2-8 行完成一阶矩与二阶矩的动量维护，以指数移动平均方式持续吸收历史梯度统计；随后在第 9 行通过二者比值进行自适应归一化，将不同尺度的梯度映射到统一的 $[0, 1]$ 区间。若启用补偿机制，第 10-12 行会把历史量化残差 $delta$ 回注到当前归一化值并进行截断约束，从而抑制误差累积。第 13-15 行执行基于伯努利采样的随机舍入，得到离散索引 $q_i in {0, 1, ..., 2^b-1}$，该过程保障了量化的统计无偏性；最后在第 17 行按位宽进行紧凑打包，减少通信与存储开销。

=== 位打包优化

为进一步减少内存占用，本实现采用通用位打包技术，将量化索引按比特流连续存储。对于位宽 $b$，每个量化值占用 $b$ bit，原始 float32 梯度可获得理论压缩比 $32 / b$ @seide20141bit。

具体实现时，系统首先执行填充对齐：当总 bit 数不是 8 的整数倍时，在末尾补零到完整字节边界，以保证后续字节级读写的正确性。随后构造位权向量 $bold(w)$，用于将 bit 序列映射到字节位置。

$ bold(w) = [2^7, 2^6, 2^5, 2^4, 2^3, 2^2, 2^1, 2^0] = [128, 64, 32, 16, 8, 4, 2, 1] $

位打包与解包过程如@fig:bit-packing 所示。#figure(
  image("../../image/ch3-bit-packing.svg", width: 98%),
  caption: [位打包与解包过程示意图]
) <fig:bit-packing>

在此基础上，量化索引会被展开为 bit 流并按字节分组，与位掩码逐元素运算后得到 uint8 表示：

$ "PackedByte" = sum_(i=0)^7 "bit"_i times 2^(7-i), quad "bit"_i in {0, 1} $

该方法适用于不同位宽。若忽略对齐与元数据开销，通信量相对 float32 的理论压缩比分别为：1-bit 为 $32 times$，2-bit 为 $16 times$，4-bit 为 $8 times$，8-bit 为 $4 times$，16-bit 为 $2 times$。

因此，该方法可在强压缩（1/2-bit）与高保真（8/16-bit）之间灵活切换，满足不同网络条件下的训练需求。

=== 反量化与聚合

在接收端，各节点需要将来自所有 worker 的压缩梯度解包、聚合并恢复到可用于优化器更新的梯度空间。首先，压缩字节会被恢复为离散索引张量 $q in {0, 1, ..., 2^b-1}$；实现上通过位运算完成高效解包，即将每个 uint8 字节展开为 8 位矩阵，再与位掩码 $bold(w) = [128, 64, 32, 16, 8, 4, 2, 1]$ 做按位与，并按位宽 $b$ 重组成量化索引序列。该过程可写为：

$ q = "UnpackBits"("PackedBytes", b), quad q in {0, 1, ..., 2^b-1}^N $

完成解包后，系统收集来自所有 $W$ 个 worker 的量化索引并执行逐元素累加：

$ Q_"sum" = sum_(w=1)^W q^((w)) $

其中 $q^((w)) in {0, 1, ..., 2^b-1}^N$ 表示第 $w$ 个节点的量化索引，$Q_"sum"$ 则对应参数维度上的离散级别和。随后进入反归一化阶段，将聚合后的离散值映射回原始梯度空间。设 $s = 2^b - 1$，其逆映射可写为：

$ g_"recovered" = 2 times ((Q_"sum" \/ W) \/ s) - 1 $

该公式将离散索引均值线性映射回 $[-1, 1]$ 区间，从而恢复梯度的符号与相对尺度信息。最终得到的 $g_"recovered"$ 可作为梯度的无偏估计参与参数更新。

=== 集成到分布式训练框架

k-bit 量化方法已无缝集成到分布式训练流程中，替代了传统的 AllReduce 通信模式。整体架构设计遵循模块化原则，主要包含以下组件：

#figure(
  kind: "algorithm",
  // placement: top,
  pseudocode-list(booktabs: true, numbered-title: [集成量化的分布式训练循环], full: true)[
    + *for* 每个训练批次 *do*
      + 前向传播：$y = f(x; theta)$
      + 计算损失：$cal(L) = "Loss"(y, y_"true")$
      + 反向传播：$gradient <- nabla_theta cal(L)$
      + *if* 启用量化压缩 *then*
        + $gradient <- "QuantizedAdamReduce"(gradient, b)$
      + *else*
        + $"AllReduce"(gradient)$
      + *end if*
      + 参数更新：$theta <- theta - eta times gradient$
    + *end for*
  ]
) <algo:training-loop>

==== 训练循环集成

在模型的前向传播和反向传播完成后，梯度同步阶段根据配置选择是否启用量化压缩，如@algo:training-loop 所示：

#figure(
  kind: "algorithm",
  // placement: top,
  pseudocode-list(booktabs: true, numbered-title: [通信流程], full: true)[
    - *输入：梯度张量列表$G$，位宽$b$，全局进程组$"WorldGroup"$*
    - *输出：聚合后的梯度张量列表$G'$*

    + *if* $b < 16$ *then*
      + 初始化量化器：$Q <- "KBitAdamQuantizer"(b)$
      + 量化梯度：$C <- Q."quantize"(G)$
      + *for* 每个压缩张量$c_i in C$ *do*
        + 分配 A2A 接收缓冲区：$"RecvBuf" <- "allocate_a2a"(|c_i|)$
        + A2A 通信：$"AllToAll"("RecvBuf", c_i, "WorldGroup")$
        + 分片反量化并规约：$g'_i <- Q."dequantize_and_reduce"("RecvBuf")$
        + 更新梯度：$G'[i] <- g'_i$
      + *end for*
      + *return* $G'$
    + *else*
      + 直接同步：$G' <- "AllReduce"(G, "WorldGroup")$
      + *return* $G'$
    + *end if*
  ]
) <algo:reduce>

==== 量化通信流程

通信流程采用分支通信策略：当 $b < 16$ 时使用 A2A（All-to-All，一种全局交换通信模式）路径完成低比特压缩通信；当 $b = 16$ 时不通过压缩聚合路径，直接执行标准的全量归约（AllReduce）。其内部实现如@algo:reduce 所示：

该设计在工程层面兼顾了透明性与可扩展性。对上层训练代码而言，量化路径几乎是低侵入接入，通常只需要通过配置开关即可启用或关闭；在模块演进层面，量化器作为相对独立的组件存在，便于后续替换其他压缩策略并开展横向对比实验，因此能够较好地支撑后续算法迭代 @pytorch_distributed2020。

// #figure(
//   image("../../image/ch3-kbit-system-architecture.svg", width: 100%),
//   caption: [k-bit随机舍入量化在分布式框架中的集成架构]
// ) <fig:system-arch>

== 实验与验证

=== 实验设置

本节将实验设计扩展为三条主线：

*主线 A（方法对比）*：在统一训练脚本与并行配置下，横向比较 FP32 All-Reduce、FP16 All-Reduce、INT8 量化、稀疏化（Top-k）以及本文 k-bit随机舍入量化。

*主线 B（网络分组）*：覆盖从数据中心内高带宽到云边端跨域低带宽的多组链路条件，显式考察带宽、RTT 与抖动变化对通信尾部与吞吐的影响。

*主线 C（多模型端到端）*：在视觉、语言模型三个层面评估端到端训练收益，避免结论只对单一模型成立。

其中，网络环境分为环境 A（数据中心内高带宽、低 RTT）与环境 B（云边端跨域、受限带宽）两大类。环境 B 采用网络整形（限速、RTT 注入与抖动注入）复现实网约束，用于验证低比特通信在跨域链路上的稳定增益。

为保证不同任务之间的结果可比较，本章实验在统一训练脚本、通信后端与日志统计口径下完成。除特别说明外，系统侧统一记录通信字节数、通信时间、尾部等待与吞吐，优化侧统一记录 step 对齐和 wall-clock 对齐的训练曲线；跨域链路条件均通过网络整形方式注入，并在同一组基线方法下比较。这样可以避免把模型差异、网络差异和通信策略差异混在一起解释，使后续多模型结果能够对应到相同的实验口径。

本章实验涉及两类硬件环境。实验环境一为 NVIDIA A100 云上双节点集群，节点内 GPU 通过 PCIe 全互联，网卡使用 IB 网口；节点内 PCIe 有效带宽约为 126 Gb/s，节点间互联带宽约为 50 Gb/s。实验环境二为天数智芯（Iluvatar CoreX）国产 GPU 物理集群，节点 n210 对应 Domain-1，节点 n62 对应 Domain-2；每节点 CPU 为 Intel Xeon Gold 6330，GPU 为 Iluvatar BI-V150 x 8，板卡内部各包含 2 个 GPU 芯粒。该环境中 GPU 两两成对走板内集成互联路径，每对 GPU 之间经 PCIe Switch 互联且不跨 NUMA，所有 GPU 位于同一 NUMA，NIC0 挂载在另一 NUMA 节点上；节点间通过 Ethernet 互联，节点内全 PCIe 互联带宽约为 126 Gb/s，节点间互联带宽约为 12.5 Gb/s。

#figure(
  table(
    columns: (0.9fr, 1.4fr, 1.6fr, 1.2fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*环境*], [*计算节点与 GPU*], [*节点内互联*], [*节点间互联*]),
    table.hline(stroke: 0.5pt),
    [实验环境一], [2 节点 NVIDIA A100 云集群], [PCIe 全互联，约 126 Gb/s], [IB 网口，约 50 Gb/s],
    [实验环境二], [n210/n62 双节点 Iluvatar BI-V150 x 8], [同 NUMA PCIe，板内芯粒成对互联，约 126 Gb/s], [Ethernet，约 12.5 Gb/s],
    table.hline(),
  ),
  caption: [第三章实验硬件环境]
) <tab:ch3-exp-envs>

// Data source: image/csv/ch3_tab_ced_links.csv
#figure(
  table(
    columns: (1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*链路*], [*带宽（示例）*], [*RTT（示例）*]),
    table.hline(stroke: 0.5pt),
    [端→边（接入网）], [10–200 Mb/s], [5–30 ms],
    [边→云（广域网）], [10–30 Gb/s], [30–100 ms],
    [边/云域内（局域互联）], [100–200 Gb/s], [< 1 ms],
    table.hline(),
  ),
  caption: [云边端协同场景的分层链路参数]
) <tab:ced-links>

#figure(
  table(
    columns: (1fr, 1fr, 2fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*对比类别*], [*方法*], [*实现说明*]),
    table.hline(stroke: 0.5pt),
    [无压缩基线], [FP32 All-Reduce], [标准分布式同步路径，作为准确性与稳定性参考],
    [低精度基线], [FP16 All-Reduce], [工业常用低精度同步，压缩比约 2x],
    [量化基线], [INT8 量化同步], [固定 8-bit 量化方案，压缩比约 4x],
    [稀疏化基线], [Top-k + 误差反馈], [稀疏通信代表方案，关注索引开销与尾部效应],
    [本文方法], [k-bit（1/2/4/8/16）], [动量归一化 + 随机舍入 + 位打包],
    table.hline(),
  ),
  caption: [对比实验方法集合与实现口径]
) <tab:method-baselines>

#figure(
  table(
    columns: (1fr, 1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*分组*], [*目标场景*], [*带宽区间*], [*RTT 区间*]),
    table.hline(stroke: 0.5pt),
    [N1], [机房/数据中心内], [100–200 Gb/s], [< 1 ms],
    [N2], [跨可用区或同城跨域], [10–30 Gb/s], [5–30 ms],
    [N3], [边→云广域链路], [1–10 Gb/s], [30–100 ms],
    [N4], [端→边接入受限链路], [10–200 Mb/s], [5–30 ms],
    table.hline(),
  ),
  caption: [多网络条件实验分组]
) <tab:network-groups>

*评价指标*：评价维度覆盖系统与优化两侧。系统侧统计每步通信字节数、通信耗时、P95/P99 尾部等待和 GPU 利用率；优化侧统计 loss-vs-step、loss-vs-time、目标精度/目标 loss 到达时间（time-to-target），用于联合评估“是否更快且是否稳定”。本文统一以吞吐指标（items/s）表示吞吐：视觉任务对应 samples/s，语言任务对应 tokens/s。

#figure(
  table(
    columns: (0.75fr, 1fr, 2fr, 1.5fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*模型*], [*任务类型*], [*端到端指标*], [*对比重点*]),
    table.hline(stroke: 0.5pt),
    [ResNet-50], [视觉分类], [吞吐指标（items/s）、收敛轮数、目标精度时间], [低比特量化对吞吐与最终精度的影响],
    [BERT-Large], [语言理解], [吞吐指标（items/s）、loss-vs-time、最终验证集指标], [中等规模语言模型的稳定性与速度平衡],
    [LLaMA-7B], [自回归语言建模], [吞吐指标（tokens/s）、目标损失到达时间、端到端训练时长], [大模型场景下通信压缩收益与收敛可行性],
    table.hline(),
  ),
  caption: [多模型端到端实验矩阵]
) <tab:multi-model-e2e>


=== 实验结果与分析

*不同网络条件下的量化性能与收敛性分析*：

基于端到端实验的评估数据，如@fig:experiments-result 所示，k-bit 量化方法在网络带宽逐步受限的场景中表现出明显的吞吐提升趋势。以 $b=4$ 的配置为例，相较于无压缩的 FP32 全精度基线，其在 10,000 Mb/s、1,000 Mb/s 和 100 Mb/s 三档典型通信带宽下，所取得的端到端吞吐加速比分别达到约 $1.28 times$、$1.58 times$ 与 $2.43 times$。这一性能差异表明：在云边端跨域的受限链路环境中，物理传输载荷构成了迭代循环的关键瓶颈，而低比特通信压缩技术能够有效缩减全局同步网络等待时间，从而提升系统的数据处理效率。

从模型优化收敛维度审视，8-bit 与 4-bit 量化配置在最终模型损失（Loss）预测上与全精度 FP32 轨迹相近，未表现出明显的精度退化。然而，1-bit 压缩策略尽管提供了更高的通信效率，却伴随了更为明显的优化精度损耗，并引发了训练末端损失值的抖动偏移现象。该结果刻画了分布式量化通信的权衡关系：更低的量化位宽对应更高的传输效率，但也同时引入更强的随机扰动。

从机制上看，上述结果与通信时延分解模型一致。在每步通信时延可写为 $T_"comm" approx T_"startup" + V/{B W}$ 的近似条件下，低比特量化主要作用于 $V/{B W}$ 项：当带宽较高时，$T_"startup"$ 占比上升，压缩对总时延的边际收益有限；当带宽下降时，$V/{B W}$ 项快速放大，压缩收益随之显著上升。因此，同一压缩策略在 N1/N2 与 N3/N4 网络区间下表现出的增益差异是链路受限程度变化导致的结构性结果。

进一步地，4-bit 在多数网络条件下呈现“收益-稳定性”较优平衡，说明其在本实验设置下能够同时满足两类约束：一方面压缩率足以降低跨域同步等待，另一方面量化噪声未显著破坏优化轨迹。相对地，1-bit 在极低带宽下虽然系统侧收益更高，但会引入更强估计扰动，导致最终 loss 与收敛速度出现偏移。这一现象支持本文在方法选择上的核心结论：跨域受限场景并不总是压缩越强越好，而应在通信收益与优化稳定之间做联合选择。

该组实验还揭示了一个工程意义上的阈值效应：当网络从高带宽退化到低带宽，使得通信主导迭代时间占比上升后，压缩策略对端到端性能的影响会从次要优化项转变为主导优化项。这意味着在云边端真实部署中，压缩策略不应固定为单一位宽，而应根据链路状态和任务阶段进行策略切换，例如预热阶段采用保守位宽、瓶颈阶段采用激进位宽，以获得更稳定的全程收益。

综合上述分析，本组结果不仅验证了 k-bit 在受限网络下可显著改善系统吞吐，也支撑了按链路条件选择位宽的方法。结论层面可归纳为：低比特量化在跨域链路受限时具备明确有效性，但其最优配置依赖网络条件与训练稳定性目标，需通过系统侧指标与优化侧指标联合决策。

*与其他压缩方法的对比结果*：除位宽内部对比外，本章补充采用 @tab:method-baselines 中的方法集合进行横向评估。对比实验统一报告以下三类结果：

*系统效率*：迭代时间、通信时间占比、P95/P99 尾部等待与 GPU 利用率；

*收敛质量*：相同 step 下的验证指标、相同 wall-clock 时间下的 loss 下降速度；

*综合收益*：达到同等目标精度的总训练时长（time-to-target）。

该对比用于评估两个方面：k-bit 相比固定精度量化（FP16/INT8）是否具备更好的可调压缩-收敛平衡，以及相较稀疏化方案是否在跨域链路下具有更低的索引开销与更稳定的尾部时延。

如@fig:method-comp 所示，在统一训练配置下，k-bit（4-bit）相较 FP32 的吞吐提升约为 $1.35 times$（6216 vs 4602，单位为 items/s），通信时间占比由 41.2% 降至 22.5%，P95 尾部时延由 168.0 ms 降至 96.8 ms。与 FP16、INT8 及 Top-k 基线相比，k-bit 在吞吐与尾部稳定性上均取得更优平衡，说明其在跨域受限链路下更能稳定释放系统吞吐。

从对比结构上看，四类基线方法分别对应不同技术路径：FP16/INT8 属于固定精度量化，Top-k 属于稀疏化压缩，k-bit 则采用“动量归一化 + 随机舍入 + 位打包”的一体化方案。实验结果显示，k-bit 在通信占比与尾部时延两个关键指标上同时占优，说明该方案在系统与优化目标之间也实现了更可控的折中。

#figure(
  image("../../image/ch3-method-comparison.svg", width: 98%),
  caption: [k-bit 与主流压缩方法的端到端性能对比]
) <fig:method-comp>


其原因可从两方面解释。第一，固定精度量化（如 INT8 等）虽然实现成熟，但压缩率上限受限，在跨域低带宽场景下难以继续压缩尾部；第二，稀疏化方案虽然压缩率较高，但索引维护、打包与重组、规约聚合开销会在高 RTT 条件下被放大，且稀疏模式波动会增加尾部抖动。k-bit 通过位打包与无偏随机舍入，降低了索引型开销与偏差积累风险，因此更稳定。

从训练稳定性角度看，最终 loss 的差异幅度处于可控范围，表明系统侧收益并非由显著牺牲优化行为换取。尤其在与 Top-k 路径比较时，k-bit 在尾部时延和最终损失上均表现更稳，支持本文关于“量化路径在跨域场景中更具工程可部署性”的结论。

本组对比还给出一个可落地的实践建议：在跨域链路受限且对收敛质量有约束的训练任务中，优先选择具备误差补偿与可调位宽能力的量化方案；对于极端带宽受限但可容忍收敛扰动的场景，再考虑更激进压缩配置。该建议与本文方法设计目标一致，即在保障训练语义可控的前提下释放通信优化收益。

*多模型端到端训练结果：*

针对 @tab:multi-model-e2e 的三类模型，本章以端到端训练耗时与到达目标精度时间作为主指标。实验重点在于完整训练流程中的真实收益，包括前后向计算、梯度同步、优化器更新与数据流水线等环节共同作用后的整体加速比。

该设置有助于降低“仅在单模型或单一网络条件下成立”的结论偏差，并为后续章节的系统级优化提供统一基线。

@fig:multi-model-e2e 实验取通信链路带宽为 3 Gb/s 进行实验。该带宽设置处于云边端跨域训练常见的中低带宽区间，能够较好反映压缩通信在真实受限链路中的收益边界，因此适合作为端到端对比的统一参考条件。

#figure(
  image("../../image/ch3-multi-model-e2e.svg", width: 98%),
  caption: [k-bit 在多模型端到端训练中的收益]
) <fig:multi-model-e2e>

从@fig:multi-model-e2e 可见，k-bit 在 ResNet-50、BERT-Large 与 LLaMA-7B 三类负载上均带来稳定端到端收益：time-to-target 分别由 128/214/680 min 降至 95/158/492 min；吞吐由 4620/3090/980 提升至 6180/4350/1320（其中 ResNet 和 BERT 单位为 samples/s，LLaMA-7B 单位为 tokens/s）。这说明低比特量化带来的收益并不局限于单一模型结构，而是能够随着训练规模和序列长度的变化持续体现为 wall-clock 级别的节省。

该组实验的核心价值在于跨任务一致性验证。视觉分类、语言理解与自回归建模在数据访问模式、梯度统计特征与训练稳定性敏感点上存在显著差异；若某通信优化仅对单一任务有效，通常难以在三类任务中同时保持正增益。当前结果显示三类任务的 time-to-target 均明显缩短，说明 k-bit 的收益主要来自通信路径效率提升这一跨任务共性。

进一步比较三类任务的收益幅度可以看到，大模型与长序列任务的绝对节省时间更为显著。这与其更高通信负载和更长训练时间窗口相一致：当通信在总时长中的占比较高时，压缩策略对 wall-clock 的改善更容易累积为可观的总时长缩减。换言之，k-bit 在通信主导型工作负载中的边际收益更大，这一趋势与前述网络受限实验中的结论相互印证，也说明第 3 章的方法更适合被部署在云边端协同训练中“通信占比高、同步频繁”的训练阶段。

同时，该组结果并未显示任务切换导致的稳定性失效，说明所采用的误差补偿机制在不同任务分布下具备一定鲁棒性。对工程部署而言，这一性质非常关键：它意味着策略可在同一系统中服务多类训练任务，而无需为每类任务完全重写通信路径，降低了运维和调参成本。

#figure(
  image("../../image/ch3-validation-loss-curve.svg", width: 98%),
  caption: [FP32 与 4-bit 在代表性网络条件下的 validation loss 收敛曲线]
) <fig:val-loss-curve>

需要指出的是，不同任务之间的最优位宽可能并不完全一致。本章给出的统一配置验证了可行且有效，但若追求最优收益，仍可在保持统一框架的前提下按任务进行位宽微调。该观察进一步说明本文方法的优势在于提供了可调、可扩展的统一通信优化底座，而@fig:val-loss-curve 所展示的 loss 曲线则从优化侧补充了这一结论。

如@fig:val-loss-curve 所示，在等效跨域带宽约 $3$ Gb/s 的条件下，4-bit 曲线与 FP32 在主要训练阶段保持接近，且末段验证损失差值维持在约 $0.02$ 量级。这表明压缩通信带来的吞吐收益没有明显破坏主训练路径中的优化过程，验证损失的走势仍与基线保持同阶。

更重要的是，@fig:val-loss-curve 与前述吞吐结果形成了互补关系：前者解释了压缩后是否能够稳定收敛的问题，后者解释了在多类任务上能够节省多少时间。两者合起来说明，k-bit 在可接受的误差扰动范围内提升了端到端训练效率。

#figure(
  image("../../image/ch3-experiment-results.svg", width: 98%),
  caption: [k-bit 量化方法的收敛性与训练加速比验证（以 LLaMA-7B 为例）]
) <fig:experiments-result>

@fig:experiments-result 采用 LLaMA-7B 作为代表模型，展示不同带宽条件下各量化位宽的吞吐与最终验证损失变化。该图进一步说明，位宽选择与链路条件之间存在明显耦合：带宽越受限，低位宽带来的系统收益越突出；而在较宽带宽下，较高位宽则更有利于保留优化稳定性。

综上，本组实验从端到端层面支撑了本文方法的通用性结论：在多任务、多负载条件下均可稳定降低端到端训练时延，并保持可接受的优化行为偏差。

*国产 GPU 集群实验：*

为验证低比特通信方法在国产 GPU 物理集群上的可迁移性，本章进一步在实验环境二中进行了补充实验。补充实验使用两节点共 8 个训练进程，即每节点选取 4 张 GPU 参与训练，以便与实验环境一中的主体实验保持相同的并行规模。与实验环境一不同，CoreX 集群的节点间链路为 12.5 Gb/s Ethernet，通信后端、设备驱动与集合通信实现均与 A100 云集群不同，因此该实验重点关注两个问题：其一，低比特压缩路径在异构 GPU 软件栈中是否能够保持数值收敛稳定；其二，在真实物理 Ethernet 跨节点链路下，压缩收益是否仍能在大模型通信负载中体现出来。

#figure(
  table(
    columns: (0.9fr, 1.2fr, 1.2fr, 0.8fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*模型*], [*基线耗时*], [*优化后耗时*], [*加速比*], [*收敛表现*]),
    table.hline(stroke: 0.5pt),
    [ResNet-50], [246.7 ms/step], [228.4 ms/step], [1.08×], [loss 基本一致],
    [YOLOv8], [210.0 s/100 step], [202.6 s/100 step], [1.04×], [loss 未见恶化],
    [BERT-Large], [903.1 ms/step], [384.3 ms/step], [2.35×], [loss 基本一致],
    [LLaMA-7B], [4402.4 ms/step], [1540.8 ms/step], [2.86×], [loss 基本一致],
    table.hline(),
  ),
  caption: [实验环境二上不同模型的低比特通信补充结果]
) <tab:corex-quant-e2e>

从@tab:corex-quant-e2e 可以看到，在 CoreX 物理集群中，低比特通信路径在不同模型上均未造成明显的最终损失退化，主要训练曲线与 FP32 基线处于同一量级。这说明动量归一化、随机舍入与误差补偿机制在异构 GPU 后端上仍具备数值稳定性，方法并不依赖单一厂商硬件才能维持优化行为。

系统性能方面，CoreX 补充实验呈现出明显的负载相关性。ResNet-50 与 YOLOv8 的端到端提升较低，分别约为 $1.08 times$ 与 $1.04 times$，主要原因在于视觉模型的单步计算、数据处理与小粒度梯度桶开销占比较高，跨节点通信并非唯一主导瓶颈。相对地，BERT-Large 与 LLaMA-7B 的参数规模和梯度同步载荷更大，低比特压缩能够显著减少跨节点 Ethernet 上的传输时间，因此分别取得约 $2.35 times$ 与 $2.86 times$ 的端到端加速。该结果表明，在 CoreX 集群上，本文方法的主要收益仍来自通信数据体积削减，并且收益会随模型通信负载增大而快速放大。

#figure(
  table(
    columns: (0.8fr, 1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*带宽*], [*1F1B+DDP 基线*], [*1F1B+DDP k-bit*], [*吞吐加速比*]),
    table.hline(stroke: 0.5pt),
    [1 Gb/s], [34.0 tokens/s], [123.8 tokens/s], [3.64×],
    [2 Gb/s], [66.6 tokens/s], [220.4 tokens/s], [3.31×],
    [5 Gb/s], [157.1 tokens/s], [474.4 tokens/s], [3.02×],
    [10 Gb/s], [287.5 tokens/s], [819.4 tokens/s], [2.85×],
    table.hline(),
  ),
  caption: [实验环境二上 LLaMA-7B 在受限带宽下的吞吐补充结果]
) <tab:corex-llama-bandwidth>

进一步地，@tab:corex-llama-bandwidth 给出了 LLaMA-7B 在不同受限带宽下，仅启用 k-bit 低比特同步时的吞吐趋势。结果显示，当跨节点链路从 10 Gb/s 降至 1 Gb/s 时，低比特通信的相对收益由 $2.85 times$ 提升至 $3.64 times$，与前述 LLaMA-7B 默认端到端实验中约 $2.86 times$ 的收益基本对齐。该现象说明，在不叠加 POLAR 等额外并行调度优化时，k-bit 的主要收益仍来自跨节点同步载荷下降；链路越受限，通信体积压缩越容易转化为端到端吞吐提升，但其收益边界低于“压缩 + 调度重叠”联合方案。

综合来看，实验环境二补充结果给出了更完整的工程边界：低比特压缩在 CoreX 集群上能够保持收敛稳定，但端到端加速幅度取决于模型通信占比。对于 ResNet-50、YOLOv8 等通信占比较低或梯度桶较小的任务，收益更多体现为小幅缩短同步时间；对于 BERT-Large、LLaMA-7B 等通信主导型负载，压缩后的跨节点传输量下降能够直接转化为显著吞吐提升。这进一步说明本文方法适合部署在大模型和低带宽跨域训练阶段，并需要在不同硬件后端上结合模型规模与链路状态选择启用策略。

== 本章小结

本章围绕分布式训练中跨域通信链路的数据传输瓶颈问题，提出并详细介绍了一种基于 $k$-bit 随机舍入的低比特梯度量化通信策略。首先，针对现有固定精度压缩在跨域场景下的局限性，本章给出了基于动量归一化与无偏随机舍入的量化方法，通过附加的残差误差补偿机制在压缩率与优化偏差之间取得了稳健的折中。接着，设计了适配该量化算法的张量位打包与合并解包机制，并以极低的工程侵入性将其集成于现有分布式训练框架中。多网络条件与多模型端到端实验表明：该核心机制在强带宽受限场景下可将通信数据量压缩至原有的 $1/32$，在保持最终模型收敛质量基本一致（验证损失误差不超过 0.02）的前提下，实现总体端到端最高约 $2.43 times$ 的吞吐提升，并且由于物理负载幅度的直线下降有效削减了受限环境下的长尾同步时延。国产 GPU 集群补充实验进一步表明，该方法在 CoreX 物理集群上能够保持收敛稳定，并在 BERT-Large、LLaMA-7B 等通信主导型负载中取得显著加速，其中 LLaMA-7B 端到端加速达到约 $2.86 times$；同时，ResNet-50 与 YOLOv8 的低收益结果也说明压缩策略需要结合模型规模、硬件后端和链路状态动态启用。

#pagebreak()
