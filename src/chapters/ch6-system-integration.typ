#import "../../template/lib.typ": multicite
#import "../../template/src/algorithm.typ": pseudocode-list

= 协同通信优化系统：集成与实验测试

本章将前文提出的三个通信优化模块统一为一个可部署的系统视角：模块 1（低比特量化压缩）面向通信数据量，模块 2（跨域集合通信流水化）面向通信路径与调度，模块 3（计算-通信掩盖）面向关键路径重叠。通过统一控制与运行时联动，系统可在不同网络状态下动态选择更合适的优化组合。

与前三章叙述逻辑不同，本章旨在构建一个完整的系统闭环：不再局限于讨论单一模块的局部最优，而是重点剖析这三大模块在同一训练迭代循环中的接口契约、触发时序、冲突消解机制以及协同验证方法。本章的核心目标在于：验证三者在复杂的实际系统运行环境中能否稳定共存，以及在云边端跨域受限的网络条件下，所取得的通信性能增益能否稳定地转化为端到端模型训练的整体加速。

== 引言

从云边端协同训练的部署现实出发，系统设计遵循以下目标与约束：

一方面必须保持“每迭代一次全局同步”的语义节奏，避免以彻底异步换取短期吞吐；另一方面，性能收益需要体现在多轮训练的端到端时间而非局部算子峰值。与此同时，系统尽量复用 PyTorch 与 NCCL 既有执行栈，将改动集中在训练循环和通信调度层，以控制工程侵入性 #multicite[@pytorch_distributed2020 @nccl2]；当网络状态或模型规模变化使收益收敛时，策略层还应具备可解释、可验证的保守回退路径。

上述约束决定了系统采取"模块 2 常驻 + 模块 3主导触发 + 模块 1按需介入"的分层启用模式，而非静态启用全部优化。该模式一方面保留了跨域调度优化的基础收益，另一方面避免在通信不再主导时引入不必要的量化误差与额外管理开销。

== 问题分析与研究思路

为降低工程复杂度并提升可维护性，协同模块被放入四层执行架构：

其中训练控制层负责 step 生命周期、micro-batch 进度和优化器更新时机，策略决策层维护 profiling 状态并输出模块组合动作，通信执行层承担分块、流水化和跨域 collective 启动与句柄管理，误差与状态层则维护误差缓冲、滑动统计量、迟滞变量与回退标志。四层分工使“策略决策”和“通信执行”解耦，便于定位瓶颈与演化系统实现。

对应到各模块的职责边界可总结为：

模块 1 的职责是进行通信数据量变换，但不改写训练循环时序；模块 2 负责通信路径与并行度组织，但不改变梯度语义；模块 3 负责通信触发时机重排，在语义约束内调整关键路径重叠关系。通过这种边界划分，系统能够在保持语义可控的同时获得组合优化收益。

该职责划分减少了功能交叉，使每个模块可以独立演进，同时保证在系统集成时具备清晰的故障定位路径。

=== 统一符号与状态记号

为避免跨章节符号复用造成歧义，本章将系统集成阶段高频使用的变量统一如下：

#figure(
  table(
    columns: (auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*符号*], [*含义*]),
    table.hline(stroke: 0.5pt),
    [$t$], [全局训练 step 索引],
    [$m$], [micro-batch 索引],
    [$tau$], [前缀触发截断点],
    [$T_"comm"$], [本步跨域通信耗时统计量],
    [$T_"comp"(tau)$], [从 $m=tau$ 到步末的可重叠计算窗口估计],
    [$s_(r,t)$], [rank $r$ 在 step $t$ 的发送缓冲],
    [$h_t$], [本步异步集合通信句柄],
    [$e_(r,t)$], [误差反馈缓冲],
    [$theta_t$], [策略状态向量（含阈值、迟滞与回退标志）],
    [$a_t$], [系统动作：模块组合与参数配置],
    table.hline(),
  ),
  caption: [第六章系统集成的统一符号与状态记号]
) <tab:system-notation>

其中，$a_t$ 可取“仅模块 2+3”“模块 1+2+3”“保守回退路径”等离散动作，$theta_t$ 由滑动统计量与阈值控制变量组成。该表示方式使系统策略可被统一建模为“状态到动作”的受约束映射，便于后续实现与测试复现。

== 详细方案设计与实现

@fig:three-module-mechanism 给出了各通信优化模块的运行时协同机制：模块 2 作为常驻基础能力持续提供可重叠窗口；模块 3 依据在线 profiling 与阈值判断决定是否执行前缀触发异步 All-Reduce；当监测到跨域通信时延上升、掩盖窗口不足时，模块 1 按需触发以进一步压缩通信量并扩大重叠空间。

该闭环可抽象为“监测-判定-执行-反馈”四个阶段：

系统先周期采样跨域通信时延与可重叠计算窗口，再依据阈值关系完成是否充分掩盖的判定；随后在“仅模块 2+3”与“模块 1+2+3”两类策略间做运行时选择，并将本轮执行效果回写到 profiling 与阈值更新逻辑，作为下一轮决策的输入。

#figure(
  image("../../image/three-module-communication-optimization-mechanism.svg", width: 98%),
  caption: [三个通信优化模块协同机制与运行时决策闭环]
) <fig:three-module-mechanism>

== 系统集成接口与执行流程

具体执行时，训练循环先按 DP+PP（1F1B）完成常规前反向与梯度累积，再在 $m=tau$ 的截断点由模块 3 触发异步 All-Reduce；模块 2 随后在独立通信 stream 上执行分块流水化集合通信，而模块 1 仅在判定为通信受限时介入发送缓冲压缩。到步末阶段，系统统一等待通信句柄、完成参数更新，并回写误差缓冲与 profiling 状态。

该流程保持“每迭代一次全局同步”的训练语义，同时把跨域通信尽量前移到可重叠窗口中，以降低尾部阻塞。

=== 模块接口与状态变量约定

为了确保模块可插拔且便于复现实验，系统在实现上约定了统一的输入输出与状态变量：

#figure(
  table(
    columns: (auto, auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*模块*], [*核心输入*], [*核心输出/状态*]),
    table.hline(stroke: 0.5pt),
    [模块 1：低比特量化], [发送缓冲 $s_(r,t)$、量化配置], [量化缓冲 $hat(s)_(r,t)$、量化残差统计],
    [模块 2：流水化集合通信], [分块张量、通信组信息、stream/event], [异步句柄 $h_t$、分块完成时间序列],
    [模块 3：计算-通信掩盖], [micro-batch 进度、$T_"comm"$、$T_"comp"$], [截断点 $tau$、触发决策、回退标志],
    [统一状态层], [本步完整梯度、预测梯度], [误差缓冲 $e_(r,t+1)$、阈值更新量],
    table.hline(),
  ),
  caption: [协同系统集成中的接口与状态变量约定]
) <tab:system-interface-contract>

该约定的意义在于将“算法策略”与“执行机制”解耦：策略层只决定何时触发、是否压缩，执行层只负责正确完成通信与同步，从而降低跨模块耦合度。

=== 运行时决策与回退机制

系统在每个 step 结束时更新一次策略状态，并在下一 step 生效。决策逻辑可概括为：

当 $T_"comm" <= T_"comp"(tau)$ 时，系统维持当前策略以优先保证稳定性；若连续多步出现 $T_"comm" > T_"comp"(tau)$，则先尝试提前 $tau$ 扩大重叠窗口。若该调整仍不足以覆盖通信尾部，系统才启用模块 1 进行压缩；当检测到通信瓶颈缓解后，策略会逐步降低压缩强度并回退到更保守路径。

该机制能够优先利用不改变梯度数据本身的优化手段，即第五章通信受限下的混合并行通信掩盖策略，在必要时再引入数据压缩方法，即第三章所述跨域通信低比特量化方法，保证在尽最大可能提升训练系统效率的前提下保证训练过程的稳定。

为将上述策略流程落到可执行实现，@algo:system-controller 给出每个 step 的统一控制器主循环。

#figure(
  kind: "algorithm",
  placement: top,
  supplement: [算法],

  pseudocode-list(booktabs: true, numbered-title: [协同通信优化系统控制器主循环], full: true)[
    - *输入状态：* $theta_t$，$e_(r,t)$，网络与计算 profiling 缓冲
    - *输出：* 更新后的参数 $w_(t+1)$ 与状态 $theta_(t+1)$

    + *for* 每个 step $t$ *do*
      + 采样 $T_"comm"$、$T_"comp"(tau)$ 并更新滑动统计
      + $a_t <- "PolicyDecision"(theta_t, T_"comm", T_"comp")$
      + *for* 每个 micro-batch $m = 1, ..., M$ *do*
        + 执行前向/反向并累积梯度
        + *if* $m = tau$ *then*
          + 根据 $a_t$ 选择是否启用模块 1 压缩
          + 在通信 stream 上调用模块 2 启动异步 All-Reduce，记录 $h_t$
        + *end*
      + *end*
      + *wait* $(h_t)$ 并执行参数更新
      + 更新误差缓冲 $e_(r,t+1)$ 与回退标志
      + $theta_(t+1) <- "PolicyUpdate"(theta_t, "观测指标", a_t)$
    + *end*
  ],
) <algo:system-controller>

=== 工程实现细节与开销讨论

在工程实现中，系统主要新增三类开销：

新增开销主要来自三个方面：一是时延采样与滑动统计带来的 profiling 成本，通常显著低于一次跨域 All-Reduce；二是误差缓冲、策略变量与事件对象管理引入的状态维护成本，表现为额外显存占用与少量主机侧调度负担；三是压缩路径开关与分块参数变更带来的短时重配置成本。

实验中通过降低 profiling 频率、限制策略更新步长与使用迟滞切换，能够将上述开销控制在可接受范围内，使其不抵消多模块协同收益。

== 实验与验证

为验证该系统的有效性，本节从系统级视角给出测试项与结果汇总。前文分章实验分别验证了单模块与局部组合能力，本节关注端到端可交付性：性能收益、收敛稳定性与工程一致性是否可同时成立。

=== 系统级实验设置

系统级实验沿用前文的跨域 DP、域内 PP 训练骨架，并将第 3-5 章的单模块实验统一到同一组运行时观测口径下。与单章实验相比，本章不再只验证某个算子或某个通信阶段，而是同时记录完整训练 step 中的计算、通信、策略决策和收敛结果。


#figure(
  table(
    columns: (auto, 1fr),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*组件*], [*系统级配置与统计口径*]),
    table.hline(stroke: 0.5pt),
    [模型与任务], [以 LLaMA-7B 语言建模任务为主要系统级负载；固定模型与 batch 实验、带宽扫描实验和 batch size 敏感性实验均在该负载口径下统计吞吐],
    [实验平台], [以天数智芯（Iluvatar CoreX）国产 GPU 物理集群为主要验证平台；n210/n62 双节点分别对应 Domain-1/Domain-2，节点内 PCIe 互联，节点间 Ethernet 互联],
    [并行骨架], [跨域数据并行 + 域内流水线并行；跨域链路只承担梯度同步，域内负责模型切分与 micro-batch 流水执行],
    [软件栈], [复用 PyTorch、NCCL 与 CUDA 分布式训练栈；系统改动集中在训练循环、通信调度、量化压缩与 profiling 控制层],
    [网络条件], [通过网络整形注入跨域带宽与 RTT；弱受限为 10–30 Gb/s、5–30 ms，中度受限为 5–10 Gb/s、30–100 ms，强受限为 1–5 Gb/s、30–100 ms],
    [对比方法], [现有同步训练系统、DiLoCo、Streaming DiLoCo，以及启用模块 1+2+3 的本文系统],
    [评价指标], [端到端吞吐、单步迭代时间、通信时间、time-to-target、最终验证损失或 PPL、退化测试中的回退行为],
    [日志变量], [$T_"comm"$、$T_"comp"(tau)$、$tau$、模块启用状态、异步通信句柄完成时间和误差缓冲状态],
    table.hline(),
  ),
  caption: [第六章系统级实验设置]
) <tab:ch6-system-experiment-setup>

需要说明的是，本章系统级实验主要在天数智芯（Iluvatar CoreX）国产 GPU 物理集群上完成，用于验证前述通信优化机制在非 NVIDIA 生态和真实物理跨节点环境中的可部署性。实验平台由 n210（Domain-1）与 n62（Domain-2）两个节点组成，每节点配置 Intel Xeon Gold 6330 CPU 与 Iluvatar BI-V150 GPU；节点内通过 PCIe 互联，节点间通过 Ethernet 互联。网络受限条件在该物理集群上通过链路整形方式注入，从而形成不同跨域带宽与 RTT 分组。相比单模块章节中部分 A100 环境或仿真化微基准，本章更关注完整系统在国产集群上的端到端运行稳定性与综合收益。

@tab:ch6-system-experiment-setup 设置使第六章的结果能够与前文形成对应关系：第 3 章的低比特压缩影响通信数据量，第 4 章的流水化调度影响通信路径执行时间，第 5 章的前缀触发影响通信是否暴露在关键路径上；而本章统一观察三者叠加后在完整训练 step 中的端到端结果。

=== 测试目标与判据

系统级测试覆盖以下四类判据：

判据体系同时覆盖性能、优化与工程三条主线：性能侧关注端到端 tokens/s 或 samples/s 相对基线是否提升，优化侧关注 step 对齐 loss 曲线是否与基线保持一致并避免明显漂移，同时通过评估达到目标loss时间是否实质缩短；工程侧则验证训练循环、通信句柄和误差缓冲更新链路能否长期稳定运行且不破坏训练语义。

#figure(
  image("../../image/ch6-system-performance-compare.svg", width: 98%),
  caption: [固定模型与 batch 条件下，本系统与基线系统的吞吐、迭代时间与通信时间对比]
) <fig:ch6-system-perf>

=== 测试流程

为保证测试结论可复核，系统级验证按照功能正确性 -> 性能收益 -> 稳定性 -> 退化行为顺序执行：

在具体实验组织上，本节补充五组系统级验证：

第一组，在固定模型与 batch 条件下开展性能测试，对比基线与协同系统的吞吐、迭代时间和通信占比；第二组，在 LLaMA-7B 训练任务上改变跨域带宽，对比本方法、DiLoCo 与 Streaming DiLoCo 的吞吐适应性；第三组，在固定 5 Gb/s 跨域带宽下改变 batch size，分析 micro-batch 数量与吞吐、最终 PPL 之间的关系；第四组，通过 step 对齐与 wall-clock 对齐的 loss 曲线评估稳定性；第五组，在更高带宽或更低 RTT 条件下执行退化测试，确认系统能够自动回退并保持与基线一致行为。

测试矩阵覆盖三类网络区间：

弱受限网络区间内通信绝大部分可被计算掩盖，重点观察回退机制是否合理；中度受限网络区间中通信与计算接近，重点考察阈值策略稳定性及抗抖动能力；强受限网络区间中通信显著主导，重点验证多模块叠加后是否仍具可持续收益。

=== 测试结果汇总

// Data source: image/csv/ch6_tab_final_system_validation.csv


@tab:final-system-validation 给出了系统级对比结果：在 弱受限、中度受限、强受限 三类网络环境中，统一启用第 3-5 章全部优化方法（模块 1+2+3）的本系统，均优于现有训练系统。

// Data source: image/csv/ch6_system_perf_compare.csv

如@fig:ch6-system-perf 所示，在固定模型与 batch 设置下，本系统在 弱受限/中度受限/强受限 网络区间均获得吞吐提升，同时迭代时间下降、通信时间降低。

#figure(
  table(
    columns: (1fr, 1fr, 1fr, 1.2fr, 0.65fr, 1fr, 0.7fr),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*网络分组*], [*链路条件*], [*现有训练系统吞吐 (tokens/s)*], [*本系统吞吐 (tokens/s)*], [*加速比*], [*TTT：现有/本系统 (min) *], [*最终验证损失*]),
    table.hline(stroke: 0.5pt),
    [弱受限], [10–30 Gb/s; 5–30 ms], [354], [1362], [3.85×], [302/79], [0.02],
    [中度受限], [5–10 Gb/s; 30–100 ms], [223], [1003], [4.50×], [480/107], [0.03],
    [强受限], [1–5 Gb/s; 30–100 ms], [86], [551], [6.41×], [1242/194], [0.04],
    table.hline(),
  ),
  caption: [不同网络环境下本系统与现有训练系统的最终对比结果]
) <tab:final-system-validation>

从吞吐看，网络约束越强，组合优化收益越明显：弱受限 为 3.85×，中度受限 为 4.50×，强受限 为 6.41×。强受限场景下绝对吞吐低于中度受限区间，但相对基线的提升幅度更大，说明跨域瓶颈越突出，多模块协同对通信关键路径的削弱作用越明显。



从端到端训练效率看，time-to-target 在三类网络中均明显缩短，分别由 302/480/1242 min 降至 79/107/194 min，表明吞吐提升能够稳定转化为训练周期收益，而非仅停留在局部算子层面。尤其在强受限网络下，训练效率达到约 6.41× 的提升，说明在极端带宽受限条件中，多模块叠加能够显著缩短达到目标精度所需的墙钟时间。

进一步观察@fig:ch6-system-perf 中吞吐与迭代时间的对应关系可以发现，三类网络下“吞吐-迭代时间”基本保持固定 batch 训练中的反比关系。基线系统从弱受限到强受限时，吞吐由 354 tokens/s 降至 86 tokens/s，单步迭代时间由 1446 ms 增至 5953 ms；完整系统吞吐由 1362 tokens/s 降至 551 tokens/s，单步迭代时间则由 376 ms 增至 929 ms。也就是说，右图中的迭代时间变化能够解释左图吞吐变化，二者共同说明系统收益来自每个训练 step 的关键路径缩短，而非统计口径变化。

从通信时间拆解看，跨域约束增强时通信占比持续上升。基线系统的通信时间从 1050 ms 增至 5480 ms，占单步迭代时间的比例由约 72.6% 增至 92.1%，说明在强受限链路下训练几乎退化为通信等待主导。完整系统中通信时间也随网络受限程度由 95 ms 增至 620 ms，占比由约 25.3% 升至 66.7%，但其绝对通信等待仍显著低于基线。这一现象符合本文各模块的作用边界：优化策略无法消除链路物理带宽下降带来的全部影响，但能够通过压缩、流水化和重叠将通信增长斜率显著压低。因此，强受限网络中完整系统的通信占比仍会上升，但上升后的迭代时间仍远低于基线。

上述结果还表明，各模块收益有叠加效应。弱受限场景中，模块 3 的通信掩盖和模块 2 的流水调度已经能够覆盖大部分通信尾部，因此模块 1 的压缩收益相对有限；中度受限场景中，通信与计算窗口接近，策略层需要在提前触发和压缩开销之间取得平衡，收益主要来自三者协同；强受限场景中，跨域通信成为绝对主导，模块 1 的通信量削减开始发挥更关键的作用，同时模块 2 与模块 3 保证压缩后的数据传输能够尽可能与剩余计算重叠。由此可见，网络越受限，系统越依赖完整模块组合，而不是单一优化手段。

=== 不同带宽下的横向方法对比

为进一步验证本系统并非仅相对传统同步基线有效，本节在 LLaMA-7B 训练任务上补充与 DiLoCo 和 Streaming DiLoCo 的横向对比。DiLoCo 通过减少全局同步频率降低跨域通信压力，Streaming DiLoCo 在此基础上进一步引入流式通信组织；二者代表了跨域训练中较典型的“降低同步频率/流式化同步”路径。与之不同，本文系统仍保持每轮迭代一次全局同步的语义，通过通信数据量压缩、层次化流水通信和计算-通信掩盖协同降低同步代价。

如@fig:ch6-llama7b-bandwidth-throughput 所示，在 1 Gb/s、2 Gb/s、5 Gb/s 与 10 Gb/s 四种网络带宽下，本方法均取得最高吞吐，分别达到 292、498、863 与 1142 tokens/s。相比 1F1B+DDP 基线，本方法在四种带宽下分别获得约 8.59×、7.43×、5.50× 与 3.97× 的吞吐提升；相比 Streaming DiLoCo，本方法仍分别提升约 1.65×、1.54×、1.40× 与 1.27×。

// Data source: image/llama7b_bandwidth_throughput_estimates_bar.svg
#figure(
  image("../../image/llama7b_bandwidth_throughput_estimates_bar.svg", width: 98%),
  caption: [不同跨域带宽下 LLaMA-7B 训练吞吐的横向方法对比]
) <fig:ch6-llama7b-bandwidth-throughput>

该结果体现出两个趋势。第一，当跨域带宽较低时，传统同步训练受到 All-Reduce 尾部阻塞影响最为明显，DiLoCo 类方法虽然能够通过降低同步频率缓解通信压力，但吞吐仍受局部更新与跨域同步阶段的交替影响；本文系统则同时削减通信载荷、改善通信路径并提前隐藏通信，因此在 1 Gb/s 与 2 Gb/s 下优势最明显。第二，随着带宽提升至 10 Gb/s，各方法的通信瓶颈均有所缓解，吞吐差距自然收敛，但本方法仍保持领先，说明系统收益并非只来自极端低带宽下的单点压缩，而是来自三类模块对通信关键路径的联合优化。

从方法特性看，DiLoCo 和 Streaming DiLoCo 的优势主要来自降低跨域同步频率或将同步过程流式化，因此它们在低带宽环境中能够避免每步全量同步的直接代价。但这种路线会把部分全局一致性压力推迟到周期性同步阶段，并且对 local step、学习率和数据异质性更敏感。本文系统的优势在于不改变每步全局同步语义，而是在每步内部重排通信执行路径。因而在同等带宽下，本文系统能够在保持同步训练语义的同时获得接近或超过低同步频率方法的吞吐，这对需要严格控制训练轨迹、调试误差和复现实验的跨域训练任务更有意义。

带宽从 1 Gb/s 提升至 10 Gb/s 时，所有方法吞吐均上升，但上升斜率不同。基线方法受全量同步限制最明显，低带宽下几乎被通信完全压制；DiLoCo 与 Streaming DiLoCo 的曲线更平滑，说明降低同步频率确实能减轻带宽敏感性；本文系统在低带宽下提升最明显，在高带宽下则逐渐接近计算侧上限。该趋势说明，当跨域链路从强瓶颈逐渐变为次要瓶颈时，系统的主要收益来源也会从“减少通信量”转向“减少调度尾部和等待空窗”，这与运行时策略中模块 1 按需介入、模块 2/3 常驻的设计一致。

=== Batch Size 敏感性分析

模块 3 的核心收益依赖可重叠计算窗口，而该窗口与 micro-batch 划分直接相关。为分析该因素对系统行为的影响，本节在 5 Gb/s 跨域链路下固定模型与通信配置，改变 batch size，并同时观察训练吞吐与最终 PPL。该实验用于回答两个问题：一是更大的 batch 是否能为通信掩盖提供更充分窗口；二是吞吐提升是否伴随明显的模型质量退化。

// Data source: image/llama7b_batch_size_sweep_5gbps_combined.svg
#figure(
  image("../../image/llama7b_batch_size_sweep_5gbps_combined.svg", width: 98%),
  caption: [5 Gb/s 跨域带宽下 batch size 对吞吐与最终 PPL 的影响]
) <fig:ch6-llama7b-batch-sweep>

如@fig:ch6-llama7b-batch-sweep 所示，batch size 从 16 增大至 64 时，各方法吞吐均随可并行计算量增大而提升，但本文方法的提升幅度更显著：吞吐由 863 tokens/s 提升至 1933 tokens/s，始终高于 DiLoCo 与 Streaming DiLoCo。在 batch size 为 32、48、64 时，本方法相对 Streaming DiLoCo 的吞吐提升分别约为 1.30×、1.17× 与 1.24×。

从机制上看，batch size 增大通常对应更多 micro-batch 或更长的局部反向计算窗口，使 $T_"comp"(tau)$ 更容易覆盖跨域通信耗时 $T_"comm"$。因此，模块 3 能够更充分地把跨域同步前移至可重叠区间，模块 2 的分块流水通信也拥有更稳定的并发执行窗口。与此同时，最终 PPL 随 batch size 增大而上升，说明吞吐收益与模型质量之间仍存在常见的大 batch 训练权衡。本文方法在 batch size 为 16 和 32 时最终 PPL 分别为 42.6 和 44.4，与基线差距较小；当 batch size 增至 64 时 PPL 升至 55.8，表明在追求最高吞吐时仍需配合学习率、warmup 或局部 batch 策略进行调参。该结果也说明，系统默认策略不应单纯追求最大 batch，而应将吞吐、目标精度和可重叠窗口共同纳入部署配置。

batch size 从 16 增至 32 时，本文方法吞吐由 863 tokens/s 提升到 1271 tokens/s，属于较高收益区间；继续增至 48 和 64 时，吞吐进一步提升至 1533 和 1933 tokens/s，但最终 PPL 也逐步上升。这说明在 5 Gb/s 链路下，较小 batch 时系统仍受可重叠窗口不足限制，而中等 batch 能显著改善计算-通信重叠；当 batch 继续增大后，吞吐收益仍存在，但训练质量代价开始更明显。换言之，batch size 存在由通信掩盖收益和优化质量共同决定的折中区间。

该实验也补充解释了在线策略的必要性，而不是固定选择一个 micro-batch 配置。不同带宽下 $T_"comm"$ 变化明显，不同 batch size 下 $T_"comp"(tau)$ 也会变化；若固定提前触发点 $tau$，在小 batch 下可能过晚触发、留下通信尾部，在大 batch 下又可能过早触发、增加预测和误差反馈压力。因此，策略层必须持续根据通信耗时和可重叠窗口更新触发位置，并在收益不足时回退到更保守的同步路径。

// Data source: image/csv/ch6_system_loss_curves_step.csv + image/csv/ch6_system_loss_curves_time.csv
// #figure(
//   image("../../image/ch6-system-loss-curves.svg", width: 98%),
//   caption: [本系统与基线系统的 step 对齐与 wall-clock 对齐 loss 曲线]
// ) <fig:ch6-system-loss-curves>

// 如@fig:ch6-system-loss-curves 所示，step 对齐与 wall-clock 对齐两类曲线均保持与基线接近，未出现明显漂移。从收敛一致性看，最终验证损失差值均控制在 0.05 以内，说明本系统符合训练结果可控。

// Data source: image/csv/ch6_system_degradation_test.csv
#figure(
  image("../../image/ch6-system-degradation.svg", width: 98%),
  caption: [高带宽/低 RTT 退化测试下的自动回退与一致性验证]
) <fig:ch6-system-degradation>

如@fig:ch6-system-degradation 所示，当网络条件表现为高带宽与低 RTT 时，通信不再是迭代的限速瓶颈。此时，系统策略决策层（Policy Decision）通过在线 Profiling 监测到通信时间 $T_"comm"$ 持续小于等于可掩盖窗口 $T_"comp" (tau)$ 。在这种状态下，系统主动关闭了模块 1 的低比特量化流程，避免了不必要的量化开销与精度扰动，使加速比自然收敛至接近 1。

退化测试的意义在于验证系统不会在不需要优化时强行引入额外复杂度。在高带宽或低 RTT 环境中，通信时间本身已经可以被计算阶段自然吸收，此时继续执行低比特压缩会带来编码、解码、残差维护和误差反馈等额外开销，且可能引入不必要的数值扰动。因此，加速比收敛到 1 并不是方法失效，而是策略层正确识别“通信不再是瓶颈”后的预期行为。该结果说明本文系统具备可解释的保守性：在受限链路下积极优化，在非受限链路下主动减少干预。

综上，第 6 章在不同网络环境下同条件基线对比、与 DiLoCo 类方法的横向对比、batch size 敏感性分析以及退化测试中，均能够支撑模块间协同方案在真实跨域受限场景下的有效性结论。

=== 结果讨论：适用边界与失效模式

尽管系统在跨域受限网络下表现稳定，仍存在明确适用边界：当模型计算量较小且通信量本身不高时，策略层的可优化空间会显著缩小；当网络抖动表现为强突发且难预测时，短时收益可能出现波动；当 micro-batch 数过少时，可重叠窗口不足，也会限制模块 3 的作用上限。另一方面，过大的 batch size 虽可提高吞吐并增强通信掩盖效果，但也可能带来最终 PPL 上升，因此实际部署中需要结合目标精度、学习率策略与训练预算选择合适的 batch 配置。对应地，系统提供两类失效保护：一是通过阈值驱动回退到保守同步路径，以避免持续误判；二是限制策略切换频率，以防止过度自适应引入额外不稳定性。该设计使系统在非理想网络下仍可维持“可运行、可解释、可回退”的工程属性。

=== 可复现性与部署建议

为便于后续复现与部署，交付材料如下：固定版本的软件栈（PyTorch、CUDA、NCCL）及关键环境变量、统一的 profiling 日志格式（含 $T_"comm"$、$T_"comp"$、$tau$ 与策略状态）、以及与 @tab:final-system-validation 对齐的最小测试脚本和验收阈值 #multicite[@pytorch_distributed2020 @nccl2]。

== 本章小结

本章聚焦多模块可协同的系统问题，将低比特量化、层次化流水线通信与计算-通信掩盖三类机制整合为统一运行时控制闭环。方案层面，构建了分层执行架构与清晰的接口契约，形成“监测—判定—执行—反馈”的决策流程，并提供了可解释的回退路径，确保在不同网络条件下保持训练语义一致性。效果层面，本章主要在天数智芯（Iluvatar CoreX）国产 GPU 物理集群上进行系统级验证；测试在弱受限、中度受限与强受限网络下均取得稳定的吞吐提升与达成目标损失时间缩短，同时 step 对齐曲线保持与基线一致；LLaMA-7B 横向实验表明，本方法在 1-10 Gb/s 带宽区间内均优于 DiLoCo 与 Streaming DiLoCo；batch size 敏感性实验进一步说明，micro-batch 带来的可重叠窗口是影响通信掩盖效果的重要因素。在高带宽/低 RTT 条件下，系统能够自动回退并保持与基线一致的行为。综合来看，本章证明了模块协同不仅具备叠加收益，而且能够在国产 GPU 物理集群上稳定部署，为跨域受限场景提供了端到端可交付的系统级解决方案。

#pagebreak()
