#import "../../template/src/algorithm.typ": pseudocode-list

= 三模块协同通信优化系统：集成与最终实验测试

本章将前文提出的三个通信优化模块统一为一个可部署的系统视角：模块 1（低比特量化压缩）面向通信数据量，模块 2（跨域集合通信流水化）面向通信路径与调度，模块 3（计算-通信掩盖）面向关键路径重叠。通过统一控制与运行时联动，系统可在不同网络状态下动态选择更合适的优化组合。

与前三章“按问题拆分”的叙述方式不同，本章采用“按系统闭环”组织：不再单独讨论某一模块的局部最优，而是聚焦三者在同一训练循环中的接口契约、触发顺序、冲突消解与验证方法。本章核心目标是验证三模块能否在统一语义下稳定共存，以及跨域受限网络下的性能增益能否在端到端训练中持续复现。

== 引言

从云边端协同训练的部署现实出发，系统设计遵循以下目标与约束：

其核心约束可以概括为“先稳后快”：一方面必须保持“每迭代一次全局同步”的语义节奏，避免以彻底异步换取短期吞吐；另一方面，性能收益需要体现在多轮训练的端到端时间而非局部算子峰值。与此同时，系统尽量复用 PyTorch 与 NCCL 既有执行栈，将改动集中在训练循环和通信调度层，以控制工程侵入性；当网络状态或模型规模变化使收益收敛时，策略层还应具备可解释、可验证的保守回退路径。

上述约束决定了系统不是“静态启用全部优化”，而是“模块 2 常驻 + 模块 3主导触发 + 模块 1按需介入”的分层启用模式。该模式一方面保留了跨域调度优化的基础收益，另一方面避免在通信不再主导时引入不必要的量化误差与额外管理开销。

== 问题分析与研究思路

为降低工程复杂度并提升可维护性，三模块被放入四层执行架构：

其中训练控制层负责 step 生命周期、micro-batch 进度和优化器更新时机，策略决策层维护 profiling 状态并输出模块组合动作，通信执行层承担分块、流水化和跨域 collective 启动与句柄管理，误差与状态层则维护误差缓冲、滑动统计量、迟滞变量与回退标志。四层分工使“策略决策”和“通信执行”解耦，便于定位瓶颈与演化系统实现。

对应到三模块的职责边界可总结为：

模块 1 的职责是进行通信数据形态变换，但不改写训练循环时序；模块 2 负责通信路径与并行度组织，但不改变梯度语义；模块 3 负责通信触发时机重排，在语义约束内调整关键路径重叠关系。通过这种边界划分，系统能够在保持语义可控的同时获得组合优化收益。

该职责划分减少了功能交叉，使每个模块可以独立演进，同时保证在系统集成时具备清晰的故障定位路径。

=== 统一符号与状态记号

为避免跨章节符号复用造成歧义，本章将系统集成阶段高频使用的变量统一如下：

#figure(
  table(
    columns: (auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*符号*], [*含义*]),
    table.hline(stroke: 0.5pt),
    [$t$], [全局训练 step 索引],
    [$m$], [micro-batch 索引],
    [$tau$], [前缀触发截断点],
    [$T_"comm"$], [本步跨域通信耗时统计量],
    [$T_"comp"(tau)$], [从 $m=tau$ 到步末的可重叠计算窗口估计],
    [$s_(r,t)$], [rank $r$ 在 step $t$ 的发送缓冲],
    [$h_t$], [本步异步集合通信句柄],
    [$e_(r,t)$], [误差反馈缓冲],
    [$theta_t$], [策略状态向量（含阈值、迟滞与回退标志）],
    [$a_t$], [系统动作：模块组合与参数配置],
    table.hline(),
  ),
  caption: [第六章系统集成的统一符号与状态记号]
) <tab:system-notation>

其中，$a_t$ 可取“仅模块 2+3”“模块 1+2+3”“保守回退路径”等离散动作，$theta_t$ 由滑动统计量与阈值控制变量组成。该表示方式使系统策略可被统一建模为“状态到动作”的受约束映射，便于后续实现与测试复现。

== 详细方案设计与实现

图@fig:three-module-mechanism 给出了三模块的运行时协同机制：模块 2 作为常驻基础能力持续提供可重叠窗口；模块 3 依据在线 profiling 与阈值判断决定是否执行前缀触发异步 All-Reduce；当监测到跨域通信时延上升、掩盖窗口不足时，模块 1 按需触发以进一步压缩通信量并扩大重叠空间。

#figure(
  image("../../image/three-module-communication-optimization-mechanism.png", width: 98%),
  caption: [三个通信优化模块协同机制与运行时决策闭环]
) <fig:three-module-mechanism>

该闭环可抽象为“监测-判定-执行-反馈”四个阶段：

系统先周期采样跨域通信时延与可重叠计算窗口，再依据阈值关系完成是否充分掩盖的判定；随后在“仅模块 2+3”与“模块 1+2+3”两类策略间做运行时选择，并将本轮执行效果回写到 profiling 与阈值更新逻辑，作为下一轮决策的输入。

进一步地，系统采用“短周期采样 + 中周期更新”的双时间尺度控制。前者用于及时感知网络抖动，后者用于抑制策略振荡：即便某一步出现瞬时峰值，也不会立即触发大幅策略切换，而是结合滑动窗口均值与迟滞阈值进行更新。这一机制对跨域链路的短时抖动尤其关键，可避免频繁切换导致的额外同步开销。

== 系统集成接口与执行流程

从工程实现角度，三模块通过统一训练循环对齐到同一执行节拍：

具体执行时，训练循环先按 DP+PP（1F1B）完成常规前反向与梯度累积，再在 $m=tau$ 的截断点由模块 3 触发异步 All-Reduce；模块 2 随后在独立通信 stream 上执行分块流水化集合通信，而模块 1 仅在判定为通信受限时介入发送缓冲压缩。到步末阶段，系统统一等待通信句柄、完成参数更新，并回写误差缓冲与 profiling 状态。

该流程保持“每迭代一次全局同步”的训练语义，同时把跨域通信尽量前移到可重叠窗口中，以降低尾部阻塞。

=== 模块接口与状态变量约定

为了确保模块可插拔且便于复现实验，系统在实现上约定了统一的输入输出与状态变量：

#figure(
  table(
    columns: (auto, auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*模块*], [*核心输入*], [*核心输出/状态*]),
    table.hline(stroke: 0.5pt),
    [模块 1：低比特量化], [发送缓冲 $s_(r,t)$、量化配置], [量化缓冲 $hat(s)_(r,t)$、量化残差统计],
    [模块 2：流水化集合通信], [分块张量、通信组信息、stream/event], [异步句柄 $h_t$、分块完成时间序列],
    [模块 3：计算-通信掩盖], [micro-batch 进度、$T_"comm"$、$T_"comp"$], [截断点 $tau$、触发决策、回退标志],
    [统一状态层], [本步完整梯度、预测梯度], [误差缓冲 $e_(r,t+1)$、阈值更新量],
    table.hline(),
  ),
  caption: [三模块系统集成中的接口与状态变量约定]
) <tab:system-interface-contract>

该约定的意义在于将“算法策略”与“执行机制”解耦：策略层只决定何时触发、是否压缩，执行层只负责正确完成通信与同步，从而降低跨模块耦合度。

=== 运行时决策与回退机制

系统在每个 step 结束时更新一次策略状态，并在下一 step 生效。决策逻辑可概括为：

当 $T_"comm" <= T_"comp"(tau)$ 时，系统维持当前策略以优先保证稳定性；若连续多步出现 $T_"comm" > T_"comp"(tau)$，则先尝试提前 $tau$ 扩大重叠窗口。若该调整仍不足以覆盖通信尾部，系统才启用模块 1 进行压缩；当检测到通信瓶颈缓解后，策略会逐步降低压缩强度并回退到更保守路径。

这种“先调度、后压缩、可回退”的顺序能够优先利用不改变梯度表示的优化手段，在必要时再引入数据形态变换，符合工程上“先保守后激进”的稳定性原则。

=== 系统控制器主循环（伪代码）

为将上述策略流程落到可执行实现，算法@algo:system-controller 给出每个 step 的统一控制器主循环。其关键点在于：策略更新与执行解耦（本步观测，下一步生效）、通信句柄与误差缓冲在步末统一收敛，从而避免并发路径上的语义冲突。

#figure(
  kind: "algorithm",
  placement: top,

  pseudocode-list(booktabs: true, numbered-title: [算法 1：三模块协同系统控制器主循环], full: true)[
    - *输入状态：* $theta_t$，$e_(r,t)$，网络与计算 profiling 缓冲
    - *输出：* 更新后的参数 $w_(t+1)$ 与状态 $theta_(t+1)$

    + *for* 每个 step $t$ *do*
      + 采样 $T_"comm"$、$T_"comp"(tau)$ 并更新滑动统计
      + $a_t <- "PolicyDecision"(theta_t, T_"comm", T_"comp")$
      + *for* 每个 micro-batch $m = 1, ..., M$ *do*
        + 执行前向/反向并累积梯度
        + *if* $m = tau$ *then*
          + 根据 $a_t$ 选择是否启用模块 1 压缩
          + 在通信 stream 上调用模块 2 启动异步 All-Reduce，记录 $h_t$
        + *end*
      + *end*
      + *wait* $(h_t)$ 并执行参数更新
      + 更新误差缓冲 $e_(r,t+1)$ 与回退标志
      + $theta_(t+1) <- "PolicyUpdate"(theta_t, "观测指标", a_t)$
    + *end*
  ],
) <algo:system-controller>

结合 @tab:system-notation 可见，该控制器并未改变优化器主语义，而是通过动作 $a_t$ 在通信路径与触发时机上进行受控调整。这也是系统能在“可解释性”与“收益性”之间取得平衡的关键。

=== 工程实现细节与开销讨论

在工程实现中，系统主要新增三类开销：

新增开销主要来自三个方面：一是时延采样与滑动统计带来的 profiling 成本，通常显著低于一次跨域 All-Reduce；二是误差缓冲、策略变量与事件对象管理引入的状态维护成本，表现为额外显存占用与少量主机侧调度负担；三是压缩路径开关与分块参数变更带来的短时重配置成本。

实验中通过降低 profiling 频率、限制策略更新步长与使用迟滞切换，能够将上述开销控制在可接受范围内，使其不抵消三模块协同收益。

== 实验与验证

为验证“三模块作为一个系统”的有效性，本节从系统级视角给出最终测试项与结果汇总。前文分章实验分别验证了单模块与局部组合能力，本节关注端到端可交付性：性能收益、收敛稳定性与工程一致性是否可同时成立。

=== 测试目标与判据

系统级最终测试覆盖以下四类判据：

判据体系同时覆盖性能、优化与工程三条主线：性能侧关注端到端 tokens/s 或 samples/s 相对基线是否提升，优化侧关注 step 对齐 loss 曲线是否与基线保持一致并避免明显漂移，同时通过 wall-clock 曲线评估 time-to-target 是否实质缩短；工程侧则验证训练循环、通信句柄和误差缓冲更新链路能否长期稳定运行且不破坏训练语义。

=== 最终测试流程与测试矩阵

为保证测试结论可复核，系统级验证按照“功能正确性 -> 性能收益 -> 稳定性 -> 退化行为”顺序执行：

为保证结论可复核，系统级验证采用分层递进流程：先进行功能正确性测试，检查异步句柄生命周期、步末同步点与误差缓冲更新顺序是否满足约束；再在固定模型与 batch 条件下开展性能测试，对比基线与协同系统的吞吐、迭代时间和通信占比；随后通过 step 对齐与 wall-clock 对齐的 loss 曲线评估稳定性；最后在更高带宽或更低 RTT 条件下执行退化测试，确认系统能够自动回退并保持与基线一致行为。

测试矩阵覆盖三类网络区间：

区间 A（弱受限）中通信大体可被计算掩盖，重点观察回退机制是否合理；区间 B（中度受限）中通信与计算接近，重点考察阈值策略稳定性及抗抖动能力；区间 C（强受限）中通信显著主导，重点验证三模块叠加后是否仍具可持续收益。

=== 最终测试结果汇总（来自第3-5章）

// Data source: image/csv/ch6_tab_final_system_validation.csv
#figure(
  table(
    columns: (auto, auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*测试维度*], [*结论*], [*证据来源*]),
    table.hline(stroke: 0.5pt),
    [集合通信调度收益], [流水线层次化相对 PyTorch 原生实现 1.35× 端到端加速], [第 4 章 @tab:e2e-speedup],
    [跨域受限场景吞吐], [POLAR-SGD 在 Cross-DC 约束下达到 1.87× 相对加速], [第 5 章 @tab:polar-throughput],
    [收敛稳定性], [step 对齐曲线与基线高度一致，未出现明显漂移], [第 5 章 @fig:polar-loss-steps],
    [time-to-target], [wall-clock 对齐曲线更快进入低 loss 区间], [第 5 章 @fig:polar-loss-time],
    table.hline(),
  ),
  caption: [三模块协同系统的最终测试结果汇总]
) <tab:final-system-validation>

综合上述结果可见，三模块在统一系统框架下并非相互替代，而是具备互补关系：模块 1 降低通信量，模块 2 优化通信路径与调度，模块 3 缩短关键路径尾部。现有证据表明，各模块在对应场景下均可带来稳定收益，并共同支持“性能提升 + 收敛稳定 + 工程可部署”的系统目标。

从维度 1（集合通信调度收益）看，第 4 章给出的 1.35× 端到端加速说明“通信执行层”优化具有独立贡献。该结果与本章分层架构中的模块 2 职责一致，即通过拓扑匹配与时序流水化降低同步等待。该证据的价值在于验证系统底座有效：即便不引入更激进策略，仅靠通信执行层优化也可获得稳定收益。

从维度 2（跨域受限场景吞吐）看，第 5 章 1.87× 相对加速说明“触发时机重排 + 误差修正”能够在高 RTT 约束下进一步压缩关键路径。该结果对应模块 3 的核心作用，即将同步从迭代尾部前移到可重叠窗口，减少尾部阻塞。与维度 1 结合后可看出：系统并非只有单一优化杠杆，而是存在“执行层优化 + 时机层优化”的分层增益来源。

从维度 3（收敛稳定性）看，step 对齐曲线与基线接近，表明系统收益并非由明显优化语义偏移换取。对工程部署而言，这一证据与性能指标同等重要：若性能提升伴随显著漂移，则训练风险会在长周期任务中累计放大。当前结果说明系统在收益与稳定之间保持了可接受平衡。

从维度 4（time-to-target）看，wall-clock 对齐曲线更快进入目标 loss 区间，说明系统侧优化最终转化为训练周期缩短。该指标把“通信加速”“迭代缩短”“收敛保持”三者连接起来，是系统级有效性的最终体现。换言之，本章不是在单点指标上追求局部最优，而是验证了端到端目标的实质改善。

结合四个维度可形成系统级证据链：通信执行层给出基础收益，关键路径重排提供额外增益，收敛稳定性约束确保语义可控，time-to-target 则验证业务目标改善。该证据链与本章提出的“监测-判定-执行-反馈”闭环相一致，说明系统设计与实验结果在逻辑上相互支撑。

需要指出的是，现有证据主要来自分模块或分场景验证，尚未覆盖全部组合参数下的联合最优搜索；但在“可运行、可解释、可回退”的工程目标下，当前结果已足以证明三模块协同框架具备实际应用价值。后续工作可在此基础上进一步补充更大规模联合消融与自动调参实验，以强化最优性结论。

从测试逻辑上看，以上证据链同时覆盖了“快”和“稳”两个维度：第 4 章结果证明通信执行层的基础调度收益，第 5 章结果证明在高 RTT 约束下关键路径可进一步压缩；loss 曲线结果则说明该收益并非通过破坏训练语义获得。换言之，系统级结论不是单一指标上的偶然提升，而是多指标一致的综合改进。

进一步从跨章一致性看，第 3 章在不同网络条件下给出的量化收益区间，与第 4 章调度优化在中高通信占比区间的增益趋势并不冲突，二者共同指向同一事实：当通信成为主导瓶颈时，任何仅依赖单点优化的方案都会较快触及上限，而“数据量压缩 + 路径调度优化 + 触发时机重排”的组合更容易形成可持续收益。第 6 章汇总结果恰好提供了这一跨章闭环证据，即前述模块级结论可以在统一系统中被同时保留而非相互抵消。该点对学位论文结论尤为关键，因为它把“章节内成立”提升为“系统级成立”，显著增强了结论外推到实际部署场景时的可信度。

=== 结果讨论：适用边界与失效模式

尽管系统在跨域受限网络下表现稳定，仍存在明确适用边界：

当模型计算量较小且通信量本身不高时，策略层的可优化空间会显著缩小；当网络抖动表现为强突发且难预测时，短时收益可能出现波动；当 micro-batch 数过少时，可重叠窗口不足，也会限制模块 3 的作用上限。

对应地，系统提供两类失效保护：一是通过阈值驱动回退到保守同步路径，以避免持续误判；二是限制策略切换频率，以防止过度自适应引入额外不稳定性。该设计使系统在非理想网络下仍可维持“可运行、可解释、可回退”的工程属性。

=== 可复现性与部署建议

为便于后续复现与部署，交付材料如下：

固定版本的软件栈（PyTorch、CUDA、NCCL）及关键环境变量、统一的 profiling 日志格式（含 $T_"comm"$、$T_"comp"$、$tau$ 与策略状态）、以及与 @tab:final-system-validation 对齐的最小测试脚本和验收阈值。

== 本章小结

本章将前文三个优化模块整合为一个运行时协同系统，并从设计目标、分层架构、接口契约、决策机制与测试矩阵五个方面补充了系统化细节。最终实验与测试结果表明，所提方案可在保持训练语义与收敛稳定性的前提下，持续缓解跨域同步瓶颈，具备面向云边端协同训练场景的整体落地价值。

#pagebreak()
