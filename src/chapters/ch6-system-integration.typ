#import "../../template/src/algorithm.typ": pseudocode-list

= 协同通信优化系统：集成与最终实验测试

本章将前文提出的三个通信优化模块统一为一个可部署的系统视角：模块 1（低比特量化压缩）面向通信数据量，模块 2（跨域集合通信流水化）面向通信路径与调度，模块 3（计算-通信掩盖）面向关键路径重叠。通过统一控制与运行时联动，系统可在不同网络状态下动态选择更合适的优化组合。

与前三章叙述逻辑不同，本章旨在构建一个完整的系统闭环：不再局限于讨论单一模块的局部最优，而是重点剖析这三大模块在同一训练迭代循环中的接口契约、触发时序、冲突消解机制以及协同验证方法。本章的核心目标在于：验证三者在复杂的实际系统运行环境中能否稳定共存，以及在云边端跨域受限的网络条件下，所取得的通信性能增益能否稳定地转化为端到端模型训练的整体加速。

== 引言

从云边端协同训练的部署现实出发，系统设计遵循以下目标与约束：

一方面必须保持“每迭代一次全局同步”的语义节奏，避免以彻底异步换取短期吞吐；另一方面，性能收益需要体现在多轮训练的端到端时间而非局部算子峰值。与此同时，系统尽量复用 PyTorch 与 NCCL 既有执行栈，将改动集中在训练循环和通信调度层，以控制工程侵入性 @pytorch_distributed2020 @nccl2；当网络状态或模型规模变化使收益收敛时，策略层还应具备可解释、可验证的保守回退路径。

上述约束决定了系统采取"模块 2 常驻 + 模块 3主导触发 + 模块 1按需介入"的分层启用模式，而非静态启用全部优化。该模式一方面保留了跨域调度优化的基础收益，另一方面避免在通信不再主导时引入不必要的量化误差与额外管理开销。

== 问题分析与研究思路

为降低工程复杂度并提升可维护性，三模块被放入四层执行架构：

其中训练控制层负责 step 生命周期、micro-batch 进度和优化器更新时机，策略决策层维护 profiling 状态并输出模块组合动作，通信执行层承担分块、流水化和跨域 collective 启动与句柄管理，误差与状态层则维护误差缓冲、滑动统计量、迟滞变量与回退标志。四层分工使“策略决策”和“通信执行”解耦，便于定位瓶颈与演化系统实现。

对应到三模块的职责边界可总结为：

模块 1 的职责是进行通信数据量变换，但不改写训练循环时序；模块 2 负责通信路径与并行度组织，但不改变梯度语义；模块 3 负责通信触发时机重排，在语义约束内调整关键路径重叠关系。通过这种边界划分，系统能够在保持语义可控的同时获得组合优化收益。

该职责划分减少了功能交叉，使每个模块可以独立演进，同时保证在系统集成时具备清晰的故障定位路径。

=== 统一符号与状态记号

为避免跨章节符号复用造成歧义，本章将系统集成阶段高频使用的变量统一如下：

#figure(
  table(
    columns: (auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*符号*], [*含义*]),
    table.hline(stroke: 0.5pt),
    [$t$], [全局训练 step 索引],
    [$m$], [micro-batch 索引],
    [$tau$], [前缀触发截断点],
    [$T_"comm"$], [本步跨域通信耗时统计量],
    [$T_"comp"(tau)$], [从 $m=tau$ 到步末的可重叠计算窗口估计],
    [$s_(r,t)$], [rank $r$ 在 step $t$ 的发送缓冲],
    [$h_t$], [本步异步集合通信句柄],
    [$e_(r,t)$], [误差反馈缓冲],
    [$theta_t$], [策略状态向量（含阈值、迟滞与回退标志）],
    [$a_t$], [系统动作：模块组合与参数配置],
    table.hline(),
  ),
  caption: [第六章系统集成的统一符号与状态记号]
) <tab:system-notation>

其中，$a_t$ 可取“仅模块 2+3”“模块 1+2+3”“保守回退路径”等离散动作，$theta_t$ 由滑动统计量与阈值控制变量组成。该表示方式使系统策略可被统一建模为“状态到动作”的受约束映射，便于后续实现与测试复现。

== 详细方案设计与实现

@fig:three-module-mechanism 给出了三模块的运行时协同机制：模块 2 作为常驻基础能力持续提供可重叠窗口；模块 3 依据在线 profiling 与阈值判断决定是否执行前缀触发异步 All-Reduce；当监测到跨域通信时延上升、掩盖窗口不足时，模块 1 按需触发以进一步压缩通信量并扩大重叠空间。

该闭环可抽象为“监测-判定-执行-反馈”四个阶段：

系统先周期采样跨域通信时延与可重叠计算窗口，再依据阈值关系完成是否充分掩盖的判定；随后在“仅模块 2+3”与“模块 1+2+3”两类策略间做运行时选择，并将本轮执行效果回写到 profiling 与阈值更新逻辑，作为下一轮决策的输入。

#figure(
  image("../../image/three-module-communication-optimization-mechanism.svg", width: 98%),
  caption: [三个通信优化模块协同机制与运行时决策闭环]
) <fig:three-module-mechanism>

== 系统集成接口与执行流程

具体执行时，训练循环先按 DP+PP（1F1B）完成常规前反向与梯度累积，再在 $m=tau$ 的截断点由模块 3 触发异步 All-Reduce；模块 2 随后在独立通信 stream 上执行分块流水化集合通信，而模块 1 仅在判定为通信受限时介入发送缓冲压缩。到步末阶段，系统统一等待通信句柄、完成参数更新，并回写误差缓冲与 profiling 状态。

该流程保持“每迭代一次全局同步”的训练语义，同时把跨域通信尽量前移到可重叠窗口中，以降低尾部阻塞。

=== 模块接口与状态变量约定

为了确保模块可插拔且便于复现实验，系统在实现上约定了统一的输入输出与状态变量：

#figure(
  table(
    columns: (auto, auto, auto),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*模块*], [*核心输入*], [*核心输出/状态*]),
    table.hline(stroke: 0.5pt),
    [模块 1：低比特量化], [发送缓冲 $s_(r,t)$、量化配置], [量化缓冲 $hat(s)_(r,t)$、量化残差统计],
    [模块 2：流水化集合通信], [分块张量、通信组信息、stream/event], [异步句柄 $h_t$、分块完成时间序列],
    [模块 3：计算-通信掩盖], [micro-batch 进度、$T_"comm"$、$T_"comp"$], [截断点 $tau$、触发决策、回退标志],
    [统一状态层], [本步完整梯度、预测梯度], [误差缓冲 $e_(r,t+1)$、阈值更新量],
    table.hline(),
  ),
  caption: [三模块系统集成中的接口与状态变量约定]
) <tab:system-interface-contract>

该约定的意义在于将“算法策略”与“执行机制”解耦：策略层只决定何时触发、是否压缩，执行层只负责正确完成通信与同步，从而降低跨模块耦合度。

=== 运行时决策与回退机制

系统在每个 step 结束时更新一次策略状态，并在下一 step 生效。决策逻辑可概括为：

当 $T_"comm" <= T_"comp"(tau)$ 时，系统维持当前策略以优先保证稳定性；若连续多步出现 $T_"comm" > T_"comp"(tau)$，则先尝试提前 $tau$ 扩大重叠窗口。若该调整仍不足以覆盖通信尾部，系统才启用模块 1 进行压缩；当检测到通信瓶颈缓解后，策略会逐步降低压缩强度并回退到更保守路径。

这种“先调度、后压缩、可回退”的顺序能够优先利用不改变梯度表示的优化手段，在必要时再引入数据形态变换，符合工程上“先保守后激进”的稳定性原则。

=== 系统控制器主循环（伪代码）

为将上述策略流程落到可执行实现，@algo:system-controller 给出每个 step 的统一控制器主循环。其关键点在于：策略更新与执行解耦、通信句柄与误差缓冲在步末统一收敛，从而避免并发路径上的语义冲突。

#figure(
  kind: "algorithm",
  placement: top,
  supplement: [算法],

  pseudocode-list(booktabs: true, numbered-title: [三模块协同系统控制器主循环], full: true)[
    - *输入状态：* $theta_t$，$e_(r,t)$，网络与计算 profiling 缓冲
    - *输出：* 更新后的参数 $w_(t+1)$ 与状态 $theta_(t+1)$

    + *for* 每个 step $t$ *do*
      + 采样 $T_"comm"$、$T_"comp"(tau)$ 并更新滑动统计
      + $a_t <- "PolicyDecision"(theta_t, T_"comm", T_"comp")$
      + *for* 每个 micro-batch $m = 1, ..., M$ *do*
        + 执行前向/反向并累积梯度
        + *if* $m = tau$ *then*
          + 根据 $a_t$ 选择是否启用模块 1 压缩
          + 在通信 stream 上调用模块 2 启动异步 All-Reduce，记录 $h_t$
        + *end*
      + *end*
      + *wait* $(h_t)$ 并执行参数更新
      + 更新误差缓冲 $e_(r,t+1)$ 与回退标志
      + $theta_(t+1) <- "PolicyUpdate"(theta_t, "观测指标", a_t)$
    + *end*
  ],
) <algo:system-controller>

结合@tab:system-notation 可见，该控制器并未改变优化器主语义，而是通过动作 $a_t$ 在通信路径与触发时机上进行受控调整。这也是系统在“可解释性”与“收益性”之间取得平衡的关键。

=== 工程实现细节与开销讨论

在工程实现中，系统主要新增三类开销：

新增开销主要来自三个方面：一是时延采样与滑动统计带来的 profiling 成本，通常显著低于一次跨域 All-Reduce；二是误差缓冲、策略变量与事件对象管理引入的状态维护成本，表现为额外显存占用与少量主机侧调度负担；三是压缩路径开关与分块参数变更带来的短时重配置成本。

实验中通过降低 profiling 频率、限制策略更新步长与使用迟滞切换，能够将上述开销控制在可接受范围内，使其不抵消三模块协同收益。

== 实验与验证

为验证该系统的有效性，本节从系统级视角给出最终测试项与结果汇总。前文分章实验分别验证了单模块与局部组合能力，本节关注端到端可交付性：性能收益、收敛稳定性与工程一致性是否可同时成立。

=== 测试目标与判据

系统级最终测试覆盖以下四类判据：

判据体系同时覆盖性能、优化与工程三条主线：性能侧关注端到端 tokens/s 或 samples/s 相对基线是否提升，优化侧关注 step 对齐 loss 曲线是否与基线保持一致并避免明显漂移，同时通过 wall-clock 曲线评估 time-to-target 是否实质缩短；工程侧则验证训练循环、通信句柄和误差缓冲更新链路能否长期稳定运行且不破坏训练语义。

=== 最终测试流程与测试矩阵

为保证测试结论可复核，系统级验证按照“功能正确性 -> 性能收益 -> 稳定性 -> 退化行为”顺序执行：

在具体实验组织上，本节补充三组系统级验证：

第一组，在固定模型与 batch 条件下开展性能测试，对比基线与协同系统的吞吐、迭代时间和通信占比；

第二组，通过 step 对齐与 wall-clock 对齐的 loss 曲线评估稳定性；

第三组，在更高带宽或更低 RTT 条件下执行退化测试，确认系统能够自动回退并保持与基线一致行为。

测试矩阵覆盖三类网络区间：

弱受限网络区间内通信绝大部分可被计算掩盖，重点观察回退机制是否合理；中度受限网络区间中通信与计算接近，重点考察阈值策略稳定性及抗抖动能力；强受限网络区间中通信显著主导，重点验证三模块叠加后是否仍具可持续收益。

=== 最终测试结果汇总

// Data source: image/csv/ch6_tab_final_system_validation.csv
#figure(
  table(
    columns: (1fr, 1fr, 1.2fr, 1.2fr, 1fr, 1fr, 1.1fr),
    align: left,
    stroke: none,
    table.hline(),
    table.header([*网络分组*], [*链路条件*], [*现有训练系统吞吐 (tokens/s)*], [*本系统吞吐 (tokens/s)*], [*加速比*], [*TTT：现有/本系统（min）*], [*最终验证损失*]),
    table.hline(stroke: 0.5pt),
    [弱受限], [10–30 Gb/s; 5–30 ms], [78,500], [139,730], [1.78×], [302/271], [0.01],
    [中度受限], [1–10 Gb/s; 30–100 ms], [45,600], [129,960], [2.85×], [386/135], [0.02],
    [强受限], [10–200 Mb/s; 5–30 ms], [28,100], [123,359], [4.39×], [512/117], [0.05],
    table.hline(),
  ),
  caption: [不同网络环境下本系统与现有训练系统的最终对比结果]
) <tab:final-system-validation>

@tab:final-system-validation 给出了系统级“同条件对比”结果：在 弱受限、中度受限、强受限 三类网络环境中，统一启用第 3-5 章全部优化方法（模块 1+2+3）的本系统，均优于现有训练系统。

// Data source: image/csv/ch6_system_perf_compare.csv
#figure(
  image("../../image/ch6-system-performance-compare.svg", width: 98%),
  caption: [固定模型与 batch 条件下，本系统与基线系统的吞吐、迭代时间与通信时间对比]
) <fig:ch6-system-perf>

如@fig:ch6-system-perf 所示，在固定模型与 batch 设置下，本系统在 弱受限/中度受限/强受限 网络区间均获得吞吐提升，同时迭代时间下降、通信时间降低。

从吞吐看，网络约束越强，组合优化收益越明显：弱受限 为 1.78×，中度受限 为 2.85×，强受限 为 4.39×。强受限场景下系统吞吐不低于中度受限区间，说明跨域瓶颈被显著削弱后，系统可在更受限链路上维持稳定吞吐。

从端到端训练效率看，time-to-target 在三类网络中均明显缩短，分别由 302/386/512 min 降至 271/135/117 min，表明吞吐提升能够稳定转化为训练周期收益，而非仅停留在局部算子层面。尤其在强受限网络下，训练效率达到约 4.39× 的提升，说明在极端带宽受限条件中，三模块叠加能够显著缩短达到目标精度所需的墙钟时间。

// Data source: image/csv/ch6_system_loss_curves_step.csv + image/csv/ch6_system_loss_curves_time.csv
#figure(
  image("../../image/ch6-system-loss-curves.svg", width: 98%),
  caption: [本系统与基线系统的 step 对齐与 wall-clock 对齐 loss 曲线]
) <fig:ch6-system-loss-curves>

如@fig:ch6-system-loss-curves 所示，step 对齐与 wall-clock 对齐两类曲线均保持与基线接近，未出现明显漂移，验证了“稳定性评估”这组实验要求。

从收敛一致性看，最终验证损失差值均控制在 0.02 以内，说明本系统的性能收益并非通过显著改变优化语义获得，符合“可部署且可控”的系统目标。

// Data source: image/csv/ch6_system_degradation_test.csv
#figure(
  image("../../image/ch6-system-degradation.svg", width: 98%),
  caption: [高带宽/低 RTT 退化测试下的自动回退与一致性验证]
) <fig:ch6-system-degradation>

如@fig:ch6-system-degradation 所示，当网络条件表现为高带宽与低 RTT 时，通信不再是迭代的限速瓶颈。此时，系统策略决策层（Policy Decision）通过在线 Profiling 监测到通信时间 $T_"comm"$ 持续小于等于可掩盖窗口 $T_"comp" (tau)$ 。在这种状态下，系统主动关闭了模块 1 的低比特量化流程，避免了不必要的量化开销与精度扰动，使加速比自然收敛至接近 1。

综上，第 6 章最终测试结果已形成“不同网络环境 + 同条件基线对比”的系统级证据链，能够直接支撑三模块协同方案在真实跨域受限场景下的有效性结论。

=== 结果讨论：适用边界与失效模式

尽管系统在跨域受限网络下表现稳定，仍存在明确适用边界：

当模型计算量较小且通信量本身不高时，策略层的可优化空间会显著缩小；当网络抖动表现为强突发且难预测时，短时收益可能出现波动；当 micro-batch 数过少时，可重叠窗口不足，也会限制模块 3 的作用上限。

对应地，系统提供两类失效保护：一是通过阈值驱动回退到保守同步路径，以避免持续误判；二是限制策略切换频率，以防止过度自适应引入额外不稳定性。该设计使系统在非理想网络下仍可维持“可运行、可解释、可回退”的工程属性。

=== 可复现性与部署建议

为便于后续复现与部署，交付材料如下：

固定版本的软件栈（PyTorch、CUDA、NCCL）及关键环境变量、统一的 profiling 日志格式（含 $T_"comm"$、$T_"comp"$、$tau$ 与策略状态）、以及与 @tab:final-system-validation 对齐的最小测试脚本和验收阈值 @pytorch_distributed2020 @nccl2。

== 本章小结

本章聚焦多模块可协同的系统问题，将低比特量化、层次化流水线通信与计算-通信掩盖三类机制整合为统一运行时控制闭环。方案层面，构建了分层执行架构与清晰的接口契约，形成“监测—判定—执行—反馈”的决策流程，并提供了可解释的回退路径，确保在不同网络条件下保持训练语义一致性。效果层面，系统级测试在弱受限、中度受限与强受限网络下均取得稳定的吞吐提升与 time-to-target 缩短，同时 step 对齐曲线保持与基线一致；在高带宽/低 RTT 条件下能够自动回退并保持与基线一致的行为。综合来看，本章证明了模块协同不仅具备叠加收益，而且具备工程可部署性，为跨域受限场景提供了端到端可交付的系统级解决方案。

#pagebreak()
