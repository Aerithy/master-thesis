#import "../../template/src/algorithm.typ": pseudocode-list

= 基于流水线的层次化梯度通信调度策略

== 引言

=== 异构集群通信特征

在分布式深度学习训练中，梯度同步的通信开销已成为制约训练效率的关键瓶颈 @pytorch_distributed2020。特别是在多节点集群环境中，通信架构表现出明显的异构性：节点内（Intra-node）通信通过高速 NVLink 或 PCIe 完成，带宽可达数百 GB/s；而节点间（Inter-node）通信受限于以太网或 InfiniBand 带宽，通常仅为 10-100 Gbps @nvidia_nvlink_v3_2021 @mellanox_hdr200。这种显著的带宽不对称性（Bandwidth Asymmetry），通常达到 10:1 甚至 50:1。这使得跨节点通信成为系统的主要性能短板。

在云边端协同训练中，这种异构性会进一步“跨域化”：云内与边缘域内往往具备较高带宽互联（如 RDMA/以太网集群），但边缘到云、边缘到边缘的跨域链路带宽更低且 RTT 更高；端到边链路还可能受到无线接入与动态拥塞影响，抖动显著。因而，梯度同步往往需要跨越至少一条“低带宽/高 RTT”的跨域链路，导致同步尾部更突出，也使得“如何在分层拓扑上合理调度 collective”成为云边端协同训练的关键系统问题。

#figure(
  image("../../image/ch4-comm-asymmetry.svg", width: 95%),
  caption: [异构带宽不对称性导致的通信瓶颈]
) <fig:comm-asymmetry>

=== 传统同步算法的局限

传统的 All-Reduce 操作（如 Ring All-Reduce 或 Tree All-Reduce）通常采用同步阻塞方式，所有进程必须在此阶段等待通信完成才能继续进行下一轮迭代的计算 @rabenseifner2004。在异构网络环境下，这种扁平化的通信模式会导致高速的节点内互联被低速的节点间链路拖累，造成计算资源闲置。

为解决上述问题，本研究提出了一种*基于流水线的层次化 All-Reduce 方法*（Pipelining Hierarchy All-Reduce），通过张量分块、双流并行和层次化通信策略，实现计算与通信的重叠，从而降低梯度同步延迟。


== 问题分析与研究思路

在分布式深度学习训练任务中，梯度同步作为迭代收敛的必要环节，其通信效率直接决定了整体训练吞吐。当训练任务部署于异构集群时，节点内高速互联与节点间低速链路形成的带宽差异，使得在梯度同步的过程中，集合通信无法充分利用带宽，从而导致训练各进程因等待跨节点通信完成而长时间等待，而高速域内带宽却因受限于低速域间带宽无法被充分利用。造成该现象的原因可归结为以下两个方面：

一方面，传统 All-Reduce 算法（如 Ring 或 Tree 拓扑）采用扁平化的通信组织方式，将所有参与进程置于同一逻辑平面进行数据交换 @rabenseifner2004。这种设计在同构网络环境下能够充分发挥集体通信的理论带宽，但在带宽不对称的异构集群中，慢链路会成为整个通信组的瓶颈。具体而言，当节点间链路带宽仅为节点内的 1/10 甚至 1/50 时，跨节点数据传输耗时将主导同步总时间，而节点内高速互联在完成本地聚合后需被动等待，造成计算资源与通信资源的双重闲置。

另一方面，现有通信优化方案在应对异构拓扑时存在适应性局限。梯度压缩类方法 @dgc2018 @bytepscompress2021 通过降低传输数据量缓解带宽压力，但压缩/解压缩引入的计算开销在算力密集的训练场景中可能抵消通信收益；异步通信策略虽能隐藏部分延迟，但梯度陈旧性可能影响模型收敛稳定性；更为关键的是，上述方法大多将通信视为原子操作进行整体优化，未能从时序维度挖掘"计算 - 通信"与"域内 - 域间"的双重并行潜力。

然而不同于通用的分布式任务调度，梯度同步通信具有强数据依赖与严格一致性约束：本地归约必须在跨节点交换前完成，全局聚合结果必须在广播前就绪。这种"阶段串行、块间并行"的特性，使得简单的任务重排或资源隔离策略难以直接适用。同时，深度学习模型的梯度张量通常具有较大尺寸（数百 MB 至数 GB），若分块粒度过粗则流水线并发度不足，若分块过细则集合通信原语的启动开销与事件同步成本将显著累积。这种"并行深度 - 调度开销"的权衡关系，为异构环境下的通信调度带来了额外的参数敏感性与调优复杂度。

因此，在带宽不对称的异构集群中进行梯度同步时，需同时满足三重目标：在拓扑层匹配"域内快、域间慢"的物理互联特征，在时序层实现计算与通信、域内与域间操作的有效重叠，在控制层保障数据依赖正确性与工程可实现性。现有方案或侧重拓扑适配而忽略时序优化，或追求并行重叠而增加同步复杂度，尚未形成层次化与流水线协同的系统性设计。

本章所研究问题的主要难点与挑战在于：

（1）如何在保障数据一致性的前提下实现层次化通信的流水线重叠。梯度同步的三阶段（本地归约→跨节点归约→本地广播）存在严格的前后依赖，需设计轻量且精确的同步原语，在解耦域内/域间操作的同时避免引入额外等待。

（2）如何动态选择分块粒度以平衡并行收益与调度开销。块大小直接影响流水线深度与集合通信启动频率，需建立与张量规模、网络带宽、硬件特性相适应的自适应策略，避免静态配置在异构场景下的性能波动。

（3）如何将算法机制无缝集成至主流训练框架。优化方案需在最小侵入的前提下兼容 PyTorch DDP 等现有接口，支持梯度累积、混合精度等常见训练模式，并提供降级路径以保障系统鲁棒性。

针对以上问题，本文研究了一种基于流水线的层次化 All-Reduce 调度策略。该方法首先将通信拓扑抽象为"本地组 - 跨节点组"两级结构，将慢链路通信压缩至代表进程路径；进而通过张量分块构建预热 - 主流水 - 冷却三阶段执行框架，利用双流并行与 CUDA Event 依赖同步实现本地归约、跨节点归约与本地广播的时序重叠；最后结合梯度展平与累积步数感知机制，完成与训练循环的工程级集成。通过拓扑匹配、时序重排与系统协同的多层优化，本章方案旨在不改变优化语义的前提下，有效缓解异构集群中的通信尾部延迟，为云边端协同训练提供可叠加的通信加速基础。

=== 层次化通信拓扑

如@fig:hierarchical-topo 所示层次化通信拓扑，本方法将分布式训练环境划分为两层通信组：

其中，本地组（Local Group）由同一节点内的 GPU 进程构成，依赖 NVLink/PCIe 等高带宽互联完成域内聚合；跨节点组（Inter Group）则由各节点代表进程（通常为 Rank 0）组成，负责跨节点数据交换与同步。

#figure(
  image("../../image/ch4-hierarchical-topology.svg", width: 97%),
  caption: [层次化 All-Reduce 通信拓扑与数据流向]
) <fig:hierarchical-topo>

层次化通信将 All-Reduce 操作分解为三个阶段：

在执行顺序上，系统先在节点内完成本地归约（Local Reduce），再由代表进程执行跨节点归约（Inter Reduce），最后将聚合结果在节点内广播（Local Broadcast）到其余 GPU。该分解方式能够把慢链路上的同步数据量压缩到代表进程粒度，并将更多通信负载留在高带宽域内链路上。

从云边端协同的角度看，上述两层抽象可自然映射为“域内/域间”两级：Local Group 对应云内或边缘域内的高带宽通信组，Inter Group 对应跨域（边缘↔云或边缘↔边缘）的代表进程通信组。这样，层次化 All-Reduce 能将跨域通信压缩为少量代表进程的数据交换，同时尽可能把域内高带宽链路用于聚合与分发，从拓扑上契合云边端协同的分层互联特征。


=== 流水线分块策略

算法将待传输的梯度张量分割为 $N$ 个固定大小的块（chunks），记为 ${C_0, C_1, ..., C_(N-1)}$。分块粒度通过参数 `chunk_size` 控制，需平衡以下因素：

首先是流水线并发度约束。当块数过少时，流水线深度不足，难以有效隐藏跨节点通信延迟；其次是系统调度与启动开销。当块数过多时，集合通信原语的启动开销会显著累积，反而侵蚀并行收益。

实验中采用自适应分块策略：

$ "chunk_size" = "tensor_size" / (8 tilde 16) $

#figure(
  image("../../image/ch4-multistream-hierarchical-allreduce.png", width: 100%),
  caption: [流水线层次化 All-Reduce 时序图（ar: All-Reduce, bc: Broadcast）]
) <fig:pipeline-timing>

=== 双流并行机制

在底层硬件调度中，GPU 默认将提交的计算或通信任务排列在单一执行队列中串行执行，这导致不同层级的通信任务无法在物理时间上真正重叠。为充分利用硬件多并发能力，本算法显式创建了两个独立的 CUDA 流（CUDA Stream，即 GPU 上的独立异步指令队列）：

节点内流（Intra-stream）专用于调度处于高带宽互联下的本地归约与广播操作；节点间流（Inter-stream）则专用于调度处于低带宽链路的跨节点归约操作。由于这两种操作在使用网络资源侧重点不同（例如局部总线与跨设备网卡），它们在底层硬件上具备同时运作的客观条件。

为确保跨流操作的数据一致性（例如，必须在本地块归约完成之后，代表节点才能将该块数据发往外部进行跨节点归约），两条流之间引入了 CUDA Event 作为轻量级同步原语。借助该原语，算法在保障各数据块前后逻辑依赖绝对正确的前提下，最大化了两条通信链路的调度重叠率。

== 详细方案设计与实现

从执行行为上看，算法由预热、主流水和冷却三个连续阶段构成，如@fig:pipeline-timing 所示。

=== 阶段一：预热阶段（Warmup）

预热阶段启动前两个块的处理流程。首先启动第一个块的本地归约操作。

#figure(
  kind: "algorithm",
  // placement: top,
  supplement: [算法],

  pseudocode-list(booktabs: true, numbered-title: [预热阶段流水线启动], full: true)[
    - *输入：* $"chunks"$，$"local_group"$，$"inter_group"$，事件列表
    - *输出：* 初始化流水线依赖图

    + 在节点内流启动 $C_0$ 的本地归约：`all_reduce(chunks[0], group=local_group)`
    + 记录 `event_list_intra[0]`
    + 在节点内流启动 $C_1$ 的本地归约：`all_reduce(chunks[1], group=local_group)`
    + 记录 `event_list_intra[1]`
    + 在节点间流等待 `event_list_intra[0]`
    + 在节点间流执行 $C_0$ 跨节点归约：`all_reduce(chunks[0], group=inter_group)`
    + 执行 `barrier(local_group)` 保证同节点进度一致
    + 记录 `event_list_inter[0]`
  ],
) <code:warmup>

如@code:warmup 所示，该阶段的主要目的在于启动集合通信流水：第一个块完成本地归约后立即开始跨节点归约，同时第二个块开始本地归约，实现两个操作的并行执行。

=== 阶段二：流水线主循环（Pipelining）

对于剩余的块 $i in [2, N-1]$，并行执行三个操作：

#figure(
  kind: "algorithm",
  // placement: top,
  supplement: [算法],

  pseudocode-list(booktabs: true, numbered-title: [流水线主循环], full: true)[
    - *输入：* 块索引 $i in [2, N-1]$，事件列表，$"local_group"$，$"inter_group"$
    - *输出：* 完成所有中间块的重叠调度

    + *for* $i = 2$ *to* $N-1$ *do*
      + ▷ 节点内流：并行执行广播和本地归约
      + 等待 `event_list_inter[i-2]` #h(1em) ▷ 等待 $C_(i-2)$ 跨节点归约完成
      + `broadcast(chunks[i-2], group=local_group)` #h(1em) ▷ 广播 $C_(i-2)$
      + `all_reduce(chunks[i], group=local_group)` #h(1em) ▷ 本地归约 $C_i$
      + 记录 `event_list_intra[i]`
      + ▷ 节点间流：跨节点归约
      + 等待 `event_list_intra[i-1]` #h(1em) ▷ 等待 $C_(i-1)$ 本地归约完成
      + *if* 当前进程是节点代表 *then*
        + `all_reduce(chunks[i-1], group=inter_group)`
      + *end*
      + `barrier(local_group)`
      + 记录 `event_list_inter[i-1]`
    + *end*
  ],
) <algo:pipeline-main>

如@algo:pipeline-main 所示，该阶段是算法的核心，其并行性体现在同一时刻由节点内流处理块 $i-2$ 的广播与块 $i$ 的本地归约，同时由节点间流处理块 $i-1$ 的跨节点归约；通过 CUDA Event 的精确依赖同步，上述三个操作能够在保持正确性的前提下实现稳定重叠。

=== 阶段三：冷却阶段（Cooling）

处理最后两个块的广播和跨节点归约。

#figure(
  kind: "algorithm",
  // placement: top,
  supplement: [算法],
  pseudocode-list(booktabs: true, numbered-title: [冷却阶段收尾流程], full: true)[
    - *输入：* 事件列表与最后两个块索引
    - *输出：* 完成全部块的传播闭环

    + 等待 `event_list_inter[N-2]`
    + `broadcast(chunks[N-2], group=local_group)` #h(1em) ▷ 广播倒数第二块
    + 等待 `event_list_intra[N-1]`
    + `all_reduce(chunks[N-1], group=inter_group)` #h(1em) ▷ 跨节点归约最后一块
    + `barrier(local_group)`
    + 等待 `event_list_inter[N-1]`
    + `broadcast(chunks[N-1], group=local_group)` #h(1em) ▷ 广播最后一块
  ],
) <code:cooling>

如@code:cooling 所示，该阶段负责处理流水线末尾的收尾流程，确保最后两个块的跨节点归约与广播能够正确完成，最终实现所有块的全局同步。

== 理论性能与开销分析

=== 时间复杂度

传统 All-Reduce 方法的总时间为各阶段时间之和：

$ T_"total" = T_"local" + T_"inter" + T_"broadcast" $

而流水线 All-Reduce 方法通过并行执行三个操作，理论加速比为：

$ "Speedup" = (T_"local" + T_"inter" + T_"broadcast") / (max(T_"local", T_"inter", T_"broadcast") + O(N_"chunks")) $

当块数足够多且三个操作耗时相近时，可接近 1.5× 加速。

=== 空间复杂度

额外内存开销分析：额外开销主要来自 CUDA Event 对象管理，其规模约为 $O(N_"chunks")$；而张量分块本身采用视图操作，不引入新的实质性数据拷贝。PyTorch 等框架中的 Tensor Chunk 仅创建新的 Metadata 视图（View）而不拷贝底层显存，因此对动辄数十 GB 的模型梯度而言，仅有的几十个轻量级 CUDA Event 对象所占用的系统内存几乎可以忽略不计。因此总体空间复杂度仍为 $O(N_"chunks")$，在典型分块数（8-16）下可视为工程上可忽略的附加成本。

== 系统集成与工程优化

为将上述流水线层次化 All-Reduce 机制无缝融入现有的分布式深度学习框架（如 PyTorch DDP ），本研究在工程层面对训练循环进行了系统级重构，重点解决了计算流管控、内存连续化及梯度累积兼容等问题。

#figure(
  kind: "algorithm",
  // placement: top,
  supplement: [算法],
  
  pseudocode-list(booktabs: true, numbered-title: [流水线层次化 All-Reduce 集成], full: true)[
    - *输入：* 梯度列表 $"grads"$，本地步数 $"local_steps"$
    - *输出：* 聚合后的梯度缓冲

    + *if* $"global_iteration" mod "local_steps" = 0$ *then*
      + ▷ 累积梯度到发送缓冲区
      + *for* 每个梯度 $"grad"_i$ *do*
        + $"send_buffer"_i <- "grad"_i$
      + *end*
      + ▷ 展平所有梯度为连续张量
      + $"flat_buffer" <- "TensorBuffer"("send_buffers")$
      + *if* 启用流水线通信 *then*
        + `pipelining_all_reduce(`$"flat_buffer"$`,` $"local_group"$`,` $"inter_group"$`,` $"chunk_size"$`)`
      + *else*
        + `all_reduce(`$"flat_buffer"$`)`
      + *end*
    + *end*
  ],
) <algo:integration>

首先，为降低频繁触发底层通信原语所带来的系统调用与握手开销，本方法引入了梯度展平（Tensor Flattening）机制。在深度神经网络中，模型参数通常由众多尺寸不一的离散张量构成，直接对独立的参数梯段进行流水线分块不仅无法形成有效的并发深缩，反而会使通信栈过载。系统通过预先在发送缓冲区（Send Buffer）分配一块连续的底层显存空间，并在计算结束后将各层梯度视图按固定偏移拷贝至该大块缓冲中（`TensorBuffer`）。这不仅确保了后续流水线分块（Chunks）的物理连续性，也使通信带宽利用率得以大幅提升。

其次，对于采用梯度累积（Gradient Accumulation）的大规模训练场景，系统在设计上将通信触发时机与局步步数（Local Steps）深度绑定。如@algo:integration 所示，仅当 `global_iteration` 满足目标累积步数要求时，才会执行展平与随后的流水线同步逻辑。在其它的局部累加步骤中，各工作节点仅在本地进行正常的矩阵运算累加，通信栈保持静默。

最后，系统保留了原生扁平通信的后备降级路径（Fallback）。可以通过配置开关在并行流水线调度与传统的 `all_reduce` 之间进行运行时切换。这种透明化集成策略将通信协议管理的复杂度限制在底层引擎，对上层用户训练脚本而言并无侵入性。

#figure(
  image("../../image/ch4-system-integration-dataflow.svg", width: 98%),
  caption: [流水线调度器在训练流程中的集成架构]
) <fig:system-integration>

== 实验与验证

=== 实验环境

实验在以下环境中进行：实验平台采用 2 个计算节点、每节点 4 张 NVIDIA A100 GPU；节点内互联为 NVLink（600 GB/s），节点间互联为 InfiniBand（200 Gbps）@nvidia_a100_2020 @nvidia_nvlink_v3_2021 @mellanox_hdr200。任务侧采用 ResNet-50 与 ImageNet 组合，并设置每 GPU batch 为 256 @he2016resnet @deng2009imagenet。对比对象包括 PyTorch 原生 All-Reduce 与无流水线的朴素层次化 All-Reduce，以隔离“层次化收益”和“流水线收益”两类贡献。

为体现本文方法在云-边跨域互联中的适用性，除上述“局域高带宽集群”设置外，实验还采用网络整形对 Inter Group 对应的域间链路注入带宽/RTT 约束，用于复现边缘到云的广域网特征；Local Group 仍保持域内高带宽互联，以刻画典型的“域内快、域间慢”。该设置重点用于验证层次化流水线在跨域瓶颈下的调度收益与敏感性趋势。

// Data source: image/csv/ch4_tab_hierarchical_links.csv
#figure(
  table(
    columns: (1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*链路层级*], [*带宽（示例）*], [*RTT（示例）*]),
    table.hline(stroke: 0.5pt),
    [域内（云内/边缘域内）], [100–200 Gb/s], [< 1 ms],
    [域间（边缘↔云/边缘↔边缘）], [10–30 Gb/s], [30–100 ms],
    table.hline(),
  ),
  caption: [用于云边端协同场景的分层链路仿真参数]
) <tab:hierarchical-links>

=== 通信性能对比

@tab:comm-perf 展示了不同方法的通信性能对比。

// Data source: image/csv/ch4_tab_comm_perf.csv
#figure(
  table(
    columns: (1fr, 1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*方法*], [*通信耗时 (ms)*], [*相对基线*], [*GPU 利用率*]),
    table.hline(stroke: 0.5pt),
    [PyTorch 原生], [128.5], [1.00×], [68%],
    [朴素层次化], [91.3], [0.71×], [76%],
    [流水线层次化], [74.1], [0.58×], [89%],
    table.hline(),
  ),
  caption: [不同 All-Reduce 方法的通信性能对比]
) <tab:comm-perf>

#h(0em) 从结果看，流水线层次化方法相对 PyTorch 原生通信路径将通信耗时降低了 42.3%，并将 GPU 利用率从 68% 提升到 89%；即便与朴素层次化方法相比，流水线机制仍额外带来 18.8% 的通信时间缩减，说明收益不仅来自拓扑分层，还来自时序重排与并行重叠。

这一结果可以从"拓扑匹配 + 时序重叠"两个维度解释。朴素层次化主要解决的是拓扑不匹配问题，即将慢链路通信压缩到代表进程路径；流水线层次化则进一步重排执行时序，把本地归约、跨节点归约和本地广播并行化。两者差值反映的正是时序优化贡献，因此相较朴素层次化仍可获得额外下降。该差值源于算法机制本身的结构性设计，而非特定参数点的特异性。

GPU 利用率提升与通信耗时下降之间也形成因果闭环。同步通信缩短后，计算流等待通信完成的空转时间减少，GPU 能够更连续地执行前后向计算。由此，通信层优化直接转化为计算资源利用效率提升，而不仅仅是通信 micro-benchmark 的局部改进。这一点对端到端训练尤为关键，因为论文目标是整体训练效率而非单算子极值。

从系统稳定性角度看，通信耗时和利用率同时改善。若方法只提升短时峰值带宽而引入额外同步抖动，通常会在系统利用率或平均通信耗时上暴露副作用；当前结果未出现此类反向指标恶化，支持本章关于工程可部署性的结论。

=== 端到端训练加速

@tab:e2e-speedup 展示了端到端训练性能的提升。

#figure(
  image("../../image/ch4-e2e-results.svg", width: 98%),
  caption: [端到端训练性能对比与耗时分解]
) <fig:e2e-result>

// Data source: image/csv/ch4_tab_e2e_speedup.csv
#figure(
  table(
    columns: (1fr, 1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*方法*], [*迭代时间 (ms)*], [*吞吐指标 (items/s)*], [*加速比*]),
    table.hline(stroke: 0.5pt),
    [PyTorch 原生], [445.2], [4,602], [1.00×],
    [朴素层次化], [408.0], [5,024], [1.09×],
    [流水线层次化], [329.7], [6,216], [1.35×],
    table.hline(),
  ),
  caption: [端到端训练性能对比]
) <tab:e2e-speedup>

端到端结果进一步验证了通信优化能够转化为训练层面的实质收益。相较通信耗时指标，迭代时间和吞吐更接近业务目标，因此其改善更具说服力。数据表明方法从“减少通信等待”推进到“缩短训练迭代”，说明优化已进入关键路径而非停留在边缘环节。

将端到端收益与前述通信收益对照可发现，两者量级并非线性一致，这符合真实训练系统特征：迭代时间由计算、通信、数据流水等多环节共同决定，通信下降会被其余环节部分吸收。即便如此仍能获得显著加速，说明通信在当前设置中确实是主要瓶颈之一，本章提出的方法命中了高杠杆优化点。

此外，朴素层次化与流水线层次化形成了清晰的“基线-改进”递进关系，避免了仅与单一基线比较导致的解释歧义。先证明拓扑分层有效，再证明时序流水化在分层基础上继续增益，使结论链条更完整：收益来源可分解、可解释、可复现。

=== 块大小敏感性分析

@fig:chunk-sensitivity 展示了不同块大小对性能的影响。

#figure(
  image("../../image/ch4-chunk-sensitivity.svg", width: 98%),
  caption: [块大小对通信性能的影响]
) <fig:chunk-sensitivity>

#h(0em) 敏感性结果显示，块数在 8-16 区间时性能最优，对应块大小约为张量尺寸的 $1/8 tilde 1/16$；当块数低于 8 时，并行深度不足，通信难以被充分掩盖；而当块数超过 32 时，启动开销累积主导，整体性能反而下降。

块大小敏感性结果揭示了典型的“并行深度-调度开销”权衡。块数过少时，流水线阶段不足以覆盖跨节点归约延迟，重叠窗口利用不充分；块数过多时，collective 启动、事件同步和调度管理开销快速累积，抵消分块带来的并行收益。最优区间的存在说明该方法不能“分得越细越好”，而需要在系统开销模型下选择稳定工作点。

这一观察直接支撑了本章提出的自适应分块策略。固定块大小难以在不同张量规模和网络条件下同时最优，而以区间方式给出可行工作带，能够在工程部署中降低调参成本，并减少因环境变化导致的性能波动。对于跨域链路波动场景，采用区间内保守配置通常比追求单点最优更稳健。

=== 带宽不对称性影响

@tab:bandwidth-ratio-3factor 分析了节点内外带宽比在固定通信张量下，不同分块数的加速效果。

该实验采用三变量控制：

变量 1（网络条件）：节点内/节点间带宽比（5:1 ~ 30:1）；
变量 2（通信张量大小）：固定为 1024 MB；
变量 3（分块数）：1、2、4、8（对应不同 chunk 粒度）。

#h(0em) 结果表明：随着带宽不对称性提升，流水线层次化方案优势持续放大；在固定 1024 MB 通信张量下，chunk=4 与 chunk=8 在中高不对称区间取得更高增益，而 chunk=1 受并行深度不足限制。该现象表明分块粒度与网络条件存在显著耦合关系，合理选择 chunk 数是低质网络优化的关键。

// Data source: image/csv/ch4_tab_bandwidth_ratio_3factor.csv
#figure(
  image("../../image/ch4-bandwidth-ratio-3factor.svg", width: 96%),
  caption: [固定张量（1024 MB）下，不同 chunk 数在带宽不对称场景中的加速比]
) <tab:bandwidth-ratio-3factor>

带宽不对称实验给出了本章方法适用性的关键证据。若方法仅在近似同构网络下有效，则其跨域价值有限；当前结果显示不对称性增强时增益反而扩大，说明层次化流水线与“域内快、域间慢”的网络结构匹配。

chunk=4 与 chunk=8 在中高不对称区间表现更优，进一步说明跨域慢链路场景下需要足够流水深度来提升重叠效率，但不应无限细化到引入过高调度开销。该结论与块大小敏感性实验形成互证：参数选择应与网络结构联合考虑，而非脱离网络条件做静态配置。

=== 低质网络全面实验设计

为更全面验证层次化集合通信在低质网络下的有效性，补充 4 组针对性实验。四组实验统一采用三种对比方法（PyTorch 原生、朴素层次化、流水线层次化），并保持模型、batch、优化器与并行拓扑一致，仅改变网络条件或系统扰动因素。

实验统一采集四类指标：通信平均耗时、迭代时长、吞吐量、GPU 空闲占比；同时记录跨域链路的有效带宽利用率，用于解释性能差异来源。

==== 组 A：带宽退化实验（Bandwidth Throttling）

目标是验证在“纯带宽受限”场景下层次化流水线的稳健性，关注在 5-30 Gb/s 区间的收益保持能力。

// Data source: image/csv/ch4_tab_expA_bandwidth_throttle_3factor.csv
#figure(
  image("../../image/ch4-expA-bandwidth-throttle-3factor.svg", width: 96%),
  caption: [实验组 A：跨域带宽退化设置]
) <tab:exp-bandwidth-throttle>

组 A 的分析重点在于验证“纯带宽受限”条件下的收益保持能力。随着可用带宽逐级下降，跨域归约时延按近似 $1/{B W}$ 规律上升，流水线层次化通过分块重叠与代表进程路径压缩，能够持续降低有效等待时间。若该方法仅在高带宽区间有效，则在低带宽下应迅速失效；图中趋势显示其在退化区间仍保持可观优势，支持本章关于低质链路稳健性的结论。

该组实验还说明收益来源主要是“跨域路径效率提升”，而非偶然的本地计算波动。因为实验控制了模型与并行配置，仅改变带宽变量，性能变化可主要归因于通信路径。由此可将结论外推到同类型带宽受限场景：当跨域同步主导迭代尾部时，层次化流水线是有效优先策略。

==== 组 B：高时延实验（RTT Escalation）

// Data source: image/csv/ch4_tab_expB_rtt_escalation_3factor.csv
#figure(
  image("../../image/ch4-expB-rtt-escalation-3factor.svg", width: 96%),
  caption: [实验组 B：跨域 RTT 分级设置]
) <tab:exp-rtt-escalation>

目标是隔离 RTT 对同步尾部的影响，验证流水线调度是否能持续隐藏高 RTT 带来的阻塞。

组 B 用于隔离 RTT 对同步尾部的影响。高 RTT 场景下，collective 启动与轮次同步等待会被显著放大，传统扁平通信更易形成尾部串行堆叠。实验趋势表明，流水线层次化在 RTT 升高时仍能维持较优性能，说明其通过重叠执行与阶段解耦降低了时延放大效应。

该结果支撑了“跨域高时延场景优先做时序重排”的实践结论。与仅靠提升带宽相比，时序层优化对 RTT 的敏感性更低，因此在无法显著改善物理链路时，仍可通过调度层手段取得稳定收益。这一结论与第5章关于同步尾部治理的思路一致，体现章节间方法协同性。


==== 统计检验与可复现性约束

每个子实验至少运行 3 次独立重复并报告均值与标准差；对关键指标（迭代时长、吞吐量）执行配对显著性检验（如 paired t-test），保证结论不依赖单次偶然波动。网络整形参数、随机种子、日志脚本和绘图脚本统一纳入仓库，确保实验可复现与可审计。

== 技术优势与创新点

本方法的技术优势可概括为：在拓扑层通过层次化通信充分利用节点内高带宽与节点间低带宽的异构性，在时序层通过张量分块与双流机制实现本地归约、跨节点归约和本地广播的并行重叠，在同步层通过 CUDA Event 提供轻量且精确的依赖控制，在参数层通过自适应分块选择稳定工作点，并在实现层依赖零拷贝张量视图避免额外内存分配。多层协同使其既具理论合理性，也具工程可部署性。

== 局限性与未来工作

当前实现仍有边界条件。首先，算法以二到三层层次结构为核心设计，对更深层级拓扑尚缺直接支持；其次，块大小主要在运行前确定，尚未形成基于在线网络观测的动态重配置机制；此外，当节点内 GPU 数量或负载分布不均时，局部负载不平衡仍可能影响整体流水效率。面向后续工作，更具价值的方向是将通信层次扩展到三层及以上、引入实时监控驱动的块大小与流水深度自适应策略，并增强对混合精度与稀疏梯度等异构训练条件的感知能力。

== 本章小结

本章聚焦“域内快、域间慢”导致的跨节点通信瓶颈问题，提出了基于流水线的层次化 All-Reduce 调度方案。方法层面，将 All-Reduce 分解为域内归约、域间归约与域内广播三阶段，并通过分块、双流与事件依赖构建预热—主流水—冷却的稳定流水线执行框架；工程层面给出在 PyTorch/NCCL 下的可落地实现与关键参数选择（如 chunk_size 的区间化配置）。实验在多节点 GPU 集群中显示该方案显著缩短通信等待并提升端到端训练效率，取得约 1.35× 的整体加速；在带宽不对称与高 RTT 条件下收益更明显，验证了方法对跨域拓扑的针对性与稳定性。综合来看，本章方案在不改变优化语义的前提下，通过调度重排与并行重叠有效缓解通信尾部，为后续量化与重叠机制提供了可叠加的通信加速基础。

#pagebreak()