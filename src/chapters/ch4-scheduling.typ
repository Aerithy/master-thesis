#import "../../template/lib.typ": multicite
#import "../../template/src/algorithm.typ": pseudocode-list

= 基于流水线的层次化梯度通信调度策略

== 引言

随着分布式训练规模持续扩大，梯度同步不再只是简单的数据交换过程，而逐渐成为决定系统端到端效率的关键环节。尤其在多节点、异构互联以及云边端协同场景中，不同通信链路之间的带宽、时延和稳定性差异会显著放大同步阶段的等待开销。仅依赖传统扁平化 All-Reduce 难以充分利用节点内高速互联，也难以避免跨节点或跨域链路造成的通信尾部阻塞。因此，本章从异构集群通信特征出发，分析传统同步算法的局限，并提出面向分层拓扑的流水线化层次 All-Reduce 优化方法。

=== 异构集群通信特征

在分布式深度学习训练中，梯度同步的通信开销已成为制约训练效率的关键瓶颈 @pytorch_distributed2020。特别是在多节点集群环境中，通信架构表现出明显的异构性，如@fig:comm-asymmetry。节点内（Intra-node）通信通过高速 NVLink 或 PCIe 完成，带宽可达数百 GB/s；而节点间（Inter-node）通信受限于以太网或 InfiniBand 带宽，通常仅为 100-200 Gb/s #multicite[@nvidia_nvlink_v3_2021 @mellanox_hdr200]。这种带宽不对称性（Bandwidth Asymmetry）可能达到 10:1。这使得跨节点通信成为通信的主要性能短板。

#figure(
  image("../../image/ch4-comm-asymmetry.svg", width: 95%),
  caption: [异构带宽不对称性导致的通信瓶颈]
) <fig:comm-asymmetry>

在云边端协同训练中，这种异构性会进一步跨域化：云内与边缘域内往往具备较高带宽互联（如 RDMA/以太网集群），但边缘到云、边缘到边缘的跨域链路带宽更低且 RTT 更高；端到边链路还可能受到无线接入与动态拥塞影响，抖动显著。因而，梯度同步往往需要跨越至少一条“低带宽/高 RTT”的跨域链路，导致同步尾部更突出，也使得如何在分层拓扑上合理调度集合通信成为云边端协同训练的关键系统问题。

=== 传统同步算法的局限

传统的 All-Reduce 操作通常采用同步阻塞方式。所有进程必须在此阶段等待通信完成才能继续进行下一轮迭代的计算 @rabenseifner2004。在异构网络环境下，这种扁平化的通信模式会导致高速的节点内互联被低速的节点间链路拖累，造成计算资源闲置。

为解决上述问题，本研究提出了一种基于流水线的层次化 All-Reduce 方法（Pipelining Hierarchy All-Reduce），通过张量分块、双流并行和层次化通信策略，实现计算与通信的重叠，从而降低梯度同步延迟。


== 问题分析与研究思路

在分布式深度学习训练任务中，梯度同步作为迭代收敛的必要环节，其通信效率直接决定了整体训练吞吐。当训练任务部署于异构集群时，节点内高速互联与节点间低速链路形成的带宽差异，使得在梯度同步的过程中，集合通信无法充分利用带宽，从而导致训练各进程因等待跨节点通信完成而长时间等待，而高速域内带宽却因受限于低速域间带宽无法被充分利用。造成该现象的原因可归结为以下两个方面：

一方面，传统 All-Reduce 算法（如 Ring 或 Tree 拓扑）采用扁平化的通信组织方式，将所有参与进程置于同一逻辑平面进行数据交换 @rabenseifner2004。这种设计在同构网络环境下能够充分发挥集体通信的理论带宽，但在带宽不对称的异构集群中，慢链路会成为整个通信组的瓶颈。具体而言，当节点间链路带宽仅为节点内的 1/10 甚至 1/50 时，跨节点数据传输耗时将主导同步总时间，而节点内高速互联在完成本地聚合后需被动等待，造成计算资源与通信资源的双重闲置。

另一方面，现有通信优化方案在应对异构拓扑时存在适应性局限。梯度压缩类方法 #multicite[@dgc2018 @bytepscompress2021] 通过降低传输数据量缓解带宽压力，关键的是，这些上述方法大多将集合通信视为原子操作进行整体优化，未能挖掘在集合通信内部，存在域内 - 域间通信并行的潜力。

然而不同于通用的分布式任务调度，梯度同步通信的调度具有强数据依赖与严格一致性约束：本地归约必须在跨节点交换前完成，全局聚合结果必须在广播前就绪。这种模式下，域内通信和域间通信在通信语义上是要求要串行的。同时，面向深度学习模型的集合通信具有一定的启动开销，若调度粒度过小，大量的通信事件将会占用 CPU 处理能力，通信过程将会受限于 CPU，若调度粒度过大则调度前后的效率提升不明显。这种权衡关系为异构环境下的通信调度带来了额外的参数敏感性与调优复杂度。

因此，在带宽不对称的异构集群中进行梯度同步时，需同时满足三重目标：在通信算法层需要匹配域内快、域间慢的物理互联特征来设计通信算法，在调度时序层面需要实现域内通信与域间通信操作的有效重叠，在控制层保障数据依赖正确性与工程可实现性。现有方案主要侧重通信拓扑的适配或是算法本身的优化，缺乏专用于在跨域场景下的优化工作，尚未形成在跨域场景下进行训练时针对集合通信操作的系统设计。

本章所研究问题的主要难点与挑战在于：

（1）如何在保障数据一致性的前提下实现层次化通信的流水线重叠。梯度同步的三阶段（本地归约→跨节点归约→本地广播）存在严格的前后依赖，需设计轻量且精确的同步原语，在解耦域内/域间操作的同时避免引入额外等待。

（2）如何动态选择分块通信的粒度以平衡并行收益与调度开销。块大小直接影响流水线深度与集合通信启动频率，需建立与张量规模、网络带宽、硬件特性相适应的自适应策略，避免静态配置在异构场景下的性能波动。

（3）如何将算法机制无缝集成至主流训练框架。优化方案需在最小侵入的前提下兼容 PyTorch DDP 等现有接口，支持梯度累积、混合精度等常见训练模式，并提供降级路径以保障系统鲁棒性。

针对以上问题，本文研究了一种基于流水线的层次化 All-Reduce 调度策略。该方法首先将通信拓扑抽象为"本地组 - 跨节点组"两级结构，将慢链路通信压缩至代表进程路径；进而通过张量分块构建预热 - 主流水 - 冷却三阶段执行框架，利用双流并行与 CUDA Event 依赖同步实现本地归约、跨节点归约与本地广播的时序重叠；最后完成与训练循环的工程级集成。通过拓扑匹配、时序重排与系统协同的多层优化，本章方案旨在不改变优化语义的前提下，有效缓解异构集群中的通信尾部延迟，为云边端协同训练提供可叠加的通信加速基础。

=== 层次化通信拓扑

如@fig:hierarchical-topo 所示层次化通信拓扑，本方法将分布式训练环境划分为两层通信组：其中，本地组由同一节点内的 GPU 进程构成，依赖 NVLink/PCIe 等高带宽互联完成域内聚合；跨节点组则由各节点代表进程（通常为 Rank 0）组成，负责跨节点数据交换与同步。

层次化通信将 All-Reduce 操作分解为三个阶段：在执行顺序上，系统先在节点内完成本地归约（Local Reduce），再由代表进程执行跨节点归约（Inter Reduce），最后将聚合结果在节点内广播（Local Broadcast）到其余 GPU。该分解方式能够把慢链路上的同步数据量压缩到代表进程粒度，并将更多通信负载留在高带宽域内链路上。

#figure(
  image("../../image/ch4-hierarchical-topology.png", width: 97%),
  caption: [层次化 All-Reduce 通信拓扑与数据流向]
) <fig:hierarchical-topo>

从云边端协同的角度看，上述两层抽象可自然映射为“域内/域间”两级：Local Group 对应云内或边缘域内的高带宽通信组，Inter Group 对应跨域（边缘↔云或边缘↔边缘）的代表进程通信组。这样，层次化 All-Reduce 能将跨域通信压缩为少量代表进程的数据交换，同时尽可能把域内高带宽链路用于聚合与分发，从拓扑上契合云边端协同的分层互联特征。


=== 流水线分块策略

算法首先将待传输的梯度张量分割为 $N$ 个固定大小的块（chunks），记为 ${C_0, C_1, ..., C_(N-1)}$。分块的目的在于将原本一次性完成的大规模梯度同步拆解为多个较小粒度的通信任务，使得不同通信阶段能够以流水线方式交错执行，从而提高链路利用率并减少同步尾部等待。对于大规模梯度张量而言，若直接对完整张量执行 All-Reduce，则必须等待整个张量完成跨节点传输和规约后才能进入下一阶段；而分块后，前一个梯度块完成节点内规约后即可立即进入跨节点通信阶段，后续块则可以继续执行本地规约或等待调度，从而形成连续的数据流。

分块粒度由参数 `chunk_size` 控制，其大小直接影响流水线并发度与通信调度开销之间的平衡。首先，从流水线并发度角度看，若 `chunk_size` 过大，则张量被划分出的块数较少，流水线深度不足，不同通信阶段之间难以充分重叠，跨节点通信延迟仍会暴露在关键路径上。此时算法退化为接近传统层次化 All-Reduce 的执行方式，无法有效隐藏低速链路带来的等待开销。其次，从系统调度与启动开销角度看，若 `chunk_size` 过小，则块数 $N$ 过多，每个块都需要触发独立的通信调度、缓冲区管理和 CUDA stream 同步，集合通信原语的启动开销会显著累积。当启动开销超过分块带来的重叠收益时，过细粒度的切分反而会降低整体通信效率。

因此，`chunk_size` 的选择需要在“足够多的块以形成流水线”和“避免过多小块导致调度开销放大”之间取得折中。本文实验中采用自适应分块策略，根据梯度张量规模动态确定块大小，使每个张量通常被划分为 4 到 8 个块：

$
"chunk_size" = "tensor_size" / (4 tilde 8)
$

其中，$"tensor_size"$ 表示当前梯度张量的元素数量或字节规模，$4 tilde 8$ 表示经验上较优的分块数量区间。该设置能够在保证流水线具备一定深度的同时，避免过多通信任务引入额外调度负担。对于较大的梯度张量，分块可以提供更充分的通信重叠空间；对于较小的梯度张量，算法则避免过度切分，以减少启动和同步开销。通过这种自适应策略，系统能够在不同模型层、不同张量规模以及不同网络条件下保持较稳定的通信效率。


=== 双流并行机制

在底层硬件调度中，GPU 默认将提交的计算或通信任务排列在单一执行队列中串行执行，这导致不同层级的通信任务无法在物理时间上真正重叠。为充分利用硬件多并发能力，本算法显式创建了两个独立的 CUDA 流（CUDA Stream，即 GPU 上的独立异步指令队列）：

节点内流（Intra-stream）专用于调度处于高带宽互联下的本地归约与广播操作；节点间流（Inter-stream）则专用于调度处于低带宽链路的跨节点归约操作。由于这两种操作在使用网络资源侧重点不同（例如局部总线与跨设备网卡），它们在底层硬件上具备同时运作的客观条件。

为确保跨流操作的数据一致性（例如，必须在本地块归约完成之后，代表节点才能将该块数据发往外部进行跨节点归约），两条流之间引入了 CUDA Event 作为轻量级同步原语。借助该原语，算法在保障各数据块前后逻辑依赖绝对正确的前提下，最大化了两条通信链路的调度重叠率。

== 详细方案设计与实现

从执行行为上看，算法由预热、主流水和冷却三个连续阶段构成，如@fig:pipeline-timing 所示。

#figure(
  image("../../image/ch4-multistream-hierarchical-allreduce.png", width: 100%),
  caption: [流水线层次化 All-Reduce 时序图（ar: All-Reduce, bc: Broadcast）]
) <fig:pipeline-timing>

=== 阶段一：预热阶段

预热阶段启动前两个块的处理流程。首先启动第一个块的本地归约操作。如@code:warmup 所示，该阶段的主要目的在于启动集合通信流水：第一个块完成本地归约后立即开始跨节点归约，同时第二个块开始本地归约，实现两个操作的并行执行。

#figure(
  kind: "algorithm",
  // placement: top,
  supplement: [算法],

  pseudocode-list(booktabs: true, numbered-title: [预热阶段流水线启动], full: true)[
    - *输入：* $"chunks"$，$"local_group"$，$"inter_group"$，事件列表
    - *输出：* 初始化流水线依赖图

    + 在节点内流启动 $C_0$ 的本地归约：`all_reduce(chunks[0], group=local_group)`
    + 记录 `event_list_intra[0]`
    + 在节点内流启动 $C_1$ 的本地归约：`all_reduce(chunks[1], group=local_group)`
    + 记录 `event_list_intra[1]`
    + 在节点间流等待 `event_list_intra[0]`
    + 在节点间流执行 $C_0$ 跨节点归约：`all_reduce(chunks[0], group=inter_group)`
    + 执行 `barrier(local_group)` 保证同节点进度一致
    + 记录 `event_list_inter[0]`
  ],
) <code:warmup>



=== 阶段二：流水线主循环

对于剩余的块 $i in [2, N-1]$，并行执行三个操作：

#figure(
  kind: "algorithm",
  // placement: top,
  supplement: [算法],

  pseudocode-list(booktabs: true, numbered-title: [流水线主循环], full: true)[
    - *输入：* 块索引 $i in [2, N-1]$，事件列表，$"local_group"$，$"inter_group"$
    - *输出：* 完成所有中间块的重叠调度

    + *for* $i = 2$ *to* $N-1$ *do*
      + ▷ 节点内流：并行执行广播和本地归约
      + 等待 `event_list_inter[i-2]` #h(1em) ▷ 等待 $C_(i-2)$ 跨节点归约完成
      + `broadcast(chunks[i-2], group=local_group)` #h(1em) ▷ 广播 $C_(i-2)$
      + `all_reduce(chunks[i], group=local_group)` #h(1em) ▷ 本地归约 $C_i$
      + 记录 `event_list_intra[i]`
      + ▷ 节点间流：跨节点归约
      + 等待 `event_list_intra[i-1]` #h(1em) ▷ 等待 $C_(i-1)$ 本地归约完成
      + *if* 当前进程是节点代表 *then*
        + `all_reduce(chunks[i-1], group=inter_group)`
      + *end*
      + `barrier(local_group)`
      + 记录 `event_list_inter[i-1]`
    + *end*
  ],
) <algo:pipeline-main>

如@algo:pipeline-main 所示，该阶段是算法的核心，其并行性体现在同一时刻由节点内流处理块 $i-2$ 的广播与块 $i$ 的本地归约，同时由节点间流处理块 $i-1$ 的跨节点归约；通过 CUDA Event 的精确依赖同步，上述三个操作能够在保持正确性的前提下实现稳定重叠。

=== 阶段三：冷却阶段

处理最后两个块的广播和跨节点归约。如@code:cooling 所示，该阶段负责处理流水线末尾的收尾流程，确保最后两个块的跨节点归约与广播能够正确完成。

#figure(
  kind: "algorithm",
  // placement: bottom,
  supplement: [算法],
  pseudocode-list(booktabs: true, numbered-title: [冷却阶段收尾流程], full: true)[
    - *输入：* 事件列表与最后两个块索引
    - *输出：* 完成全部块的传播闭环

    + 等待 `event_list_inter[N-2]`
    + `broadcast(chunks[N-2], group=local_group)` #h(1em) ▷ 广播倒数第二块
    + 等待 `event_list_intra[N-1]`
    + `all_reduce(chunks[N-1], group=inter_group)` #h(1em) ▷ 跨节点归约最后一块
    + `barrier(local_group)`
    + 等待 `event_list_inter[N-1]`
    + `broadcast(chunks[N-1], group=local_group)` #h(1em) ▷ 广播最后一块
  ],
) <code:cooling>



== 理论性能与开销分析

=== 时间复杂度

传统 All-Reduce 方法的总时间为各阶段时间之和：

$ T_"total" = T_"local" + T_"inter" + T_"broadcast" $

而流水线 All-Reduce 方法通过并行执行三个操作，理论加速比为：

$ "Speedup" = (T_"local" + T_"inter" + T_"broadcast") / (max(T_"local", T_"inter", T_"broadcast") + O(N_"chunks")) $

当块数足够多且三个操作耗时相近时，可接近 1.5× 加速。

=== 空间复杂度

额外内存开销分析：额外开销主要来自 CUDA Event 对象管理，其规模约为 $O(N_"chunks")$；而张量分块本身采用视图操作，不引入新的实质性数据拷贝。PyTorch 等框架中的 Tensor Chunk 仅创建新的 Metadata 视图（View）而不拷贝底层显存，因此对动辄数十 GB 的模型梯度而言，仅有的几十个轻量级 CUDA Event 对象所占用的系统内存几乎可以忽略不计。因此总体空间复杂度仍为 $O(N_"chunks")$。

== 系统集成与工程优化

#figure(
  image("../../image/ch4-system-integration-dataflow.png", width: 98%),
  caption: [流水线调度器在训练流程中的集成架构]
) <fig:system-integration>

系统集成如@fig:system-integration 所示，主要分为控制层，训练执行层，通信调度层；分别负责管理训练配置，梯度数据获取，通信原语调度。首先，为降低频繁触发底层通信原语所带来的系统调用与握手开销，本方法引入了梯度展平（Tensor Flattening）机制。在深度神经网络中，模型参数通常由众多尺寸不一的离散张量构成，直接对独立的参数梯段进行流水线分块不仅无法形成有效的并发深缩，反而会使通信栈过载。系统通过预先在发送缓冲区（Send Buffer）分配一块连续的底层显存空间，并在计算结束后将各层梯度视图按固定偏移拷贝至该大块缓冲中（`TensorBuffer`）。这不仅确保了后续流水线分块（Chunks）的物理连续性，也使通信带宽利用率得以大幅提升。其次，对于采用梯度累积（Gradient Accumulation）的大规模训练场景，系统在设计上将通信触发时机与局步步数（Local Steps）深度绑定。如@algo:integration 所示，仅当 `global_iteration` 满足目标累积步数要求时，才会执行展平与随后的流水线同步逻辑。在其它的局部累加步骤中，各工作节点仅在本地进行正常的矩阵运算累加，通信栈保持等待。最后，系统保留了原生扁平通信的后备降级路径。可以通过配置开关在并行流水线调度与传统的 `all_reduce` 之间进行运行时切换。这种透明化集成策略将通信协议管理的复杂度限制在底层引擎，对上层用户训练脚本而言并无侵入性。

#figure(
  kind: "algorithm",
  // placement: top,
  supplement: [算法],
  
  pseudocode-list(booktabs: true, numbered-title: [流水线层次化 All-Reduce 集成], full: true)[
    - *输入：* 梯度列表 $"grads"$，本地步数 $"local_steps"$
    - *输出：* 聚合后的梯度缓冲

    + *if* $"global_iteration" mod "local_steps" = 0$ *then*
      + ▷ 累积梯度到发送缓冲区
      + *for* 每个梯度 $"grad"_i$ *do*
        + $"send_buffer"_i <- "grad"_i$
      + *end*
      + ▷ 展平所有梯度为连续张量
      + $"flat_buffer" <- "TensorBuffer"("send_buffers")$
      + *if* 启用流水线通信 *then*
        + `pipelining_all_reduce(`$"flat_buffer"$`,` $"local_group"$`,` $"inter_group"$`,` $"chunk_size"$`)`
      + *else*
        + `all_reduce(`$"flat_buffer"$`)`
      + *end*
    + *end*
  ],
) <algo:integration>

== 实验与验证

本章围绕流水线层次化 All-Reduce 方法开展实验验证，重点评估其在异构集群和跨域链路下的通信优化效果。实验从通信微基准、端到端训练性能、分块参数敏感性以及带宽不对称影响等多个角度展开，以验证方法收益的稳定性与适用范围。为保证结论可靠，本章同时在 NVIDIA A100 云集群和 Iluvatar CoreX 国产 GPU 物理集群上进行测试，考察方法在不同硬件后端下的可迁移性。通过与 PyTorch 原生 All-Reduce 和朴素层次化 All-Reduce 对比，进一步分析性能提升来源是否来自拓扑分层与流水线重叠的共同作用。

=== 实验环境

本章实验涉及两类硬件环境，如@tab:ch4-exp-envs 所示。实验环境一为 NVIDIA A100 云上双节点集群，节点内 GPU 通过 PCIe 全互联，网卡使用 IB 网口；节点内 PCIe 有效带宽约为 126 Gb/s，节点间互联带宽约为 50 Gb/s。软件侧采用 PyTorch/NCCL/CUDA 分布式训练栈，并固定通信后端、batch 配置和日志采集脚本，以保证不同通信策略之间仅在集合通信路径与调度方式上存在差异。任务侧采用 ResNet-50 与 ImageNet 组合，并设置每 GPU batch 为 256 #multicite[@he2016resnet @deng2009imagenet]。对比对象包括 PyTorch 原生 All-Reduce 与无流水线的朴素层次化 All-Reduce。实验环境二为天数智芯（Iluvatar CoreX）国产 GPU 物理集群，节点 n210 对应 Domain-1，节点 n62 对应 Domain-2；每节点 CPU 为 Intel Xeon Gold 6330，GPU 为 Iluvatar BI-V150 x 8，板卡内部各包含 2 个 GPU 芯粒。该环境中 GPU 两两成对走板内集成互联路径，每对 GPU 之间经 PCIe Switch 互联且不跨 NUMA，所有 GPU 位于同一 NUMA，NIC0 挂载在另一 NUMA 节点上；节点间通过 Ethernet 互联，节点内全 PCIe 互联带宽约为 126 Gb/s，节点间互联带宽约为 12.5 Gb/s。由于该环境的通信后端、网卡位置与节点间链路均不同于 A100 云集群，本章将其作为异构硬件可迁移性验证。

#figure(
  table(
    columns: (0.9fr, 1.6fr, 1.6fr, 1.2fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*环境*], [*计算节点与 GPU*], [*节点内互联*], [*节点间互联*]),
    table.hline(stroke: 0.5pt),
    [实验环境一], [2 节点 NVIDIA A100 云集群], [PCIe 全互联，约 126 Gb/s], [IB 网口，约 50 Gb/s],
    [实验环境二], [2 节点 Iluvatar BI-V150], [同 NUMA PCIe，板内芯粒成对互联，约 126 Gb/s], [Ethernet，约 12.5 Gb/s],
    table.hline(),
  ),
  caption: [第四章实验硬件环境]
) <tab:ch4-exp-envs>

// Data source: image/csv/ch4_tab_hierarchical_links.csv
#figure(
  table(
    columns: (1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*链路层级*], [*带宽（示例）*], [*RTT（示例）*]),
    table.hline(stroke: 0.5pt),
    [域内（云内/边缘域内）], [100–200 Gb/s], [< 1 ms],
    [域间（边缘↔云/边缘↔边缘）], [10–30 Gb/s], [30–100 ms],
    table.hline(),
  ),
  caption: [用于云边端协同场景的分层链路仿真参数]
) <tab:hierarchical-links>

为体现本文方法在跨域互联中的适用性，实验还采用网络整形对应的域间链路注入带宽约束，用于浮现训练时的广域网特征；域内链路仍保持域内高带宽互联，以刻画典型的域内快、域间慢的云边端跨域训练环境，如@tab:hierarchical-links。该设置重点用于验证层次化流水线在跨域瓶颈下的调度收益与敏感性趋势。



=== 通信性能对比

@tab:comm-perf 展示了不同方法的通信性能对比。

// Data source: image/csv/ch4_tab_comm_perf.csv
#figure(
  table(
    columns: (1fr, 1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*方法*], [*通信耗时 (ms)*], [*相对基线*], [*GPU 利用率*]),
    table.hline(stroke: 0.5pt),
    [PyTorch 原生], [128.5], [1.00×], [68%],
    [朴素层次化], [91.3], [0.71×], [76%],
    [流水线层次化], [74.1], [0.58×], [89%],
    table.hline(),
  ),
  caption: [不同 All-Reduce 方法的通信性能对比]
) <tab:comm-perf>

#h(0em) 从结果看，流水线层次化方法相对 PyTorch 原生通信路径将通信耗时降低了 42.3%，并将 GPU 利用率从 68% 提升到 89%；即便与朴素层次化方法相比，流水线机制仍额外带来 18.8% 的通信时间缩减，说明收益不仅来自拓扑分层，还来自时序重排与并行重叠。

这一结果可以从拓扑匹配 + 时序重叠两个维度解释。朴素层次化主要解决的是拓扑不匹配问题，即将经由慢链路通信的数据块数降低从而降低由于高RTT和低带宽导致的通信效率低的问题；流水线层次化则进一步重排执行时序，把本地归约、跨节点归约和本地广播并行化，从而进一步提升了在整体网络环境下的带宽利用率。GPU 利用率提升与通信耗时下降是因为，同步通信缩短后，计算流等待通信完成的空转时间减少，GPU 能够更连续地执行前后向计算。由此，通信层优化直接转化为计算资源利用效率提升，从而可以提升整体训练效率。

=== 端到端训练加速

@tab:e2e-speedup 展示了端到端训练性能的提升。

// Data source: image/csv/ch4_tab_e2e_speedup.csv
#figure(
  table(
    columns: (1fr, 1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*方法*], [*迭代时间 (ms)*], [*吞吐指标 (items/s)*], [*加速比*]),
    table.hline(stroke: 0.5pt),
    [PyTorch 原生], [445.2], [4,602], [1.00×],
    [朴素层次化], [408.0], [5,024], [1.09×],
    [流水线层次化], [329.7], [6,216], [1.35×],
    table.hline(),
  ),
  caption: [端到端训练性能对比]
) <tab:e2e-speedup>

端到端结果进一步验证了通信优化能够转化为训练层面的实质收益。相较通信耗时指标，迭代时间和吞吐更接近业务目标，因此其改善更具说服力。数据表明方法从减少通信等待推进到缩短训练迭代。将端到端收益与前述通信收益对照可发现，两者量级并非线性一致，这符合真实训练系统特征：迭代时间由计算、通信、数据流水等多环节共同决定，通信下降会被其余环节部分吸收。即便如此仍能获得显著加速，说明通信在当前设置中确实是主要瓶颈之一。此外，朴素层次化与流水线层次化形成了清晰的递进关系，避免了仅与单一基线比较导致的解释歧义。先证明拓扑分层有效，再证明时序流水化在分层基础上继续增益，使结论链条更完整：收益来源可分解、可解释、可复现。

#figure(
  image("../../image/ch4-e2e-results.svg", width: 98%),
  caption: [端到端训练性能对比与耗时分解]
) <fig:e2e-result>

=== 块大小敏感性分析

#figure(
  image("../../image/ch4-chunk-sensitivity.svg", width: 98%),
  caption: [块大小对通信性能的影响]
) <fig:chunk-sensitivity>

@fig:chunk-sensitivity 展示了不同块大小对性能的影响。敏感性结果显示，块数在 4-8 区间时性能最优，对应块大小约为张量尺寸的 $1/4 tilde 1/8$；当块数低于 4 时，并行深度不足，通信难以被充分掩盖；而当块数增加到 16 时，启动开销与事件同步开销已经超过流水线重叠收益，开始出现负优化。

块大小敏感性结果揭示了典型的并行深度与调度开销的权衡。块数过少时，流水线阶段不足以覆盖跨节点归约延迟，重叠窗口利用不充分；块数过多时，集合通信启动、事件同步和调度管理开销快速累积，抵消分块带来的并行收益。最优区间的存在说明该方法无法通过无限细化块大小来获得持续收益，需要在系统开销模型下选择稳定工作点。

这一观察直接支撑了本章提出的自适应分块策略。固定块大小难以在不同张量规模和网络条件下同时最优，而以区间方式给出可行工作带，能够在工程部署中降低调参成本，并减少因环境变化导致的性能波动。对于跨域链路波动场景，采用区间内保守配置通常比追求单点最优更稳健。

=== 带宽不对称性影响

@tab:bandwidth-ratio-3factor 分析了节点内外带宽比在固定通信张量下，不同分块数的加速效果。

// Data source: image/csv/ch4_tab_bandwidth_ratio_3factor.csv
#figure(
  image("../../image/ch4-bandwidth-ratio-3factor.svg", width: 96%),
  caption: [固定张量（256 MB）下，不同 chunk 数在带宽不对称场景中的加速比]
) <tab:bandwidth-ratio-3factor>

该实验采用三个变量控制：变量 1（网络条件）：节点内/节点间带宽比（5:1 ~ 30:1）；变量 2（通信张量大小）：固定为 256 MB；变量 3（分块数）：1、2、4、8（对应不同 chunk 粒度）。

#h(0em) 结果表明：流水线层次化方案在轻度到中等带宽不对称场景中收益最明显；当节点内/节点间带宽比继续增大时，节点间慢链路逐渐成为不可隐藏的主瓶颈，加速比随之收敛到 1 附近。在固定 256 MB 通信张量下，chunk=4 取得整体最优结果，chunk=8 与其接近但在极端失衡时受到调度开销影响，而 chunk=1 受并行深度不足限制。该现象表明分块粒度与网络条件存在显著耦合关系，合理选择 chunk 数是低质网络优化的关键。带宽不对称实验给出了本章方法适用性的关键证据。若方法仅在近似同构网络下有效，则其跨域价值有限；当前结果显示，在 2:1 等轻度失衡场景中流水线重叠能够显著缩短等待时间，而在 20:1、30:1 等极端失衡场景中收益逐步收敛，说明层次化流水线能够缓解但不能完全消除跨域慢链路瓶颈。

chunk=4 与 chunk=8 在低到中等不对称区间表现更优，进一步说明跨域慢链路场景下需要足够流水深度来提升重叠效率，但不应无限细化到引入过高调度开销。该结论与块大小敏感性实验形成互证：参数选择应与网络结构联合考虑，而非脱离网络条件做静态配置。

=== 低质网络全面实验设计

为更全面验证层次化集合通信在低质网络下的有效性，补充 4 组针对性实验。四组实验统一采用三种对比方法（PyTorch 原生、朴素层次化、流水线层次化），并保持模型、batch、优化器与并行拓扑一致，仅改变网络条件或系统扰动因素。

实验统一采集四类指标：通信平均耗时、迭代时长、吞吐量、GPU 空闲占比；同时记录跨域链路的有效带宽利用率，用于解释性能差异来源。

==== 组 A：带宽退化实验（Bandwidth Throttling）

目标是验证在纯带宽受限场景下层次化流水线的稳健性，关注在 5-30 Gb/s 区间的收益保持能力。

// Data source: image/csv/ch4_tab_expA_bandwidth_throttle_3factor.csv
#figure(
  image("../../image/ch4-expA-bandwidth-throttle-3factor.svg", width: 96%),
  caption: [实验组 A：跨域带宽退化设置]
) <tab:exp-bandwidth-throttle>

组 A 的分析重点在于验证纯带宽受限条件下的收益保持能力。随着可用带宽逐级下降，跨域归约时延按近似 $1/{B W}$ 规律上升，流水线层次化通过分块重叠与代表进程路径压缩，能够持续降低有效等待时间。若该方法仅在高带宽区间有效，则在低带宽下应迅速失效；图中趋势显示其在退化区间仍保持可观优势，支持本章关于低质链路稳健性的结论。

该组实验同时说明收益来源主要是在跨域路径上的通信效率提升。实验控制了模型与并行配置，仅改变带宽变量，性能变化可主要归因于通信路径。由此可将结论外推到同类型带宽受限场景：当跨域同步主导迭代尾部时，层次化流水线是有效优先策略。

*组 B：高时延实验（RTT Escalation）：*目标是隔离 RTT 对同步尾部的影响，验证流水线调度是否能持续隐藏高 RTT 带来的阻塞。

组 B 用于隔离 RTT 对同步尾部的影响。高 RTT 场景下，collective 启动与轮次同步等待会被显著放大，传统扁平通信更易形成尾部串行堆叠。实验趋势表明，流水线层次化在 RTT 升高时仍能维持较优性能，说明其通过重叠执行与阶段解耦降低了时延放大效应。
// Data source: image/csv/ch4_tab_expB_rtt_escalation_3factor.csv
#figure(
  image("../../image/ch4-expB-rtt-escalation-3factor.svg", width: 96%),
  caption: [实验组 B：跨域 RTT 分级设置]
) <tab:exp-rtt-escalation>


该结果支撑了“跨域高时延场景优先做时序重排”的实践结论。与仅靠提升带宽相比，时序层优化对 RTT 的敏感性更低，因此在无法显著改善物理链路时，仍可通过调度层手段取得稳定收益。这一结论与第5章关于同步尾部治理的思路一致，体现章节间方法协同性。

*国产 GPU 集群验证实验：*

为进一步验证层次化集合通信调度在异构国产 GPU 物理集群中的行为，本章在实验环境二上进行了 All-Reduce 微基准实验。该实验保持两节点、每节点 4 GPU 的分布式进程布局，即在每个物理节点中选取 4 张 GPU 参与测试，以对齐实验环境一的 8 进程规模。实验比较 PyTorch/NCCL 原生 All-Reduce、朴素层次化 All-Reduce 与流水线层次化 All-Reduce 三种路径，并在 4 MB 至 128 MB 的张量规模下统计平均通信耗时与有效吞吐。从@tab:corex-collective-latency 和 @tab:corex-collective-throughput 可以看到，在 CoreX 物理集群中，层次化流水线的收益随张量规模增大而逐渐显现。对于 4 MB 与 16 MB 小张量，PyTorch/NCCL 原生路径分别为 1.56 ms 与 4.73 ms，仍略优于流水线层次化；这是因为小张量下通信启动、进程组调度与事件同步开销占比较高，流水线尚未形成足够长的重叠窗口。对于 64 MB 和 128 MB 张量，流水线层次化分别将耗时降至 12.31 ms 和 21.12 ms，相比原生 All-Reduce 的 17.08 ms 和 33.28 ms 明显更优；其中 128 MB 场景下有效吞吐由 4.03 GB/s 提升至 6.35 GB/s，约为 $1.58 times$。

#figure(
  table(
    columns: (0.8fr, 1fr, 1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*张量规模*], [*PyTorch 原生*], [*朴素层次化*], [*流水线层次化*], [*最快方法*]),
    table.hline(stroke: 0.5pt),
    [4 MB], [1.56 ms], [9.44 ms], [3.11 ms], [PyTorch 原生],
    [16 MB], [4.73 ms], [11.29 ms], [4.95 ms], [PyTorch 原生],
    [64 MB], [17.08 ms], [18.70 ms], [12.31 ms], [流水线层次化],
    [128 MB], [33.28 ms], [27.40 ms], [21.12 ms], [流水线层次化],
    table.hline(),
  ),
  caption: [实验环境二上不同 All-Reduce 路径的通信耗时]
) <tab:corex-collective-latency>

#figure(
  table(
    columns: (0.8fr, 1fr, 1fr, 1fr),
    align: center,
    stroke: none,
    table.hline(),
    table.header([*张量规模*], [*PyTorch 原生*], [*朴素层次化*], [*流水线层次化*]),
    table.hline(stroke: 0.5pt),
    [4 MB], [2.69 GB/s], [0.44 GB/s], [1.35 GB/s],
    [16 MB], [3.55 GB/s], [1.49 GB/s], [3.39 GB/s],
    [64 MB], [3.93 GB/s], [3.59 GB/s], [5.45 GB/s],
    [128 MB], [4.03 GB/s], [4.90 GB/s], [6.35 GB/s],
    table.hline(),
  ),
  caption: [实验环境二上不同 All-Reduce 路径的有效吞吐]
) <tab:corex-collective-throughput>

朴素层次化与流水线层次化的差异进一步说明，收益并不仅来自拓扑分层，还来自阶段之间的时序重叠。以 128 MB 张量为例，朴素层次化耗时为 27.40 ms，而流水线层次化进一步降低到 21.12 ms，说明本地归约、跨节点传输与本地广播按块接力执行后，能够更充分利用节点内 PCIe 与节点间 Ethernet 两类链路。随着张量规模增大，单次通信的数据传输时间占比上升，流水线的预热与冷却开销被摊薄，因此其优势更加明显。

该补充实验为第四章结论提供了本方法更明确的适用范围：层次化流水线更适合中大张量和跨节点通信占比较高的同步阶段，而对于小张量通信，原生 collective 的低启动开销仍具有优势。因此，在国产 GPU 集群等新后端上部署时，不应静态替换所有 All-Reduce 操作，而应根据张量规模、拓扑结构和在线基准测试结果选择通信路径。对于大模型训练中常见的大梯度桶，实验环境二的结果仍支持本文关于“域内快、域间慢”拓扑下采用流水线层次化调度的核心结论。

同时，该实验也指出了后续工程优化方向：一是减少本地组和跨节点组切换的固定开销，二是提高流水线分块的批处理效率，三是在运行时根据张量规模、NUMA 位置和链路带宽选择原生 All-Reduce 或层次化流水线。该方向与前文块大小敏感性实验一致，说明集合通信调度优化需要由硬件拓扑和在线性能共同驱动。


统计检验与可复现性约束：每个子实验至少运行 3 次独立重复并报告均值与标准差；对关键指标（迭代时长、吞吐量）执行配对显著性检验（如 paired t-test），保证结论不依赖单次偶然波动。网络整形参数、随机种子、日志脚本和绘图脚本统一纳入仓库，确保实验可复现。

== 本章小结

本章聚焦“域内快、域间慢”导致的跨节点通信瓶颈问题，提出了基于流水线的层次化 All-Reduce 调度方案。方法层面，将 All-Reduce 分解为域内归约、域间归约与域内广播三阶段，并通过分块、双流与事件依赖构建预热—主流水—冷却的稳定流水线执行框架；工程层面给出在 PyTorch/NCCL 下的可落地实现与关键参数选择（如 chunk_size 的区间化配置）。实验在多节点 GPU 集群中显示该方案显著缩短通信等待并提升端到端训练效率，取得约 1.35× 的整体加速；在轻度到中等带宽不对称场景下收益更明显，而在极端失衡时逐步受限于跨域慢链路瓶颈。CoreX 物理集群补充实验显示，流水线层次化在 64 MB 及以上张量规模下优于原生 All-Reduce，128 MB 场景下有效吞吐提升至约 $1.58 times$，但小张量场景仍受启动开销影响。因此部署时需要结合拓扑探测与在线基准测试动态选择通信路径。综合来看，本章方案在不改变优化语义的前提下，通过调度重排与并行重叠有效缓解通信尾部，为后续量化与重叠机制提供了可叠加的通信加速基础。

#pagebreak()
